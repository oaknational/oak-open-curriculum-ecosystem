/**
 * Shared user-facing copy fragments for structured search states.
 *
 * Keeping messages centralised ensures UI components, server actions,
 * and visual tests stay aligned when we refine tone or accessibility language.
 */
export const STRUCTURED_EMPTY_RESULTS_MESSAGE =
  'No results found. Adjust filters, broaden your scope, or try another search term.';

export const STRUCTURED_FIXTURE_OUTAGE_MESSAGE =
  'Search is temporarily unavailable (503). Switch to live data or try again later.';
