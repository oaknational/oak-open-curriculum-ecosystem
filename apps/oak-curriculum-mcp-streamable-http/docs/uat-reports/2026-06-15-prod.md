# UAT run record — production — 2026-06-15

Run of the [UAT validation runbook](../manual-uat-guide.md) against the live
production server. This is also the **worked example** of a filled record.

```text
Oak Curriculum MCP — UAT run record
Target:        https://curriculum-mcp-alpha.oaknational.dev
Upstream API:  0.7.0 (2026-05-21)   [from 2.2 get-changelog-latest]
App version:   not separately exposed to the agent client this run
Date (UTC):    2026-06-15
Run by:        AI agent via Cursor's built-in MCP client (OAuth seat)
Mode:          full matrix

Section 0  Inventory self-check ......... PASS   (tools: 37  resources: 4  prompts: 7)
Section 1  Transport & auth ............. N-A    (validated via authenticated client; curl baseline not re-run this pass)
Section 2  Orientation .................. PASS
Section 3  Discovery & browse ........... PASS
Section 4  Search ....................... PASS
Section 5  Fetch ........................ PASS
Section 6  Lesson/unit/sequence depth ... PASS   (get-lessons-summary; other depth tools not individually probed)
Section 7  Graph tools .................. PASS
Section 8  EEF evidence ................. PASS
Section 9  Assets & downloads ........... N-A    (not probed this run)
Section 10 Resources .................... PASS
Section 11 Prompts ...................... N-A    (7 descriptors present; agent-side invocation unavailable in Cursor)
Section 12 Error & filter behaviour ..... PASS
Section 13 MCP App / widget (optional) .. N-A    (no MCP Apps host this run)

Response-shape contract: dual content + structuredContent confirmed? YES
Verdict: GO   (no open P0/P1)
```

## Per-section evidence

- **0 — Inventory.** `tools/list` / `resources/list` / `prompts/list` reconciled
  against Appendix A: 37 tools, 4 resources, 7 prompts. No drift.
- **2 — Orientation.** `get-curriculum-model` returned the 42 KB domain model
  (dual shape; summary block + diverted JSON). `get-changelog-latest` →
  `0.7.0` / `2026-05-21`. `get-rate-limit` → `{limit:0, remaining:0, reset:0}`
  (unlimited for the internal principal).
- **3 — Discovery.** `browse-curriculum {science, ks3}` → 41 units / 290
  lessons. `get-subjects` → 17 subjects.
- **4 — Search.** `search {lessons, "photosynthesis", science, ks3}` → 3 ranked
  hits with highlights and fetchable slugs. `search {threads, maths}` → threads
  sorted by unit count (`number` 110, `geometry-and-measure` 59, …).
- **5 — Fetch.** `fetch lesson:photosynthesis` → full lesson document.
- **6 — Depth.** `get-lessons-summary {photosynthesis}` → full summary record
  (keywords, key learning points, misconception + response, teacher tips).
- **7 — Graph tools (working-with-graphs checklist satisfied):**
  - `get-thread-progressions {maths, ks2}` → 10 bounded thread descriptors,
    no sequences, kind-qualified ids, anchor echoed.
  - `get-prior-knowledge-graph {[plant-nutrition-and-photosynthesis], depth:1}`
    → 2 units / 1 `prerequisiteFor` edge; endpoints are members;
    `resolvedAnchors` / `unknownAnchors` / `depth` echoed.
  - `get-misconception-graph {[plant-nutrition-and-photosynthesis]}` → 4 lessons
    with misconceptions; one lesson honestly returns `[]`; anchors echoed.
  - `get-keyword-graph {maths, ks2, limit:5}` → top 5 of 405, honest totals
    (`totalMatchingKeywords:405`, `hasMore:true`), per-entry lessons with
    `hasMoreLessons`, ranked by in-scope placement count.
- **8 — EEF.** `get-eef-evidence inspect-strand eef-tl-feedback` → **full dual
  shape, rendered in Cursor.** Corpus values verbatim: `+6` months / `Very Low`
  cost / `Extensive` evidence; full `provenance.source` incl. `original_authors`
  (Higgins, Katsipataki, Kokotsaki, Coleman, Major, Coe); caveats present.
  `eef://interpretation` resource → full markdown interpretation guide with the
  complete 30-strand index.
- **10 — Resources.** `curriculum://model` → 61 KB JSON (`domainModel`,
  version `0.1.0-poc`). `docs://oak/getting-started.md` → markdown intro.
  `eef://interpretation` → markdown (see §8).
- **12 — Error & filter.**
  - `search {}` (no scope) → JSON-RPC `-32602` input-validation error.
  - `get-keyword-graph {…, limit:0}` → JSON-RPC `-32602` (schema bound
    `[1,100]` enforced at the input layer).
  - `get-eef-evidence evidence-for-move` (no selector) → typed refusal
    ("requires at least one selector …").
  - `get-misconception-graph {unitSlugs, unitOffset}` → handler typed refusal
    ("unitOffset/unitLimit apply to the thread anchor only").
  - `fetch lesson:<invalid>` → "Lesson not found".

## Findings

- **[Resolved confirmation] EEF visibility in Cursor.** The historical P1 —
  `get-eef-evidence` success rendering as `(omitted)` in content-only clients —
  is fixed in production: the dual response shape is live and the tool renders
  fully in Cursor. No action.
- **[Runbook fix, applied] `limit:0` error class.** The draft runbook predicted
  a handler-level typed refusal for `get-keyword-graph limit:0`; live behaviour
  is JSON-RPC `-32602` (the schema declares `[1,100]`, so the bound is enforced
  at the input layer). Runbook rows 7.4 and 12.3 corrected in the same change.

## Coverage not exercised this run (named, not silently skipped)

Section 1 curl baseline, Section 6 depth tools beyond `get-lessons-summary`
(quiz/transcript/assets, sequences, key-stages-subject-\*), Section 9 assets +
`download-asset`, Section 11 prompt retrieval (no agent-side surface in Cursor),
Section 13 widget (no Apps host). None are regressions; they are scope this pass
did not cover. A `local` no-auth run can close Sections 1, 9, and 11 via curl.
