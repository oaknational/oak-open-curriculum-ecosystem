# UAT run record — preview (PR #945) — 2026-09-02

Run of the [UAT validation runbook](../manual-uat-guide.md) against the
Vercel preview of PR #945 (`feat/innovation-kit-updates`, head `6028ac95c`,
which carries the MCP-655 PRM fix). Mode: the quick smoke subset plus the
Section 0 inventory reconciliation and the Section 1 baseline — the
live-service validation the owner required before merging a 320-file change.

```text
Oak Curriculum MCP — UAT run record
Target:        https://poc-oak-open-curriculum-mcp-git-feat-innovation-kit-updates.vercel.thenational.academy
Upstream API:  0.11.0 (read from the live swagger.json; the changelog tools cannot report it — see finding 1)
App version:   not separately exposed to the agent client (no x-app-version header on the metadata routes)
Date (UTC):    2026-09-02
Run by:        AI agent (Luna seeks Twilight) through Claude Code 2.1.258's MCP client on the OAuth seat the owner signed in; Section 1 by curl
Mode:          smoke subset + Section 0 + Section 1

Section 0  Inventory self-check ......... PASS   (tools: 40  resources: 6  prompts: N-A on this channel — see 0.3)
Section 1  Transport & auth ............. PASS   (1.1–1.4 by curl; 1.5 owner sign-in from Claude Code 2.1.258, no MCP_SDK_GENERATION override)
Section 2  Orientation .................. PASS   (2.1 PASS; 2.2 FAIL — pre-existing upstream drift, finding 1)
Section 3  Discovery & browse ........... N-A    (not in the smoke subset)
Section 4  Search ....................... PASS
Section 5  Fetch ........................ PASS
Section 6  Lesson/unit/sequence depth ... N-A    (not in the smoke subset)
Section 7  Graph tools .................. PASS
Section 8  EEF evidence ................. N-A    (get-eef-evidence is a dormant row; absent from tools/list by design)
Section 9  Assets & downloads ........... N-A    (not in the smoke subset)
Section 10 Resources .................... PASS   (resources/list = the six served; docs://oak/guidance/find-lessons.md read back)
Section 11 Prompts ...................... N-A    (0.3 not observable through Claude Code's client — see 0.3)
Section 12 Error & filter behaviour ..... PASS   (12.2)
Section 13 MCP App / widget (optional) .. N-A    (no MCP Apps host this run)

Findings (severity P0–P3, with repro):
- [P2] get-changelog-latest and get-changelog return "Not found - non existent endpoint" — Section 2.2 — repro: call either tool with {} on this preview; the #946 preview (main + the same fix, no other change) fails identically; upstream GET /api/v0/changelog and /changelog/latest answer 404 while GET /api/v0/key-stages answers 401, and the live swagger.json (0.11.0) lists no changelog path against the cached 0.7.0 spec's two — disposition: pre-existing on main, not a change on this branch; owned by MCP-626 and MCP-630 (schema-cache refresh, dead changelog tools) behind the owner decision MCP-653; runbook row 2.2 is trued by that work, not here.

Response-shape contract: dual content + structuredContent confirmed? PARTIAL — the JSON text content was present and well-formed on every call; structuredContent is not surfaced by Claude Code's tool-result rendering, so it is unverified (not failed) on this channel.
Verdict: GO   (no open P0/P1; the one P2 is pre-existing and already owned)
```

## Section 0 evidence

| Row | Observed                                                                                                                                                                                                                                            | Result |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| 0.1 | `tools/list` as loaded by Claude Code at sign-in: 40 tools, exactly Appendix A's 39 universal plus `oak-under-the-hood`; the dormant rows (`get-eef-evidence`, `user-search`, `user-search-query`) absent as expected. No addition, no removal.     | PASS   |
| 0.2 | `resources/list`: `curriculum://model`, `docs://oak/getting-started.md`, `docs://oak/guidance/{find-lessons,explore-curriculum,learning-progression}.md`, `ui://widget/oak-curriculum-app-e9a7101f.html` — the six served, nothing dormant leaking. | PASS   |
| 0.3 | `prompts/list`: Claude Code's client does not expose the raw JSON-RPC response, so the `-32601` assertion is not observable on this channel. No prompt surfaced as a slash command, which is consistent with zero prompts but is not the assertion. | N-A    |

## Section 1 evidence (curl, deployment `poc-oak-open-curriculum-etsziryr6`, Vercel success 08:55:48Z, `preview-serves` success 08:55:59Z)

| Row | Probe                                                                     | Observed                                                                                                                 | Result |
| --- | ------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ | ------ |
| 1.1 | `GET /healthz` and `GET /mcp/healthz`                                     | `200`, `cache-control: no-store` on both                                                                                 | PASS   |
| 1.2 | `GET /.well-known/oauth-protected-resource/mcp`                           | `200`; `resource` = the alias `/mcp`; `authorization_servers` = `["https://native-hippo-15.clerk.accounts.dev"]`         | PASS   |
| 1.3 | unauthenticated `POST /mcp` (initialize)                                  | `401`; `www-authenticate: Bearer resource_metadata="<alias>/.well-known/oauth-protected-resource/mcp"`                   | PASS   |
| 1.4 | 1.2 with `Host: evil.example`                                             | `403`                                                                                                                    | PASS   |
| 1.5 | RFC 9207 issuer alignment (owner-held): Claude Code `/mcp` → Authenticate | Claude Code 2.1.258, `MCP_SDK_GENERATION` unset; "Authentication successful. Connected to oak-preview-945"; tools loaded | PASS   |

Issuer the client held (from the server named in 1.2): Clerk's metadata
serves `issuer: https://native-hippo-15.clerk.accounts.dev` with
`authorization_response_iss_parameter_supported: true`, so the `iss` on the
authorisation response equalled the recorded issuer by construction — the
sign-in that failed on 2026-09-01 with the RFC 9207 issuer mismatch now
completes. The app-origin authorisation-server metadata
(`/.well-known/oauth-authorization-server`) still serves `issuer` = the app
origin with the same claim `true`; the proxy-path projection is out of this
change's scope (MCP-656).

## Smoke subset evidence (through the authenticated Claude Code session, 09:03–09:06Z)

| Row  | Call                                                                                         | Observed                                                                                                                                                                       | Result |
| ---- | -------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------ |
| 2.1  | `get-curriculum-model {}`                                                                    | Orientation returned: `toolGuidance` (categories, workflows, tips, id formats) and `domainModel` 0.2.0 (17 subjects, 4 key stages, threads, property graph); `status: success` | PASS   |
| 2.2  | `get-changelog-latest {}`                                                                    | `Not found - non existent endpoint` — finding 1                                                                                                                                | FAIL   |
| 4.1  | `search { scope: lessons, query: photosynthesis, subject: science, keyStage: ks3, size: 5 }` | 5 ranked KS3 science hits in 1306 ms, each with a fetchable `lesson_slug`, unit ids, thread slugs and highlights; top hit `photosynthesis`                                     | PASS   |
| 5.2  | `fetch { id: "lesson:photosynthesis" }`                                                      | `httpStatus: 200`; lesson summary with keywords, key learning points, misconceptions, teacher tips, `oakUrl`; `status: success`                                                | PASS   |
| 7.2  | `get-prior-knowledge-graph { unitSlugs: ["plant-nutrition-and-photosynthesis"], depth: 1 }`  | Bounded subgraph: 2 units, 1 `prerequisiteFor` edge, `resolvedAnchors` echoed, `unknownAnchors: []`, `depth: 1`                                                                | PASS   |
| 8.x  | EEF rows                                                                                     | `get-eef-evidence` absent from `tools/list` (dormant row)                                                                                                                      | N-A    |
| 12.2 | `search {}`                                                                                  | JSON-RPC error `-32602` "Input validation error: Invalid arguments for tool search … at scope" — the server rejects bad input                                                  | PASS   |
| 12.x | `search { scope: lessons }` (schema-valid, semantically invalid)                             | Tool-level error "search requires a non-empty query (threads scope can omit query when subject or keyStage filter is provided)" — graceful, not a 5xx                          | PASS   |

Controls run alongside: the same two changelog calls against the #946 preview
(main plus the same fix) fail identically, and the upstream route probes above
locate the cause upstream. No 5xx and no auth loop was seen on any call.
