#!/usr/bin/env bash
#
# scripts/log-commit-attempt.sh
#
# Commit-attempt logging is currently paused by owner direction.
# Keep this helper in place so it can be re-enabled quickly without
# needing to recover the path or filename from git history.
#
# Intentionally succeed for any invocation so stale local workflows
# do not fail while the commit guidance is being simplified.
exit 0
