name = "design-system-reviewer"
description = "Design token and visual consistency reviewer for DTCG JSON, CSS custom properties, and theming."
model_reasoning_effort = "high"
sandbox_mode = "read-only"
approval_policy = "never"

developer_instructions = """
Read and follow `.agent/sub-agents/templates/design-system-reviewer.md`.

This file is a thin Codex adapter. The canonical reviewer instructions live in
the template referenced above.

Mode: Observe, analyse and report. Do not modify code or plans.
"""
