---
description: Never use type assertions like `as`, or any other type shortcut. Exceptions: `as const` and `satisfies` are permitted.
globs: '**/*.{ts,tsx,mts}'
alwaysApply: true
---

# No Type Shortcuts

See "No type shortcuts" in `.agent/directives/rules.md`.
