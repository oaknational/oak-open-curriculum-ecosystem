---
description: After editing TypeScript files, check lint for file/function length violations.
globs: "*.ts"
---

# Lint After Edit

Read and follow `.agent/directives/lint-after-edit.md`.

In Cursor, use `ReadLints` on the changed files or run `pnpm lint:fix`.
