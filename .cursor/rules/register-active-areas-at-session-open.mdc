---
description: >
  Before any edit in a session, list intended areas and scan
  active-claims.json for overlap. On overlap, consult the shared
  communication log and decide how to coordinate before proceeding.
  Then register the session's own claim. Before staging or committing,
  use the advisory commit queue and a short-lived git:index/head claim.
  Advisory, not refusal.
alwaysApply: true
---

Read and follow `.agent/rules/register-active-areas-at-session-open.md`.
