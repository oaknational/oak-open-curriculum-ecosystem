---
paths:
  - '**/*.{ts,tsx,mts}'
---

# No Type Shortcuts

Never use type assertions (`as`), `any`, or other type shortcuts.
Exceptions: `as const` and `satisfies` are permitted.

See "No type shortcuts" in `.agent/directives/rules.md`.
