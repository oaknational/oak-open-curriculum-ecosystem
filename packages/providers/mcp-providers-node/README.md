# @oaknational/mcp-providers-node

Minimal Node providers for `@oaknational/mcp-core`:

- `createNodeClock()`
- `createConsoleLogger(name?: string)`
- `createInMemoryStorage()`

Includes unit tests and a contract consumer test using the core testing helper.

See `docs/architecture/provider-contracts.md` for contract rationale and usage.
