/**
 * `@oaknational/graph-ingest` root barrel.
 *
 * Public ingestion surfaces are introduced by paired product and test cycles.
 * WS2.1 reserves the package entrypoint and sub-path shape only.
 */

export {};
