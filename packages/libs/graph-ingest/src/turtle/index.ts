/**
 * Turtle / SKOS ingestion entrypoint.
 *
 * Reserved as a new sub-path barrel for the WS2.2 cycle (the recommended
 * `src/turtle/` location peer to the six WS2.1 pre-declared barrels). The
 * first implementation + tests land in the substantive WS2.2 commit; this
 * scaffold commit registers the barrel + its dependency-cruiser
 * no-orphans exception + the package.json subpath export so downstream
 * consumers can import `@oaknational/graph-ingest/turtle` once the
 * implementation lands.
 */

export {};
