/**
 * PropertyGraph type definitions sub-path.
 *
 * Reserved in WS3.1; WS3.2 introduces the Node, Edge, and PropertyGraph
 * type definitions alongside the projection function. The types sub-path
 * stays free of executable code so `adjacency` can depend on types
 * without depending on `projection`.
 */

export {};
