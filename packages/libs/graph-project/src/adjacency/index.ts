/**
 * Adjacency sub-path: incoming, outgoing, and neighbour primitives over
 * a projected PropertyGraph.
 *
 * Reserved in WS3.1; WS3.3 owns the first implementation and tests.
 * MUST NOT duplicate `DatasetCore.match()` semantics — adjacency is
 * node→node traversal over the property-graph projection, while
 * quad-pattern matching stays on Dataset (graph-core/dataset).
 */

export {};
