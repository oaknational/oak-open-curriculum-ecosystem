# Oak API key for fetching the live OpenAPI schema during codegen.
# Required for local development. CI uses the committed schema cache.
# Get your key from the Oak API developer portal.
OAK_API_KEY=not_a_real_key
