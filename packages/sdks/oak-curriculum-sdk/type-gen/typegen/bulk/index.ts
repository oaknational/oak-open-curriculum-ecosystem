/**
 * Bulk schema generation module.
 *
 * @remarks
 * Generates Zod schemas for bulk download files at type-gen time.
 * Follows the schema-first principle: types flow from schemas.
 *
 * @module type-gen/typegen/bulk
 */

export { generateBulkSchemas } from './generate-bulk-schemas.js';

