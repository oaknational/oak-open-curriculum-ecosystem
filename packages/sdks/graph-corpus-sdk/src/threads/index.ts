/**
 * `@oaknational/graph-corpus-sdk/threads` — Oak Curriculum Ontology Threads adapter.
 *
 * Surface placeholder for the Threads adapter landing at WS4.2. The
 * adapter consumes the pinned Oak Curriculum Ontology raw import via
 * `@oaknational/graph-ingest` and implements the
 * `GraphView<ThreadNode, ThreadEdgeType>` interface from
 * `@oaknational/graph-core` over the `curric:Thread` enumeration and
 * inverse `curric:includesThread` edge mapping.
 */

export {}; // NOSONAR typescript:S7787 — documented WS4.2 surface placeholder stub
