# @oaknational/design-tokens-core

Pure build-time helpers for Oak design tokens.

This workspace validates the DTCG source structure, enforces the
palette → semantic → component tier model, and emits CSS custom-property
blocks for consumers such as `@oaknational/oak-design-tokens`.
