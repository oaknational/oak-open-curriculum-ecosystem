# HANDOFF — cold-session pickup notes

**Hand-off in progress (July 19, v1.7.0): ALL project files are being handed to the ecosystem repo** ([oaknational/oak-open-curriculum-ecosystem](https://github.com/oaknational/oak-open-curriculum-ecosystem)). Consistency pass done: research doc rev 3, KNOWN-ISSUES #11, SKILL/README/LICENCES current, compiler clean + manifest in sync. Remember the standing gate: Oak marks are NOT open-licensed (LICENCES.md, DECISIONS "Ecosystem convergence") — tracked-package status in the repo needs that licensing call; the hub keeps brand assets untracked. If the hand-off has landed by the time you read this, treat the repo copy as canonical for consumers and this project as the design source.

Read this first (CLAUDE.md's session protocol). CLAUDE.md = permanent principles; DECISIONS.md = durable rationale + lessons; KNOWN-ISSUES.md = gotchas (READ BEFORE DEBUGGING — the @import, capture-quirk, region-collapse and map rules live there); CHANGELOG.md = semver history + public-surface definition. This file = current state + open threads only. Rewrite it at every session end.

## Current state (end of July 19 sessions, v1.7.0)

**The system**: white-label-ready Oak design system. Three strict token tiers (primitives → light-dark() roles + dialect aliases → component tokens shared by class library AND compiled React components). Five theme values (light/dark/system/high-contrast/colour-safe) + independent motion axis (`data-motion`, OS-following default). Region contract for page composition (`.oak-canvas`/`.oak-main` + `data-region` + page-type-scoped maps). Structural brand levers: type slots (`--type-*`), density, flow topology, tone bands (`--band-bg/border/pad/radius`), container measure, polarity (`color-scheme`), atmosphere, form tokens, per-brand icons, §expression hook layer. Multi-medium: print.css in cascade, SVG contract, display scale, DOCX/PPTX/MD/standalone-HTML shipped exports, reveal.js integration, DTCG export, Next.js mapping.

**Brands**: Oak (baseline; mint `--band-bg` hero band is Oak's own dress — do NOT neutralise the baseline again) · EMC² "Educate My Creature Too" (`creature/` — dark-first nocturnal toy world; post-refinement: crisp candy offsets at rest, glow=reward on hover/featured, square bands, butterscotch dec-3 dark, nocturnal `.util`) · PDS (`pds/` — **v7 "the public service": authentic GDS visual grammar**, Public Sans, black masthead + blue bar via §11 expression layer; user-directed reversal of the v6 placement rule — see DECISIONS "Direction outranks the placement rule"). `failing-example.css` = negative control, must keep failing the audit.

**Proof pages**: `Identity White-Labelling.html` (3× specimen side-by-side, subgrid-aligned tops) · `Identity Switchboard.html` (one copy + identity/stage/theme controls) · `Example Front Pages.html` (3 composed pages) · `whitelabel/specimen.html` (the byte-identical full-app page, `?brand=`). **Next.js consumption**: docs/consuming-nextjs.md is the comprehensive guide (1.6.5) — install geometry, theme wiring (beforeInteractive + suppressHydrationWarning + detection matrix), single/multi-tenant identity (multi-tenant is a FIRST-CLASS requirement per user: per-tenant brand.css + server-emitted link, per-tenant audit gating, tenant assets/fonts/caching bullets in §5), §5b component-library guidance (headless yes / pre-styled no — behaviour vs appearance split, focus-ring + state rules), new-component recipe; docs/nextjs-theme-switcher.tsx is the copyable control.

**Compiler**: clean — 70 cards, 590 tokens (scoped tokens now counted with scope), 4 components, 2 templates. ℹ️ notes only: reveal vendor 115KB rides in `_ds_bundle.js` (KNOWN-ISSUES #9), theme-scoped tokens registered with scope. Gotcha fixed 1.6.6: any capitalized-export `.tsx`/`.jsx` anywhere (even docs/) gets compiled into the bundle — doc-only code samples need a non-compiled extension (`.tsx.txt`).

**Audits**: Oak 32/32 · EMC² 32/32 · **PDS v7 34/34 — full audit card re-run July 19 ×4 themes with brand-full.css verified loaded** (the since-v7 caveat is closed). Oak primitives zero-drift vs `reference/oak-components-theme` (shared 44 exact; extensions ours by design).

## July 19 review pass (context flush + cohesion) — DONE

- Fixed: 3 preview cards pointed at renamed `tweaks-panel.jsx` → `.js` (brand-icons, banners, brand-subject-icons — were broken since the rename); `Example Front Pages.html` → stale `Byte-Identical.html` link (renamed page); dtcg `component.json` band.bg stale (`transparent` → `{surface.decorative-1}`) + `band.radius` added; KNOWN-ISSUES #4 rewritten v6→v7 (Public Sans, GDS-hue dark ramp); DECISIONS white-label paragraph un-staled (was v3-era PDS description) + new "Direction outranks the placement rule" section (v7 + EMC² refinement lessons); README file index gained the three root proof pages; SKILL.md setup line now includes print.css + KNOWN-ISSUES pointer + whitelabel line; `oak-icons.css` rewritten (token map + usage; conflicting legacy `.oak-icon`/`.oak-mask` blocks removed) and wired into styles.css + demonstrated in brand-icons card. Full link audit (91 files): clean. CHANGELOG 1.6.2.
- Inventory verdicts: everything earns its place EXCEPT items in "Open questions" below. uploads/ = user's provenance (screenshots are historical evidence of found bugs; PDF/logos/design-note are sources) — never delete. reference/ = provenance, never runtime. `.thumbnail`, `_ds_*`, `_adherence*` = system-owned.

## Resolved by user (July 19): icons wired in (Oak official set via --i-* map in styles.css; counter-brands each get their OWN openly-licensed set — EMC² Material Symbols Rounded (Apache 2.0), PDS **Bootstrap Icons** (MIT, jsDelivr, `bi bi-<name>` class markup) — 1.6.3); reveal.js stays vendored (consuming repo will make it a real dependency — KNOWN-ISSUES #9 documents the bundle weight); uploads/ provenance kept in full. **Icon ROLES (1.6.4)**: shared markup refers to icons by role — `.ic-<role>` + `--ic-*` tokens, Oak defaults in oak-icons.css, brands re-point to their sets' SVG URLs (counter-brand URLs pinned: bootstrap-icons@1.11.3, @material-symbols/svg-600@0.19.0 — if a glyph 404s, check those package paths); specimen exercises search/download/info.

## Open thread (minor, next session)

- Behaviour-library direction fully recorded (1.6.6–1.7.0): **Base UI default** · **React Aria admitted as scoped non-default** (dates/locale/conformance surfaces — owner softened the Adobe exclusion when we went multi-library) · **Ark UI/Zag** for non-React. Three pairing guides in docs/ + §5b chooser table + §7b worked Combobox + wrapped-widget checklist + console-tui-tones + LICENCES.md. Pairing-guide code written from research, not live installs — consumers verify against pinned docs (stated in each guide).
- **Integration with oak-curriculum-hub is the active thread**: `docs/integration-oak-curriculum-hub.md` has the grounded migration plan (hub currently mirrors tokens as raw Tailwind `@theme` literals + own focus ring + blanket motion collapse; sequence: kit+aliases → theme/motion/focus → components → audit-in-CI). Our readiness checklist is ALL GREEN (PDS audit re-run 34/34 ×4; dtcg confirmed current; theme-mapping extended with warning/info + shadow-accent/neutral-brand aliases). Next concrete step is on the consumer side: fresh export → hub migration §1+§3.
- Resolved 1.6.6: brand-icons card now lists all 122 icons; nextjs-theme-switcher no longer leaks into the bundle.

## Deferred by decision (DECISIONS.md has rationale)

- `@layer` cascade refactor (HC/CS guard bug strengthened the case; interacts with editor !important overrides — deliberate future refactor).
- Fixtures-as-parity-contract (worth it if compiled component set grows past 4).
- DTCG naming convergence (`--oak-` vs `--oak-color-`) — discussed, not started.
- PDS "no icons, typographic markers" pole — noted in BRAND.md as a further step if wanted.

## Operational context that lives nowhere else

- GitHub connected: `oaknational/oak-open-curriculum-ecosystem` (pnpm+turbo monorepo; `packages/design/` = design-tokens-core + oak-design-tokens; `demos/oak-curriculum-hub` consumes a Claude Design export of this system). Licensing of Oak marks gates making this a tracked package there (links in DECISIONS "Ecosystem convergence").
- Probe recipes: set `data-theme` explicitly per probe pass and measure against a real fill (canvas bg is transparent; stored user theme leaks into probes — oakTheme.get() to restore). Cross-iframe probing via `eval_js_user_view` + `contentDocument` works same-origin (used for the identity pages' frames).
- gen_pptx recipe for the deck template: selector `deck-stage > [data-deck-active]`, showJs `goTo(N)`, `resetTransformSelector: "deck-stage"`, 1920×1080.
- Peer DS exchange: adopted-from/sent-to lists are in DECISIONS "Peer exchange"; partner has our v2 note (11 items). No open obligations.
- Workspace gotcha: a file can be READABLE via the file API yet 404 at the serve layer (hit July 19: assets/icons/lock.svg + pause.svg — valid content, served 404, broke the icons card). Remedy: read + re-save the file unchanged; the rewrite refreshes the stored object. Diagnose with an Image() probe or resource_error console logs, not readFile.
- Session pauses on quota have happened mid-task several times: ALWAYS leave this file current enough to resume cold.
