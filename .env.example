# Oak Open Curriculum Ecosystem - Environment Variables
#
# CONTRIBUTION LEVELS:
# - Level 1 (Unit tests, type-check): No variables required
# - Level 2 (Local dev servers): OAK_API_KEY required
# - Level 3 (Full E2E testing): All variables below
#
# See docs/operations/environment-variables.md for complete reference

# Oak Curriculum API Configuration
# Request a free key: https://open-api.thenational.academy/docs/about-oaks-api/api-keys
OAK_API_URL=https://open-api.thenational.academy/api/v0
OAK_API_KEY=your_oak_api_key_here

# Clerk Authentication (for MCP streamable HTTP server)
# Get these from: https://dashboard.clerk.com/
CLERK_PUBLISHABLE_KEY=your_clerk_publishable_key_here
CLERK_SECRET_KEY=your_clerk_secret_key_here
CLERK_TEST_EMAIL=your.email+clerk_test@some.domain

# Optional: DNS rebinding protection — auto-includes localhost (+ Vercel URL if deployed)
# ALLOWED_HOSTS=localhost,127.0.0.1,::1
#
# CORS is unconditionally permissive (all origins allowed). Security is
# enforced by OAuth authentication, not by origin restrictions.

# The base url for the `pnpm smoke:remote` tests
SMOKE_REMOTE_BASE_URL=https://example.com/mcp

# CRITICAL: Completely disables auth requirements
# DANGEROUSLY_DISABLE_AUTH=true

# Elasticsearch (required only for search features and some MCP server modes)
# Same credentials used by apps/oak-search-cli
# See: apps/oak-search-cli/docs/ES_SERVERLESS_SETUP.md
ELASTICSEARCH_URL=https://your-elasticsearch-url-here
ELASTICSEARCH_API_KEY=your_elasticsearch_api_key_here

# Optional: Logging
LOG_LEVEL=info
