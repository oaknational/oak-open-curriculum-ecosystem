# No Underscore Prefix

Never prefix unused variables with an underscore. This is a hack and it does not work. Figure out why the variable is unused and fix the root cause. Either use the variable or delete it.

See `.agent/directives/rules.md` §Code Quality for the full policy.
