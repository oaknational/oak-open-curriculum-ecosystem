# Behaviour Components

This directory is for reusable **behaviour-oriented** prompt components.

Use this for shared execution/review behaviours that are not domain-specific
principles and not architecture-team persona guidance.

Guidelines:

- Keep components small and focused.
- Keep components leaf-only (no component-to-component dependencies).
- Prefer adding here only when duplication is concrete and current (DRY/YAGNI).
