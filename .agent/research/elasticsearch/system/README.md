# Elasticsearch System Notes

This folder contains system-specific notes, diagnostics, and planning material. It is not method guidance and may be out of date. Use it only when you need context on the current system or historical decisions.

Current direction: CLI/SDK-only (hosted API routes are retired). See `semantic-search-sdk-and-cli-extraction.md` for the decision record. For method guidance, use `../methods/README.md`. For multi-view architecture and derived-subject design, use `../features/`.
