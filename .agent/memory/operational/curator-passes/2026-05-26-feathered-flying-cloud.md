---
date: 2026-05-26
agent: Feathered Flying Cloud
platform: codex
model: GPT-5
session_id_prefix: 019e65
pass_kind: cross-platform-memory-buffer-sweep
---

# 2026-05-26 — Feathered Flying Cloud

## Surfaces Surveyed

| Surface | Disposition |
|---|---|
| Claude Code per-user memory at `~/.claude/projects/<project>/memory/` | Current Oak project memory is rich; many entries already name graduated repo homes. Remaining import candidates are listed below. |
| Codex per-user memory at `~/.codex/memories/` and Codex history | Current Oak curation lessons are already reflected in repo memory, recent curator passes, and Codex memory summaries. No raw-history import recommended. |
| Cursor project sidecars under `~/.cursor/projects/<project>/` and Cursor plans | Treat as transcript/sidecar evidence, not canonical memory. Sampled oak-preview-1 UAT evidence is already homed in the connecting-oak-resources thread record, repo-continuity, archived napkin, and the snagging plan. |
| Repo `.remember/now.md`, `.remember/recent.md`, `.remember/today-2026-05-26.md`, `.remember/archive.md` | Recent operational summaries are already represented in commits, thread records, and repo-continuity. Identity-candidate bullets are mostly already represented in Practice doctrine; do not bulk-import. |
| Repo homes for candidate phrases | Checked rules, skills, PDRs, frictions register, thread records, repo-continuity, and current memory shards for existing homes before naming carry-forward items. |

## Import Candidates

- `project_comms_cli_grounding_gap` → imported to
  `.agent/plans/agent-tooling/frictions-register.md` F-07 with the sharper
  `comms list --tail N --format summary` and `comms show <event-id>` shape.
- `project_memory_vs_state_split_planned` → imported to ADR-165 as the host
  boundary expectation, and to `open-questions.md` Q-005 for the unresolved
  migration trigger / replacement read-model contract.
- `feedback_new_eslint_rules_start_warn` → imported to
  `docs/governance/development-practice.md` and
  `.agent/rules/no-warning-toleration.md` as a bounded rule-authoring nuance.
- `feedback_opus_team_quota_ceiling` → imported to `start-right-team` as a
  model-seat cost check on top-level team responsibilities.
- `project_vendor_plugin_redundancy_after_canonicalisation` → imported to
  ADR-125 as the post-canonicalisation plugin-retention default.

## Already Homed Or No Import

- `feedback_all_quality_gates_blocking_always` is already stated in
  `docs/foundation/agentic-engineering-system.md` and reinforced by gate rules.
- Periodic team comms checks are already in `start-right-team` at the 120s
  cadence. Cross-session occurrences remain useful evidence, not a new import.
- Cursor oak-preview-1 UAT evidence is already in the connecting-oak-resources
  thread record, repo-continuity, archived napkin, and the relevant snagging
  plan.
- Claude entries marked "Graduated to ..." should remain external audit trail
  unless a current repo search proves their named home is missing.
- Raw Cursor transcripts, Codex prompt history, and `.remember` daily summaries
  should not be bulk-imported. Extract only named, cross-session substance.

## Path Invariant Check

- The candidate report and active napkin addition from this sweep use
  repo-relative paths or templated per-user-memory placeholders only.
- A wider scan surfaced older local-path residue in collaboration state,
  historical comms, and an existing thread record. That is a separate
  portability cleanup surface; do not treat it as imported memory substance.

## Carry-Forward

- Import slice completed 2026-05-26 by Feathered Flying Cloud. Further changes
  should now start from the named durable homes above rather than the original
  platform-memory entries.
- Avoid machine-local path leakage in any future durable write: use templated
  platform-memory paths in prose, never flattened project IDs or absolute
  local paths.
