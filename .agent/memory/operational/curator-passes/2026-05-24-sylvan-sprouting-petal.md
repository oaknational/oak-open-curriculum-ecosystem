---
date: 2026-05-24
agent_name: Sylvan Sprouting Petal
platform: codex
model: GPT-5
session_id_prefix: "019e59"
pass_kind: critical-drain-continuation-ledger
retroactive: false
predecessor_pass: 2026-05-24-shaded-silencing-dusk.md
claim_id: f348f0cd-fdf3-4767-811b-38bc6de0276f
---

# Curator Pass — 2026-05-24 — Sylvan Sprouting Petal

Continuation ledger for the memory-surface critical-drain lane. This file is
metadata only; substance stays in the archive, plan, register, napkin, and
permanent homes it points to.

## Surfaces Surveyed

| Surface | Disposition pointer |
|---|---|
| `.agent/memory/active/napkin.md` | Active-session capture and latest archive-after-processing note |
| `.agent/memory/operational/pending-graduations.md` | Live queue state; four due entries remain owner/PDR-gated |
| `.agent/memory/operational/archive/pending-graduations-archive-2026-05-24.md` | Verbatim archive for processed 2026-05-24 graduated bodies |
| `.agent/plans/agentic-engineering-enhancements/current/memory-surface-critical-drain-2026-05-24.plan.md` | Controlling critical-drain lane |
| `.agent/state/collaboration/active-claims.json` | Live claim survey; no overlap on this slice's files |
| `docs/governance/sonar-disposition-policy.md` | Durable home for Charcoal's owner-authorised Sonar audit-trail capture |
| `.agent/rules/sonarqube-mcp-instructions.md` | Durable home for the Sonar hotspot changelog verification lesson |
| `.agent/memory/operational/tracks/skills-standardisation-followup-timing.md` | Expired tactical card resolved after durable homes were verified |

## Knowledge Routed By This Pass

| Concept | Destination |
|---|---|
| Processed 2026-05-24 graduated pending-graduations bodies | `.agent/memory/operational/archive/pending-graduations-archive-2026-05-24.md` |
| Current Phase 2 continuation status | `.agent/plans/agentic-engineering-enhancements/current/memory-surface-critical-drain-2026-05-24.plan.md` |
| Tooling mistake on `claims list` flag shape | `.agent/memory/active/napkin.md` |
| Owner-authorised Sonar generated API-schema audit-trail boundary | `docs/governance/sonar-disposition-policy.md` Block 2 |
| Compose-vs-emit substrate-pointer evidence | Existing `pending-graduations.md` substrate-pointer v3 route |
| Sonar hotspot rationale verification | `.agent/rules/sonarqube-mcp-instructions.md` § Hotspot review |
| Skills-standardisation timing card substance | Thread record, commands-retirement plan, and pending-graduations follow-up route |
| Identity-seed CLI precondition message | `agent-tools/src/collaboration-state/identity.ts` plus `identity.unit.test.ts` |
| Remaining owner-profile distilled observations | `.agent/directives/user-collaboration.md` § Owner Working Style |
| 2026-05-14 plan-author distilled observations | PDR-018 plus `verify-vendor-call-shapes-at-plan-author-time.md` |
| 2026-05-09 PR closeout distilled observations | `agent-collaboration.md` § PR Closeout Discipline |
| 2026-05-09 multi-reviewer distilled observation | PDR-015 § Audit-shape surface framing |
| Current command-shape mistakes | `.agent/memory/active/napkin.md` Mistakes Made |
| 2026-05-09 self-lint tooling distilled observation | `docs/governance/typescript-gotchas.md` § ESLint Plugin Typing |

## Findings Surfaced

| Finding | Substantive home |
|---|---|
| Remaining due set is owner/PDR-gated, not safely agent-actionable doctrine work | Critical-drain plan Phase 2 continuation |
| `claims list` does not accept `--platform` / `--model` flags | Active napkin tooling note |
| Three distilled entries duplicated verified homes | Distilled graduations log |
| Two additional distilled entries duplicated verified homes | Distilled graduations log |
| Bare timer cleanup note has no exact verified home yet | Active distilled memory |
| Seven additional distilled entries duplicated verified homes | Distilled graduations log |
| Two owner-profile entries have no exact verified home yet | Active distilled memory |
| Bare timer cleanup durable home gap | `guarded-fire-and-forget-cleanup.md` pattern plus `mcp-handler.ts` timer cleanup |
| First timer cleanup patch left a stray dead timeout promise | Active napkin mistake note |
| Active napkin tail carried two unprocessed Charcoal captures | Sonar policy Block 2 plus substrate-pointer v3 pending route |
| Sonar hotspot MCP read model is not the audit-trail carrier | SonarQube MCP rule § Hotspot review |
| Old tactical track card lacked `expires_at` and duplicated promoted state | Thread record plus commands-retirement follow-up surfaces |
| Critical register is currently claimed by Shaded | Read-only triage only; no write into `pending-graduations.md` while claim is fresh |
| Reviewer-pass critical absorption duplicated a durable rule home | `verify-dont-trust.md` plus distilled graduation log |
| Compaction-boundary handoff duplicate needed exact runtime verification clause | `handoff-messages-self-contained.md` plus distilled graduation log |

## Validation

| Check | Result |
|---|---|
| `pnpm exec markdownlint` on touched plan, pass log, and napkin | Passed |
| `git diff --check` | Passed |
| Targeted `rg` verification | Found plan continuation, pass log, archive pointer, and napkin tooling note |
| `pnpm practice:fitness:informational` | Exit 0; `CRITICAL (1 critical, 6 hard, 13 soft)` |
| `pnpm practice:fitness --strict-hard` | Exit 1; `CRITICAL (1 critical, 6 hard, 13 soft)` |
| Distilled verified-home prune validation | Live headings removed; audit-log entries present |
| Second distilled verified-home prune validation | Live duplicate headings removed; audit-log entries present; timer entry retained |
| Third distilled verified-home prune validation | Live duplicate headings removed; audit-log entries present; two owner-profile entries retained |
| Timer cleanup graduation validation | Pattern carries timer-disposal rule; source clears timeout in `finally`; live distilled heading removed; archive ledger present |
| Historical pending-graduations pointer drain | Seven live-body stubs removed after archive bodies and durable homes were verified |
| Historical pointer drain validation | Anchored `rg` found no live-body stubs; broad `rg` found only the intentional ADR-041 graduation-log row |
| 2026-05-24 processed-stub drain | Seven processed stubs removed after archive-body and durable-home verification |
| 2026-05-24 processed-stub drain validation | Anchored `rg` found no remaining live stubs; focused formatting, markdownlint, and whitespace checks passed |
| Legacy implementation-route stub drain | Three processed implementation-route stubs removed after archive-body and durable-home verification |
| Focused formatting / markdown / whitespace | `prettier --check`, focused `markdownlint`, and `git diff --check` passed |
| MCP streamable HTTP package validation | `type-check`, `lint`, and `test` passed; test suite reported 85 files and 722 tests |
| Practice fitness validation | Informational exit 0 and strict-hard exit 1; now `CRITICAL (2 critical, 5 hard, 13 soft)` after evidence-preserving napkin updates |
| Charcoal capture-tail processing validation | Targeted `rg`, focused `prettier --check`, focused `markdownlint`, and `git diff --check` passed |
| Sonar hotspot distilled graduation validation | Targeted `rg`, focused `prettier --check`, focused `markdownlint`, `git diff --check`, informational fitness, and strict-hard fitness ran |
| Track-card resolution validation | Targeted `rg`, focused formatting, focused `markdownlint`, and `git diff --check` passed |
| Critical-register read-only triage | Found three due owner/PDR-gated entries and no obvious remaining `status: graduated` live bodies |
| Reviewer-pass distilled graduation validation | Targeted `rg`, focused formatting, focused `markdownlint`, `git diff --check`, informational fitness, and strict-hard fitness ran |
| Compaction-boundary distilled graduation validation | Targeted `rg`, focused formatting, focused `markdownlint`, `git diff --check`, informational fitness, and strict-hard fitness ran |
| PDR-072 downstream Core cascade | `practice.md` and `practice-lineage.md` now carry the autonomic-learning / two-output-surface principle named by PDR-072 |
| PDR-072 downstream Core cascade validation | Focused `prettier --check`, focused `markdownlint`, `git diff --check`, informational fitness, and strict-hard fitness ran |
| PDR-072 critical-register residual closure | Removed the live residual after verifying PDR-072, `practice.md`, `practice-lineage.md`, and archive evidence |
| PDR-072 residual closure validation | Focused `rg`, focused formatting, focused `markdownlint`, `git diff --check`, informational fitness, and strict-hard fitness ran |
| PDR-073 critical-register residual closure | Landed recursion-as-method in the Core trinity, declined duplicate directive surface, and removed the live residual |
| PDR-073 residual closure validation | Focused `rg`, focused formatting, focused `markdownlint`, `git diff --check`, informational fitness, and strict-hard fitness ran |
| Practice-lineage hard-pressure response | Refined lineage-level wording after the Core cascade so full doctrine stays in PDRs/`practice.md` and `practice-lineage.md` returns to soft |
| Repo-continuity live-state correction | Updated local HEAD/origin, active-lane summary, and current fitness pointer after team reactivation |
| Post-critical continuity correction | Re-grounded after shard split; focused checks passed; strict-hard exits 1 with `HARD (1 hard, 19 soft)` and no critical surfaces |
| Legacy active-shard Sonar entry | Verified the Sonar hotspot audit-trail entry already lives in the SonarQube MCP rule; archived the original body and removed it from the live shard |
| Soft-only fitness state | Current strict-hard fitness passes with `SOFT (21 soft)` after the owner-profile distilled graduation moved durable owner-working-style substance into `user-collaboration.md` |
| Legacy active-shard deferred-at-write-time entry | Verified exact pattern home; archived the original body and removed it from the live shard |
| Legacy active-shard resolved-stub cleanup | Removed five already-processed stubs after re-verifying archive bodies and durable homes |
| Resolved-stub cleanup validation | Targeted `rg`, focused formatting, focused `markdownlint`, `git diff --check`, and strict-hard fitness passed |
| Team-autonomy active-shard resolved-stub cleanup | Removed two already-processed commit-queue stubs after re-verifying the Sylvan archive and commit-queue cure plan homes |
| Team-autonomy cleanup validation | Targeted `rg`, focused formatting, focused `markdownlint`, `git diff --check`, and strict-hard fitness passed |
| Identity-seed CLI refinement | Generic missing-seed error replaced with explicit env-var list and platform-specific hint; live pending body archived and removed |
| Identity-seed validation | Focused Vitest, Prettier, ESLint, markdownlint, and whitespace checks passed |
| Owner-profile distilled graduation | Final two 2026-05-22 owner-profile observations moved to `user-collaboration.md`; active distilled duplicates removed |
| Plan-author distilled graduation | `PDR-018` and `verify-vendor-call-shapes-at-plan-author-time.md` carry the exact 2026-05-14 plan-author discipline entries; active distilled duplicates removed |
| Plan-author graduation validation | Targeted `rg`, focused formatting, focused `markdownlint`, `git diff --check`, and strict-hard fitness passed |
| PR closeout distilled graduation | `agent-collaboration.md` and the collaboration-channel routing card carry the exact 2026-05-09 PR closeout discipline entries; active distilled duplicates removed |
| PR closeout graduation validation | Targeted `rg`, focused formatting, focused `markdownlint`, `git diff --check`, and strict-hard fitness passed |
| Multi-reviewer distilled graduation | PDR-015 carries the exact 2026-05-09 audit-shape-surface entry; active distilled duplicate removed |
| Multi-reviewer graduation validation | Targeted `rg`, focused formatting, focused `markdownlint`, `git diff --check`, and strict-hard fitness passed |
| Tooling mistake capture | Active napkin now records the `comms inbox --seen-file`, rejected `comms inbox --thread`, `claims open --area-kind`, and shell-pipe evidence-readability mistakes from this continuation |
| Self-lint tooling distilled graduation | `typescript-gotchas.md` carries the self-lint and ESLint helper type-boundary guidance; active distilled duplicate removed |
| Self-lint tooling graduation validation | Targeted `rg`, focused formatting, focused `markdownlint`, `git diff --check`, and strict-hard fitness passed with `SOFT (21 soft)` |
| Session handoff | Claim `fba31dad-0fbf-465e-b91b-ab706789b444` closed; handoff claim `809d9cff-8146-4fcb-8113-ec1722008d75` opened and closed; thread and repo continuity updated with the no-hard/no-critical fitness state and next safe curator step; closeout broadcast `c155d0c5` emitted |
| Identity-helper ownership follow-up | Mistbound identified the identity-helper files as source-class residue after `cd4efc15`; claim `5e66df2a-120c-4438-b577-25269f22512f` owns the revalidation and marshal-ready handoff |
| Identity-helper ownership validation | Focused Vitest, ESLint, agent-tools type-check, Prettier, markdownlint, `git diff --check`, and strict-hard fitness all passed; marshal-ready event `821b7af1` sent to Mistbound |
| Lockfile-in-flux grounding amendment | Legacy shard entry graduated to `start-right.md` § Live state; dirty unclaimed slices must be checked against repo-continuity, thread record, and active plans before orphan classification |

Fitness disposition: current strict-hard validation is `SOFT (21 soft)`.
There are no hard or critical files. The active napkin remains healthy at
58 lines / 2,955 chars after fresh capture and mistake notes.
`user-collaboration.md` is newly soft by four lines after receiving the final
two owner-profile observations; that is a durable-home graduation result, not
trim pressure. The plan-author distilled graduation moved `distilled.md` to
297 lines / 15,808 chars, the PR closeout graduation moved it to 275 lines /
14,627 chars, the multi-reviewer graduation moved it to 266 lines /
14,183 chars, and the self-lint tooling graduation plus live CLI-shape repair
moved it to 254 lines / 13,735 chars. `typescript-gotchas.md` is soft at
97 lines / 3,299 chars because it accepted the durable-home tooling note;
this is knowledge placement, not trim pressure. Earlier
distilled pruning moved it to 647 lines /
33,671 chars, still hard, and the second verified-home slice moved it to 629
lines / 32,752 chars. The third verified-home slice moved it to
558 lines / 29,203 chars. The timer cleanup graduation removed one more live distilled
duplicate after strengthening its durable home; `distilled.md` still remains
hard and still requires the planned full per-entry home-gap walk. The
historical pending-pointer drain moved `pending-graduations.md` to 4,725 lines /
337,418 chars; it remains the single critical surface because the remaining
mass is larger than a narrow stub drain can responsibly solve. The 2026-05-24
processed-stub drain moved `pending-graduations.md` to 4,672 lines /
334,677 chars; it remained the single critical surface. The legacy
implementation-route drain moved `pending-graduations.md` to 4,663 lines /
333,758 chars / 86 critical prose-width lines. The active napkin is now
critical at 462 lines because curation evidence was preserved; route that to a
substance-led napkin processing/rotation pass rather than trimming useful
observations. Charcoal capture-tail processing moved active napkin to 472 lines
after routing the unprocessed tail to Sonar policy and the existing
substrate-pointer pending route; the added lines are processing evidence, not
budget filler. The Sonar hotspot distilled graduation moved the hotspot
audit-trail lesson into the SonarQube MCP rule and moved `distilled.md` to 512
content lines / 26,850 chars. `distilled.md` remains hard by line count, while
`pending-graduations.md` remains the single critical surface at 4,515 content
lines / 320,641 chars / 75 critical prose-width lines. Strict-hard therefore
still exits 1.

The track-card resolution removed one obsolete operational card after verifying
that its useful content was already promoted: reviewer-dispatch details and
timing evidence live in the agentic-engineering thread record; the
commands-retirement plan carries the executable retirement work; and the
commands-retirement follow-up route remains in `pending-graduations.md`.
Read-only triage of `pending-graduations.md` then showed the active critical
file was under a fresh Shaded claim. The live index named three due entries
(`director-pure-direction-only`, `owner-action-is-not-a-cure`, and
`marshal-as-cycle-discipline`), each owner/PDR-gated; no obvious remaining
`status: graduated` live bodies were found.
The reviewer-pass critical-absorption distilled entry then graduated after
`verify-dont-trust.md` was amended to carry the exact instruction that reviewer
output is evidence to test, not a substrate pointer to trust. Current fitness
still reports `pending-graduations.md` as the single critical file. The
distilled slice moved `distilled.md` to 504 lines / 26,483 chars, still hard by
line count.
The compaction-boundary handoff entry then graduated after
`handoff-messages-self-contained.md` gained the runtime-monitor / cron /
watcher verification clause for future-self handoffs. The slice moved
`distilled.md` to 490 lines / 25,684 chars, soft rather than hard.
The PDR-072 downstream Core cascade then landed in `practice.md` and
`practice-lineage.md`. Focused formatting, markdown, and whitespace validation
passed. Fitness remains `CRITICAL (1 critical, 5 hard, 14 soft)`:
`pending-graduations.md` is still the only critical surface at 4,021 lines /
287,706 chars / 64 critical prose-width lines, while `practice-lineage.md` is
now hard at 834 lines / 48,673 chars because the Core-trinity cascade preserved
the PDR-072 substance.
After Shaded's claim cleared, the live PDR-072 residual was removed from
`pending-graduations.md`; the original source and processing trail remain in
the 2026-05-24 archive. Focused validation passed. Fitness remains
`CRITICAL (1 critical, 5 hard, 14 soft)`; `pending-graduations.md` improved to
4,005 lines / 286,628 chars / 64 critical prose-width lines.
The PDR-073 residual then closed the same way: `practice.md` carries
recursion-as-method in the self-teaching property, `practice-lineage.md`
carries the learned principle, and the optional directive surface is declined
as duplicative because the Core trinity plus `metacognition.md` already provide
the recognition path. Focused validation passed. Fitness remains
`CRITICAL (1 critical, 5 hard, 14 soft)`: `pending-graduations.md` is still
the only critical surface at 4,178 lines / 278,725 chars / 36 critical
prose-width lines, while `practice-lineage.md` remains hard at 840 lines /
49,115 chars after preserving the PDR-072 and PDR-073 Core-trinity substance.
After Shaded re-claimed the critical register for width repair, this pass stayed
off `pending-graduations.md` and responded to the next available pressure:
`practice-lineage.md` was refined to lineage-weight wording for concept exchange,
learning-before-fitness, autonomic learning, and recursion-as-method. Full
doctrine remains in PDR-072, PDR-073, and `practice.md`. Focused validation
passed, and fitness moved to `CRITICAL (1 critical, 4 hard, 15 soft)`;
`practice-lineage.md` is now soft at 829 lines / 48,449 chars.
Repo-continuity then received a narrow live-state correction: local HEAD is
`79c148e4`, origin remained at `795d2e9d` at the 13:27Z check, the session is
no longer a solo curator lane, and the current fitness pointer now matches the
critical-drain plan.
After compaction, a fresh fitness run showed the critical register pressure had
cleared completely: `HARD (1 hard, 19 soft)`. `pending-graduations.md` is now
soft at 690 lines / 36,308 chars / max prose line 260; the remaining hard
surface is `testing-strategy.md`. This pass corrected the continuity surfaces
so the live routing no longer points future agents at a nonexistent critical
drain, while preserving the active shard pointers as live queue material rather
than archives.
Shaded then claimed the sole remaining hard file (`testing-strategy.md`), so
this pass stayed off that lane and processed one verified-home legacy shard
entry instead. The Sonar MCP `show_security_hotspot.comments` audit-trail gap
now has an exact durable home in `.agent/rules/sonarqube-mcp-instructions.md`
§ "Hotspot review"; the original body is preserved in the 2026-05-24 archive
with a disposition note and removed from the active shard.
The next informational and strict-hard fitness runs reported `SOFT (21 soft)`:
all hard and critical pressure is currently cleared. Remaining work is still
real curation, but it is soft-routing work rather than a hard/critical-file
emergency.
This pass then processed a second legacy active-shard body whose exact pattern
home already exists:
`.agent/memory/active/patterns/deferred-at-write-time-is-unmade-load-bearing-decision.md`.
The pattern carries the diagnostic, worked instances, cure, and PDR-026
cross-reference, so the original pending body is preserved in the 2026-05-24
archive with disposition and removed from the active shard.
After Shaded claimed the freshly rotated active napkin and distilled surfaces,
this pass stayed off those files and cleaned up five legacy active-shard stubs
that were already processed. Archive bodies and durable homes were re-verified
for the sibling-repos doc, peer-commit absorption pattern, frictions-register
F-18, plan reachability surfaces, and coordination-watcher canonicalisation
plan before the stubs were removed from the active shard.
Focused validation passed: targeted `rg` found the five headings absent from
the active shard and present in the 2026-05-24 archive, formatting and markdown
checks passed, `git diff --check` passed, and strict-hard fitness remains
`SOFT (20 soft)`.
This pass then removed two already-processed team-autonomy active-shard stubs.
The original bodies remain in the Sylvan-specific 2026-05-24 archive, and the
commit-queue multi-writer cure plan carries line-scoped staging as Tranche A
and native intent-scoped message storage as Tranche C. The rest of the
team-autonomy shard remains live because its entries are still trigger-gated,
owner/PDR-gated, or missing exact durable homes.
Focused validation passed: targeted `rg` found the two headings absent from the
active shard and present in the Sylvan archive, formatting and markdown checks
passed, `git diff --check` passed, and strict-hard fitness remains
`SOFT (20 soft)`.
The identity-seed precondition entry then graduated through its named code
home: `deriveCollaborationIdentity` now reports the four accepted seed
variables and a platform-specific primary seed hint, with the behaviour covered
by `agent-tools/tests/collaboration-state/identity.unit.test.ts`. The original
pending body is preserved in the 2026-05-24 archive and removed from the active
legacy shard.
The remaining two owner-profile distilled observations then graduated to
`user-collaboration.md` § Owner Working Style: destructive incidents call for
structural cures, and consolidation surfaces are observability infrastructure.
The active distilled duplicates were removed after the directive carried the
substance and the graduations log recorded the route.
Strict-hard fitness remains green at `SOFT (21 soft)`; the new soft item is the
directive's owner-working-style home absorbing durable substance.

## Carry-Forward

- Owner ratification or PDR review is still required before mutating the
  remaining principle-class due entries.
- Active napkin now needs a later substance-led consolidation/rotation check
  after the preserved tooling notes and archive-continuation entries are
  processed.
- Phase 2 remains partial while the three live due entries have no owner-approved
  permanent doctrine route.
- Phase 3 remains partial until `distilled.md` receives a full per-entry
  graduation/home-gap walk.
- This pass verified and pruned three duplicate distilled entries. Their homes
  are PDR-081, the curator-pass skill,
  `knowledge-preservation-over-fitness-warnings.md`, and
  `verify-dont-trust.md`.
- This pass later verified and pruned two more duplicate distilled entries:
  cadence-aware substrate absorption and HTTP/MCP transport test
  classification.
- The timer-race cleanup entry graduated after the durable pattern and proven
  source path were corrected to carry the exact timer-disposal rule.
- Seven already-graduated historical pending-register stubs were removed after
  archive-body and durable-home verification. The ledger rows stayed because
  they are historical context, not live queue bodies.
- Seven 2026-05-24 processed stubs were removed after verifying archive bodies
  and durable homes: comms-watch, sidebar protocol, verify-dont-trust, cron
  owner-input precedence, `/remember`, plan taxonomy, and Supertest
  classification.
- Three legacy implementation-route stubs were removed after verifying archive
  bodies and current homes: CLI first-touch friction, pre-commit staged-content
  gating, and advisory-protocol enforcement through the commit-queue guard.
- This pass later verified and pruned seven more duplicate distilled entries:
  six owner-working-style observations and the metacognition structural-cure
  entry.
- Five legacy active-shard processed stubs were removed after re-verifying
  archive bodies and durable homes: cross-repo sibling list,
  peer-commit absorption, gatekeeper stale-sweep race, plan leaf-to-root
  reachability, and canonical watcher definitions.
- Two team-autonomy active-shard processed stubs were removed after verifying
  the Sylvan archive and commit-queue cure plan homes: line-scoped staging and
  native intent-scoped commit-message storage.
- The identity-seed precondition CLI refinement landed in code and tests before
  its pending-graduations body was archived and removed from the active shard.
- The 2026-05-22 owner-profile distilled set is now fully graduated to
  `user-collaboration.md`; no owner-profile entries remain live in
  `distilled.md`.
- This pass later moved the Sonar hotspot rationale-verification lesson into
  the SonarQube MCP rule before pruning the duplicate live distilled entry.
- The destructive-incident and consolidation-surface owner-profile entries stay
  live until exact durable homes exist.
- Current validation still reports `pending-graduations.md` as the single
  critical surface. This pass has now made a narrow claim-bounded Phase 2
  continuation on that register without mutating principle-class homes.
- The stale skills-standardisation timing track card is resolved; it should not
  be recreated unless a fresh single-writer tactical cycle actually starts.
- The next direct critical-register edit should wait for Shaded's claim to
  clear or be explicitly routed as a non-overlapping owner-approved slice.
- The reviewer-pass critical-absorption duplicate has been removed from live
  `distilled.md`; the pending second-instance pattern candidate remains in the
  critical register for Shaded/next-claim handling.
- The compaction-boundary duplicate has been removed from live `distilled.md`;
  future-self handoff verification now lives in the self-contained handoff rule.
- Current validation reports `pending-graduations.md` as the only critical
  surface at 4,223 lines / 302,239 chars / 71 critical prose-width lines.
- The PDR-072 downstream Core cascade has landed in `practice.md` and
  `practice-lineage.md`; after Shaded's critical-register claim cleared, the
  remaining PDR-072 residual was removed from the live queue.
- The PDR-073 downstream Core cascade has landed in `practice.md` and
  `practice-lineage.md`; the optional directive was declined as duplicate, and
  the residual was removed from the live queue.
- Latest validation still reports `pending-graduations.md` as the only critical
  surface. Shaded's width-repair pass appears to have moved prose width below
  the critical threshold; current fitness reports 4,114 lines / 261,232 chars /
  28 hard prose-width lines.
- `practice-lineage.md` has been brought back from hard to soft while preserving
  the PDR-072/PDR-073 substance through lineage-weight wording.
- `repo-continuity.md` has a fresher live-state header, but remains hard and
  still needs a later structural index/archive pass if owner priority returns
  to that surface.
- The critical-register split created
  `.agent/memory/operational/pending-graduations/2026-05-23-team-session-autonomy.md`
  as an active queue shard, not an archive. It preserves the full 2026-05-23
  first-out closeout, commit-queue ceremony, PDR-074 autonomy, and PDR-078 /
  ADR-186 heartbeat bodies while reducing the main register's always-open
  weight.
- Focused formatting, markdownlint, and diff whitespace checks passed. Fitness
  remains `CRITICAL (1 critical, 4 hard, 15 soft)`: `pending-graduations.md`
  is critical by line count at 3,561 lines, hard by characters at 214,645, and
  green on prose width.
- A second active-shard split moved the legacy `## Entries` body to
  `.agent/memory/operational/pending-graduations/2026-05-06-to-2026-05-21-legacy-backlog.md`.
  This is still live unresolved queue substance, not an archive. The main
  register now holds schema, index, shard pointers, current due bodies, and
  recent routing surfaces.
- Focused checks passed again after the legacy split. Fitness now reports
  `HARD (4 hard, 16 soft)`: `pending-graduations.md` is hard only by line
  count at 764 lines / 40,064 chars / max prose line 260. Critical pressure is
  cleared.
- A final pointer compaction removed duplicated shard-body prose from the main
  register while leaving the shard index intact. Fitness now reports
  `HARD (3 hard, 17 soft)`: `pending-graduations.md` is soft at 741 lines /
  38,880 chars / max prose line 260.
- Final closure still requires Phase 4 validation of the broader touched set
  and explicit disposition for the remaining hard surfaces.
- A further legacy shard item has been processed: insight-report Item 12
  "Cross-repo sibling list". Verification found no sibling map in
  `practice-lineage.md` and a current host-doc home in
  `docs/engineering/sibling-repos.md`, so the active shard now keeps a pointer
  and the original body plus disposition live in the 2026-05-24 archive.
- The peer-commit absorption third-direction pending body has been drained after
  verifying its exact durable home in
  `peer-commit-absorption-third-direction.md`. The active shard keeps a pointer;
  the original body plus disposition live in the 2026-05-24 archive; the
  enforcement tail remains live in the adjacent R4-new / commit-queue UX entries.
- The gatekeeper green-light stale-sweep body has been drained after verifying
  its durable operational home in the agent-tooling frictions register, F-18. The
  active shard keeps a pointer; the original body plus disposition live in the
  2026-05-24 archive; the unresolved cure remains open in the frictions register.
- The plan-portfolio leaf-to-root reachability body has been drained after
  verifying the invariant in `.agent/plans/README.md` and the unresolved
  validator/ADR route in the decision-incomplete reachability remediation plan.
  The active shard keeps a pointer; the original body plus disposition live in
  the 2026-05-24 archive.
- The canonical-tool-definitions/code-adjacent body has been drained after
  verifying the coordination-watcher canonicalisation future plan as the live
  work surface. The active shard keeps a pointer; the original body plus
  disposition live in the 2026-05-24 archive; the plan-execution trigger remains
  live in that future plan.
- Shaded re-claimed the legacy backlog shard and shared 2026-05-24 archive, so
  this pass moved to the non-overlapping 2026-05-23 autonomy shard. The two
  commit-queue ceremony entries now point at the commit-queue multi-writer cure
  plan; original bodies plus dispositions live in a Sylvan-specific 2026-05-24
  archive file.
- After a fresh strict-hard run reported `SOFT (22 soft)`, this pass corrected
  the near-limit `repo-continuity.md` live-state wording that still described
  `pending-graduations.md` as critical. No content was removed; this was a
  routing-truth fix after critical pressure had already cleared.
