---
fitness_line_target: 260
fitness_line_limit: 340
fitness_char_limit: 24000
fitness_line_length: 100
merge_class: curation-ledger
---

# 2026-06-03 Opalescent Illuminating Prism Curation Ledger

Mode: `dedicated-knowledge-curation`.

Bridge: fitness output is routing evidence. The value is conserving insight.
Completion requires item-level disposition, not a smaller-looking file.

## Before State

- Source: `.agent/memory/active/napkin.md`.
- Fitness before: `CRITICAL` on napkin (`428` content lines, `27477`
  characters) and `HARD` on `repo-continuity.md`.
- Active claim: `b3f6c607-3683-4479-9a2c-d813888b8cf8`.
- Important correction: this pass initially made an invalid archive move before
  this ledger existed. The move was reversed immediately; this ledger is the
  corrected process-before-archive proof.

## Critical Post-Mortem

1. Earlier zones fired, but the session kept appending high-value live learning
   across multiple active lanes instead of rotating immediately. That was
   correct for preservation, but it made rotation due.
2. The limit is still appropriate for `napkin.md`: the file is a capture buffer,
   not a long-term home. The pressure means the current rotation contains enough
   mature lessons to route upward.
3. The critical state is partly a missing-graduation symptom. Several entries
   were already durable elsewhere; the fresh gaps are mostly workflow
   enforcement and specialist-skill lessons captured below.

## Source Item Dispositions

### 1. School-search briefs citation audit

- **Disposition**: `graduated` / `owner-gated`.
- **Durable homes**: the live `school-data-search` thread record holds the next
  session instructions; `repo-continuity.md` points to it; the clean source
  artefacts live in reference-local as evidence for the next report.
- **Graduated route**: PDF-only citation recovery was added to
  `.agent/skills/chatgpt-report-normalisation/SKILL-CANONICAL.md` and
  `.agent/memory/active/patterns/chatgpt-report-normalisation.md`; the
  pending-graduations item is now marked graduated.
- **Duplicate evidence**: the owner-supplied 21-link Brief 1 reference
  supplement is live in the current napkin source and in the clean reference
  artefact; no extra permanent repo doc is needed before the planned report.

### 2. Antigravity Practice integration closeout

- **Disposition**: `duplicate`.
- **Durable homes**: Antigravity identity and matrix substance already landed in
  `agent-tools` docs, ADR-125, executive matrix/inventory surfaces, and the
  `agentic-engineering-enhancements` thread record.
- **Retained learning**: the ownership-freeze lesson remains represented by the
  existing mixed-tree commit-scope entries in the thread record and napkin
  archive. No new rule is promoted in this pass.

### 3. D3 review-then-ratify settlement

- **Disposition**: mixed `duplicate`, `graduated`, and `owner-gated`.
- **Durable homes**: D3 contract settlement lives in the EEF plan, EEF thread
  record, upstream realignment plan, and existing rules
  `verify-data-supports-shape-before-building.md`,
  `invoke-mcp-expert.md`, and `verify-dont-trust.md`.
- **New route**: the live-claim requirement for commit-queue write commands
  combines with the Lofty closeout-tooling entry below and is added to
  `pending-graduations.md`.
- **Duplicate evidence**: reviewer-brief scope protection is already in
  `distilled.md` and `pending-graduations.md`.

### 4. Antigravity audit vs skills taxonomy separation

- **Disposition**: `duplicate`.
- **Durable homes**: the vocabulary split lives in
  `.agent/memory/executive/agent-capability-vocabulary.md`, the
  skills-classification future plan, and the agentic-engineering thread record.
- **No new route**: platform integration versus taxonomy separation is stable
  but already homed.

### 5. Final closeout observations

- **Disposition**: `graduated`.
- **Durable homes**: the capture-to-enforce cycle evidence is represented by
  the graduated D3 rules and pending-graduations entries.
- **New route**: between-turn surface drift is refined into `distilled.md` as a
  compact behaviour rule for live parallel curation.

### 6. Closeout commit tooling

- **Disposition**: `owner-gated`.
- **New route**: `pending-graduations.md` gets a tool-improvement candidate for
  commit-queue identity/help consistency.
- **Why not implement now**: this curation pass is already repairing knowledge
  flow and hard/critical fitness; changing agent-tools CLI behaviour would be a
  separate TDD implementation lane.

### 7. D3 adversarial self-audit

- **Disposition**: `duplicate`.
- **Durable homes**: design-rationale cite-or-strike and MCP target pinning are
  already graduated to the two rules named above.
- **No new route**: the entry is preserved as source evidence in the napkin
  archive.

### 8. EEF D3 execution lessons

- **Disposition**: mixed `duplicate` and `owner-gated`.
- **Durable homes**: stale-file-list, source-side enumeration, and false-claim
  span checking are already represented in EEF plan corrections, existing
  `verify-dont-trust.md` doctrine, and the contamination-scan pattern.
- **New route**: the canonical-script recurrence remains a behaviour memory in
  the archive; it does not yet merit a new rule in this pass.

### 9. 2026-06-02 Shaded napkin rotation record

- **Disposition**: `duplicate`.
- **Durable homes**: the Shaded ledger and archived napkin are already linked
  from the section itself; this section is historical proof, not a live item.
- **No new route**: preserve in the archive only.

### 10. Graph-estate consolidation execution

- **Disposition**: mixed `duplicate` and `owner-gated`.
- **Durable homes**: archive/de-link doctrine is already in ADR-117 and the
  plans README; relative-link integrity already has a future validator plan;
  source-side completeness is already in `verify-dont-trust.md`.
- **New route**: commit-queue CLI drift is covered by the same tool-improvement
  candidate as item 6.

### 11. Graph-estate final-capture additions

- **Disposition**: mixed `duplicate` and `owner-gated`.
- **Durable homes**: decision-completeness grounding and link-integrity
  substance are already in the relevant plans, thread records, and
  pending-graduations entries.
- **New route**: opener self-instructed staleness refines the existing
  "opening statements teach by their form" owner-gated item in
  `pending-graduations.md`.

### 12. JC4 plan authoring

- **Disposition**: mixed `duplicate`, `graduated`, and `owner-gated`.
- **Durable homes**: claim-scoped verification is already in
  `verify-dont-trust.md`; known-answer probe calibration remains source
  evidence; opener staleness is routed as above.
- **New route**: zsh false-green verifier extraction is added to
  `distilled.md` as an assertion-time verifier lesson.

### 13. This pass's archive-before-ledger mistake and fitness-language slip

- **Disposition**: `graduated`.
- **Durable homes**: `consolidate-docs`, `consolidate-until-done`, and
  `session-handoff` now carry a visible pre-archive ledger gate: before any
  archive/rename/park/supersede/replace move, the agent must name or create the
  ledger that records the source item set and dispositions.
- **Evidence**: user correction in this session after the attempted
  `git mv`; move was reversed before continuing.
- **Additional correction**: the owner also corrected my phrase "make fitness
  pass" as the exact wrong framing. Disposition: `graduated` into this ledger,
  the active napkin correction, and the curation skill language that fitness
  output is only routing evidence and rest-state health evidence.

## Exit Condition for Archive Move

This ledger is the required process-before-archive proof. Once the routed
updates named above have landed in `distilled.md`, `pending-graduations.md`,
the ChatGPT report-normalisation skill, and the ChatGPT report-normalisation
pattern, the source napkin may be moved to
`.agent/memory/active/archive/napkin-2026-06-03-opalescent-curation.md` and a
fresh live `napkin.md` may be created.

## Additional Buffer Inventory

- `.remember/now.md`: 3 entries. The two school-search normalisation entries
  are represented in `threads/school-data-search.next-session.md`, the active
  napkin source archive, and
  `.agent/experience/2026-06-03-hushed-numbered-chips.md`; the PDF-only
  protocol they exposed is graduated to the ChatGPT report-normalisation skill
  and pattern. The API/schema clarification entry names its local brief and is
  carried by the same school-data-search thread handoff. Disposition:
  `duplicate` / `graduated`.
- `.remember/today-2026-06-03.md`: recent EEF and upstream-realignment entries
  are operationally represented by the `eef` thread, repo-continuity current
  state, and the upstream realignment plan. The "napkin pending" note is
  superseded by this ledger-backed rotation. Disposition: `duplicate`.
- `.remember/recent.md`: summarised older graph/EEF and identity-candidate
  material already lives in committed plans, rules, PDR/ADR-linked doctrine, or
  owner-gated pending-graduations entries. Disposition: `duplicate` /
  `owner-gated`.

## After State

- Active napkin: reopened as a small live capture buffer; processed source
  preserved at
  `.agent/memory/active/archive/napkin-2026-06-03-opalescent-curation.md`.
- Pending-graduations inventory: no `status: pending` or `carried-forward`
  matches found in the active learning buffers checked; remaining live register
  items are owner-gated, graduated, duplicate, or stale-withdrawn.
- Rest-state health check: `pnpm practice:fitness:strict-hard` reports
  `SOFT` with 0 hard and 0 critical files after the ledger-backed rotation and
  repo-continuity compaction.
