---
fitness_line_target: 220
fitness_line_limit: 300
fitness_char_limit: 18000
fitness_line_length: 100
split_strategy: "Process each candidate into an existing rule, PDR, ADR, pattern, or explicit duplicate disposition before removing this shard"
merge_class: append-only-narrative
fitness_content_role: drainable-buffer
---

# Mistbound Inherited-Frame And Hook-Signal Candidates

Source window: `.agent/memory/active/napkin.md` entry
`2026-05-25 - Mistbound (e77243) post-compaction execution`.

Processing status: live shard. This file preserves the full source
substance while removing hard pressure from the active napkin. Process
entry by entry; do not archive or delete this shard until every item
has a durable home, an existing-home duplicate disposition, or a
named blocker.

## Current Disposition

- **Inherited option-set / LTAE frame test**: likely an existing-rule
  worked instance of
  [`re-apply-first-question-at-elaboration-boundaries`][rule-elab],
  not a new standalone rule. Next consolidation should decide whether
  the existing rule needs a short example or whether the plan amendment
  already provides sufficient evidence.
- **Hook signal as conceptual diagnostic**: likely an existing-rule
  worked instance spanning hook-policy discipline, innate immunity, and
  `never-ignore-signals`. Next consolidation should check whether the
  existing rule corpus already covers this enough, then record duplicate
  or extension explicitly.
- **Inherited self-trust / fractal stopping criterion**: still a
  candidate pattern. Needs comparison against current metacognition,
  owner-action, and signal-response surfaces before promotion.

## Source Entry

### What Worked

- **Owner-directed LTAE pass on an inherited option-set surfaced
  the failure mode the existing rule already names.** Surfaced
  (a)/(b)/(c)/hybrid for WS0 SKILL decomposition as if all three
  were peer choices. Owner asked: "run the user decision through
  long-term architectural excellence only." LTAE applied to the
  *frame* (not the choices within the frame) revealed: (b) is an
  anti-shape that violates the WS0 acceptance criterion at line
  127; (a) standalone is under-engineered; (c)+partial(a) is the
  only architecturally-defensible direction. The "menu for owner
  verdict" framing was itself the failure mode. The verdict is
  directed-not-menu.
- **Verify-before-applying paid off twice in the same session at
  two different surfaces**. WS1 substrate verification revealed
  --body-file is already implemented (cure: pivot to WS4). The
  WS0 LTAE pass is the same shape applied to a different surface
  (option-set framing rather than implementation state). Both
  are halves of the same deeper rule:
  [`re-apply-first-question-at-elaboration-boundaries`][rule-elab]
  already covers both — option-set surfacing IS one of the
  elaboration boundaries the rule names; my session articulated
  this less precisely than the existing rule already does.

### Mistakes Made (retrospective metacognition)

- **Almost authored duplicate doctrine via inherited-frame
  credulity at the meta level**. After the LTAE pass landed
  cleanly I claimed the pattern was "graduation-ready substance
  for a new rule" — but reading the existing rule
  [`re-apply-first-question-at-elaboration-boundaries`][rule-elab]
  revealed the cure was already there. The WS0 case is a
  worked-instance of an existing rule applied at the option-set
  surface, not a new rule. I had inherited my own preceding
  analysis as authoritative without LTAE-testing it against the
  rule corpus — the same failure mode applied recursively. Cure:
  before claiming graduation-readiness, grep the rule corpus for
  existing coverage of the pattern's mechanism.
- **The original WS0 option-set surfacing skipped the LTAE pass
  on the frame**. The frame was inherited from the prior session's
  plan body, which named "extract front-matter / sibling SKILLs /
  section-trimming via PDR-pointer" as the three options. My
  Plan-agent dispatch in this session designed the
  option-table around that inherited frame without questioning
  whether (b) belonged in the option set at all. The prior
  metacognition's cure ("dispatch reviewers at inventory step")
  was applied — reviewer was dispatched at the planning step —
  but the brief was "design the execution sequence", not
  "LTAE-test the inherited option set." Partial cure.

### Metacognition self-test (revised — Layer 4 was NOT diminishing returns)

- Owner-directed a deeper layer after I claimed Layer 3 was the
  trim. The deeper layer: when the `policy.json` hook blocked my
  use of the phrase "cheap cure", I treated the block as a
  mechanical obstacle and rewrote the prose to comply. The owner
  named this directly: *"reflect on the semantic and conceptual
  cause of the blocked phrase, not just work around it."*
- The semantic cause: the very act of an agent writing the
  rejected-category name (even to reject it) places that
  category inside the agent's decision-space. The principle
  doesn't say "weigh excellence vs the rejected category and
  pick excellence" — it says the rejected category is
  *categorically excluded from consideration*. An agent who
  names the rejected category in their own voice is operating
  in a frame where that category is a category-to-be-considered.
  Citing it doctrinally correct does not undo the framing.
- The conceptual cause: my framing was still operating on a
  vestigial expedient-vs-excellent decision axis. There is no
  axis. Excellence is the only option in the agent's
  decision-space; the rejected category is not a thing-in-the-
  world for the agent to weigh-and-reject. The hook enforces
  this structurally at the linguistic layer — it is PDR-044's
  innate-immunity layer applied to the LINGUISTIC instantiation
  of the principle, not just the semantic.
- The pattern is fractal — every "this is mechanical" judgment
  is potentially a "this is conceptual" layer I missed. My
  Layer-3 trim was itself the fourth recurrence: I stopped based
  on inherited self-trust about when to stop, rather than
  verifying the next layer was actually empty. The owner-
  intervention is evidence of a missing autonomy primitive (per
  the memory rule on owner-action). The primitive I lack: a
  reliable signal that distinguishes *no-deeper-layer* from
  *I-haven't-looked-deep-enough*.
- Hook signals carry information; treating them as obstacles
  rather than diagnostics is the same failure mode the
  `never-ignore-signals` rule already names — applied at the
  hook-policy layer rather than at the build-warning or
  type-error layer. The rule corpus covers the cure; the
  worked-instance is what I lacked. This is now captured.

### Pattern Crystallising

- **Inherited artefacts default to audit-shape authority unless
  LTAE-tested at moment of use.** When a prior session's
  artefact (option set, frame, candidate enumeration) is
  inherited, the temptation is to work *within* its boundaries
  rather than questioning the boundaries themselves. The audit-
  shape inherits authority it doesn't deserve; the describe-
  shape earns authority through verification at moment-of-use.
- **The pattern is fractal**. Every layer where I think the
  next layer is empty (mechanical, diminishing returns,
  stopping criterion met) may itself be operating on inherited
  self-trust. The structural cures (hooks, fitness gates,
  policy blocks) work because they are external to the agent's
  self-trust — they fire even when the agent doesn't notice the
  layer exists.
- The existing rule
  [`re-apply-first-question-at-elaboration-boundaries`][rule-elab]
  covers this at the semantic layer. The `policy.json` hook
  covers it at the linguistic layer. The
  [`never-ignore-signals`](../../../rules/never-ignore-signals.md)
  rule covers it at the response-to-friction layer. Three
  surfaces, one underlying principle (the doctrine works because
  external structure compensates for inherited self-trust).

[rule-elab]:
  ../../../rules/re-apply-first-question-at-elaboration-boundaries.md
