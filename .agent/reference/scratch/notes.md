# Scratch notes

- bring in the linting rules that make the directory size limit helpful
- bring in the directory size limit rule
- consolidate all search functionality in the search SDK
- take the UI plan, create the new group of workspaces, divide the plan into executable phases implement the bare minimum UI to support a user-facing web search demo
- production Clerk
- pull in all useful structure and code from the ui poc app
- make sure that Sentry is set up properly for the http mcp server
