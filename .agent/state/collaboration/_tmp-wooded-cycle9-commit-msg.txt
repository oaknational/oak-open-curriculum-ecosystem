feat(agent-tools): comms-watch auto-seed + --seed-from-now/--no-auto-seed flags (tidy cycle 9)

PDR-063 mid-cycle resume from Eclipsed Watching Secret (00d760). Tidy plan
Cycle 9 (WS1 comms-watch CLI auto-seed) — intentional behaviour change per
R1 finding #15: auto-seed-on-empty default; `--seed-from-now` forces seed
regardless of seen-file state; `--no-auto-seed` preserves the legacy
replay-on-empty contract for explicit opt-out callers.

Eclipsed authored the extracted module (`comms-watch-auto-seed.ts`,
discriminated-union result, narrow `Pick<>` IO surface) plus the unit test
file (6 system-state tests) and retired for compaction. Wooded picked up
via PDR-063 handoff record at
`.agent/state/collaboration/handoffs/2026-05-25-eclipsed-cycle9-compaction-handoff.md`,
ratified the frozen decisions in §3, authored the 4 integration edits
(cli-options.ts BOOLEAN_OPTION_KEYS routed via parseValueOption,
cli-spec-options.ts commsWatchOptions extension, cli-specs.ts help-text
update, cli-comms-watch.ts seedSeenStateIfNeeded call after
ensureDirectory), added 4 parser tests in a new
cli-options-parser.integration.test.ts file, dispatched 3 Sonnet
reviewers (test-expert, code-expert, type-expert), absorbed the
substantive findings:

- code-expert P2-1 (zero-events idempotency): `applySeed` now guards
  against `eventIds.length === 0` to prevent the seen-file growing by a
  trailing newline on every empty-comms-dir restart. New regression test
  added.
- type-expert P2 Finding 1 (codebase-convention divergence): renamed the
  `AutoSeedDecision` discriminant from `seeded: boolean` to
  `outcome: 'seeded' | 'skipped'` to match the established
  `WatcherStalenessResult` / `DrainResult` `kind`-string pattern.
- test-expert P2-1 (fake state-machine): removed the unused
  `state.seenIds.add` mutation loop from the test fake `appendSeenMessageIds`.
- test-expert P2-2 (test-name claim): renamed the "missing seen-file"
  test to drop the embedded production-IO rationale.
- test-expert P2-3 (parser test classification): renamed the parser test
  file `.unit.test.ts` → `.integration.test.ts` per the test-classification
  doctrine — `parseOptions` is a multi-unit composite with module-level
  state, not a pure unit.
- test-expert P3-4 (terminal-argv coverage): added a parser test
  asserting `--seed-from-now` at end-of-argv does not consume a
  non-existent next token.
- test-expert P3-5 (implementation-detail audit): dropped the
  `readSeenIdsCalls === 0` assertion in the `--seed-from-now` test; the
  `appendCalls` assertion already proves seeding occurred. Equivalent
  assertion retained in the `--no-auto-seed` test where zero-IO IS the
  contract.

Code-expert P3 nits (parallel-registries / boolean-presence idiom /
asymmetry / help-text default) declined with rationale: each is consistent
with the established codebase pattern; no action required.

Workspace gates green: 546/546 tests pass (67/67 files), workspace
type-check + lint clean. Marshal pnpm-check protocol pending Hushed.

Plan §Cycle 9 todo status flipped to `completed`.

Co-Authored-By: Claude Opus 4.7 (Wooded Flowering Leaf f03dbd) <noreply@anthropic.com>
