fix(practice-substrate): use String.raw for 2 regex patterns (PR114 Sonar S7780)

Cures PR #114 SonarCloud quality-gate failure: two `typescript:S7780` issues
in `agent-tools/src/practice-substrate/open-questions-evaluator.ts` at
lines 152 and 159 where escaped backslashes inside template literals can be
expressed more legibly via `String.raw`.

Mechanical no-behaviour-change refactor at two sites:

- L152 (open-questions field regex): switch the `new RegExp(\`...\\\\s*...\`)`
  template literal to `new RegExp(String.raw\`...\\s*...\`)` so the `\s`
  reads as literal `\s` in source.
- L159 (escapeRegExp $& replacement): switch the second argument of
  `value.replace(...)` from the string-literal `'\\\\$&'` to the
  `String.raw\`\\$&\`` template.

Both edits produce identical runtime strings; the difference is source
legibility (no double-escape). The function tests cover the runtime
contract and pass unchanged.

Verification at author seat (Eclipsed):

- `pnpm --filter @oaknational/agent-tools exec vitest run tests/practice-substrate/`:
  PASS (41/41).
- `pnpm --filter @oaknational/agent-tools type-check`: PASS.
- `pnpm --filter @oaknational/agent-tools lint`: PASS.
- `pnpm exec prettier --check` on the edited file: PASS.

Surfaced by Celestial Glimmering Galaxy (codex GPT-5, 019e5d) via SonarQube
MCP at directed event 07:26:45Z; picked up by Eclipsed at 07:31Z under owner
prompt; enqueued at 07:32:53Z (intent 187506c8); PDR-063-handed to Wooded
at 07:37Z (Eclipsed compaction-prep); marshalled by Hushed (post-resume).

Refs: PR #114, SonarCloud rule typescript:S7780, intent
187506c8-d07d-4527-99f3-285bc5f93d28, claim
fd8d39fb-7aae-47bb-af79-2cd437055fe3.

Co-Authored-By: Claude Opus 4.7 (Eclipsed Watching Secret 00d760) <noreply@anthropic.com>
