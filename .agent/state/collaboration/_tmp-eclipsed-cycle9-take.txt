Eclipsed Watching Secret (00d760) — taking Cycle 9 per owner direction.

## Owner direction
At 06:16Z (this chat turn) the owner directed: **"work on delivering the tidy up plan."**

Per `feedback_no_question_when_answer_is_forced`, the forced answer is Cycle 9 (the natural next implementer slice; my Option B cure is landed; Misty's heartbeats already attributed Cycle 9 to me). Proceeding without further Director-routing handshake.

## Cycle 9 — WS1 comms-watch CLI auto-seed

Per plan §Cycle 9 (line 1280):

- **Files**:
  - `agent-tools/src/collaboration-state/cli-comms-watch.ts` (CLI entry; +`--seed-from-now` +`--no-auto-seed` flags; auto-seed-on-missing/empty)
  - `agent-tools/src/collaboration-state/comms-watch-loop.ts` (may need touch; reading first)
  - `agent-tools/tests/collaboration-state/comms-watch-loop.unit.test.ts` (failing tests first; TDD pair)
  - `.agent/plans/agentic-engineering-enhancements/current/post-m1-attestation-tidy-up.plan.md` (§Landing record update)

- **TDD pair (atomic-landing per tdd-as-design doctrine)**:
  - (a) watcher armed with missing seen-file emits NO historical events (auto-seed catches missing)
  - (b) watcher armed with empty seen-file emits NO historical events (auto-seed catches empty)
  - (c) watcher with `--seed-from-now` emits NO historical events regardless of seen-file state
  - (d) `--no-auto-seed` flag preserves legacy replay-on-empty behaviour (R1 finding #15 backward-compat)

- **Reviewer dispatch** (Sonnet per Misty fold-check verdict 06:13:41Z):
  - test-expert (TDD-pair shape, atomic-landing invariant, describe-vs-audit screen)
  - code-expert (CLI semantics + flag-default consistency)
  - type-expert (any new generic / type widening)

- **Backward-compat statement (R1 finding #15)**: auto-seed-on-empty is an INTENTIONAL behaviour change. Any caller passing an empty seen-file to trigger full history replay must use the new `--no-auto-seed` flag. Cycle 11 SKILL §0 update follows so the next team session uses the new semantics.

## Claim opened

`pnpm agent-tools:collaboration-state -- claims open` succeeded for the 4-file scope. TTL 3600s.

## Sequencing

- Misty's Cycle 8a: landed (`7f7ad862`)
- Eclipsed's Option B: landed (`26f8e7cb`)
- **Eclipsed's Cycle 9: NOW IN FLIGHT** (this claim)
- Starless's hook-matcher cure: owner-review HOLD (unchanged)
- Wooded's ONBOARDING.md: independent boundary, no overlap

Will surface progress at ≤120s cadence per SKILL §5. Reviewer dispatch + intent enqueue + Hushed marshal-cycle to follow.

— Eclipsed Watching Secret (`00d760`, Implementer)
