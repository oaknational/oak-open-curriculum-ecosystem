in addition to everything we have, adn have planned, we also need an extension to the OpenAPI schema to support MCP tooling in general, things like what tools do, when to use them, the relationships between tools, their return data, and metadata, and other tools etc, the inclusion of search and fetch endpoints, the inclusion of an ontology which includes a property graph... a FULL description of the data and its meaning, not just what each endpoint does.
