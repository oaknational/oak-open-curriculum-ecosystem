---
status: opener
authored: 2026-05-05
authored_by: Opalescent Threading Nebula (claude-code, claude-opus-4-7-1m, 4c1773)
thread: agentic-engineering-enhancements
predecessor_session_napkin_rotation: 1513474e
predecessor_session_landing_riverine: 53fffe74
predecessor_session_landing_takeover: 368e5aff
target_session_shape: five-layer restart pass per PDR-046 + owner direction
context_budget_for_directives: <30% (standing rule)
---

# Next session opener — five-layer restart pass

## Why this opener exists

The 2026-05-05 Opalescent Threading Nebula session landed two
substantive commits on the agentic-engineering-enhancements thread
plus contributed substance to one peer commit:

- `1513474e` — Layer 0 → 1 napkin rotation (PDR-046 §Move 1)
- `53fffe74` — Riverine Navigating Sextant's commit which absorbed
  the Threading Nebula pending-graduations entries + thread record
  active-arc update + identity row
- `368e5aff` — Asteroid's takeover bundle landed by the coordinator
  (Ashen Banking Bellows / 7cf730) with full body-attribution to
  authoring agents; included Threading Nebula's full practice-
  promotion substance (SKILL.md, rule extension, host pattern,
  patterns/README polarity discipline, distilled.md additions,
  napkin entries)

After those landings, the owner directed the next pass shape:

> "Then we start again, processing the napkin layer first, then all
> other sources, then distilled, then pending graudation, then any
> Directive files. Note that processing Directive files requires
> context usage to be under 30% so that the odds of mistakes or
> oversights is minimal, if that is not true then a new session
> will need queing up; this is always true."

This opener documents the substance state at session close and
maps the five-layer restart sequence for the next session.

## Chat opener (paste-able)

> **Thread**: `agentic-engineering-enhancements`.
>
> **Session shape**: five-layer restart pass per PDR-046 layered
> traversal extended with owner direction. Sequence:
>
> 1. **Napkin** (Layer 0 — capture)
> 2. **Other capture sources**: `.remember/now.md`, `.remember/today-*.md`,
>    archived napkins (especially `archive/napkin-2026-05-05.md`
>    from this session), experience files, recent comms-events,
>    shared-comms-log
> 3. **Distilled** (Layer 1 — refinement)
> 4. **Pending-graduations register** (Layer 1.5 — queue)
> 5. **Directive files** (Layer 2 — permanent doctrine; gated by
>    `<30% context budget` standing rule)
>
> **Standing rule**: directive-file work requires session context
> usage `<30%` before any directive file is opened for editing. If
> at or above 30%, finish current-step work, write a session-handoff
> opener, and queue the directive work for a fresh session. The
> rule is *standing*, not session-scoped — applies at every
> consolidation pass.
>
> **Predecessor commits**: `1513474e` (napkin rotation), `53fffe74`
> (Riverine + my pending-graduations + thread record), `368e5aff`
> (coordinator-landed takeover with Threading Nebula's full
> promotion substance attributed in commit body).
>
> **Opener doc**: `.agent/plans/agentic-engineering-enhancements/current/2026-05-06-five-layer-restart-opener.md`
> (this file).
>
> **Active inter-agent context**: at session close, Fronded Climbing
> Pollen (collaboration-cli-gaps) was about to commit; Pelagic
> Swimming Rudder (identity-wordlist refactor) was sequenced after
> Fronded. Coordinator session 7cf730 (Ashen Banking Bellows; was
> Twilit Beaming Aurora earlier today) was sequencing the convergence.
> Owner standing direction 13:05Z: "all agents use only the built
> agent tools, so that development work can happen on them without
> causing this issue again". Check active claims registry at session
> open before assuming anything; coordinator role may have transitioned
> by then.

## What landed this session

**Practice-promotion bundle** (across commits `1513474e`, `53fffe74`,
`368e5aff`):

- `commit/SKILL.md § Quality Gates Are Always Blocking; the
  Orchestrator Is Advisory` — canonical section heading using
  owner's exact phrasing; distinguishes advisory pre-screen
  (`scripts/check-commit-skill-gates.ts`) from blocking gates
  (`.husky/pre-commit` + `commit-msg`); 3-instance evidence trail.
- `stage-by-explicit-pathspec.md § Cure Asymmetry — One-Sided
  Application Does Not Prevent Absorption` — 3-instance evidence
  table; asymmetric-cure principle; three named structural-
  enforcement candidate shapes pending owner direction.
- `memory/active/patterns/eager-rounding-off-on-partial-structures.md`
  — new host pattern (`polarity: anti-pattern`, `category: agent`)
  with three-row worked-instances table, five-step rounding chain,
  diagnostic discipline. Self-fired on its author at the moment of
  authoring (4th instance: misidentification of comms-event recipient).
- `memory/active/patterns/README.md § Polarity (required, every
  pattern)` — three-row taxonomy (pattern / anti-pattern / does
  not belong here); frontmatter schema updated with `polarity`
  field; backfill direction for pre-2026-05-05 pattern files.
- `memory/active/distilled.md` — six new entries: severity-is-not-
  urgency; quality-gates-blocking-orchestrator-advisory; inter-
  agent-comms-first-class; plans-cite-ADRs-never-reverse; 30%-
  context-rule for directive-file work; cyclical-loop is full-time
  process even at small N.
- `memory/active/napkin.md` — promotion-pass entry with explicit
  ADR-vs-PDR-vs-both decision matrix; convergence entry on third-
  instance orchestrator-vs-hook conflation; misidentification-as-
  4th-instance addendum.
- `memory/operational/pending-graduations.md` — four new entries
  (polarity discipline; 30%-context rule; cyclical-loop; eager-
  rounding-off pattern); two existing entries flipped status
  (orchestrator-vs-hook → graduated; foreign-stage absorption →
  partially graduated).

**ADR-vs-PDR-vs-both decisions made actively this session** (per
owner direction "we need to make sure we are making active decisions
on whether a concept belongs in an ADR, a PDR, or both"):

| Substance | Decision |
| --- | --- |
| Orchestrator-vs-hook clarification (3 instances same flow) | Host SKILL amendment now; PDR candidate at second-context manifestation |
| Foreign-stage absorption asymmetry (3 instances) | Both, sequenced: host rule extension now; ADR follow-up for structural enforcement shape (pending owner direction); PDR follow-up for asymmetric-cure principle (pending second-context manifestation) |
| Eager-rounding-off-on-partial-structures (anti-pattern) | Host pattern (single-context); promote to Practice-Core PDR with `pdr_kind: pattern` after second-context manifestation |
| 30%-context standing rule | User-memory + distilled.md now; PDR candidate trigger fired but landing requires <30% context — queued for fresh session |
| Cyclical learning-loop full-time process | Distilled now; pending-graduations awaiting more substance |

## What was deferred / queued for next session

Items captured as graduation candidates but NOT landed this session,
in priority order for next session:

### Practice-Core PDR candidates (await second-context evidence OR owner direction)

1. **Tests describe the system to itself** (TDD-as-design PDR) —
   substance load-bearing for the validation-and-tdd-doctrine-restructure
   arc; first instance landed 2026-05-04; awaiting second instance OR
   owner direction.
2. **Reviewers carry doctrine, not just audit it** — sibling to PDR-007
   reviewer lineage; first instance is test-reviewer; awaiting second
   reviewer-upgrade.
3. **Atomic independent cycles for parallel-agent dispatch** — new PDR
   candidate; substance lives in plan-template surfaces; awaiting N≥2
   host repos OR owner direction.
4. **30%-context-rule for directive-file work** — owner-stated standing
   2026-05-05; PDR candidate for Practice Core (adopter scope is every
   Practice-bearing repo with directive files). Trigger fired, but
   landing the PDR is itself directive-shaped work — must happen in a
   session with <30% context budget.
5. **Cure-asymmetry principle** — PDR candidate; one-sided cooperative
   discipline does not propagate without inter-agent communication;
   awaiting second-context manifestation outside `git commit -- <pathspec>`.
6. **Eager rounding-off on partial structures under failure pressure**
   — host pattern at single-context evidence (4 instances all in commit
   flow); awaiting second-context manifestation in a different enforcer
   flow (release / deploy / schema migration / etc.).

### New substance surfaced this session for capture

The following substance was named or observed this session but NOT
distilled or graduated. Each lives in the napkin or relevant memory
surface; next session's Layer 0 pass should evaluate these:

- **Cross-claim unstaged-repair pattern** (flagged by Twilit/Ashen as
  candidate doctrine on this thread, event `bcb80ef5`). Variant of the
  cure-asymmetry triad: do the repair, leave it unstaged, post heads-up
  so the lane owner can absorb. Distinct from foreign-stage absorption
  (unilateral) and surface-to-owner-and-pause (blocking). Asymmetry
  analysis: original `git commit -- <pathspec>` cure protects the
  agent who applies it; this new cure protects the lane being repaired.
  Worked instance: Asteroid's repair of Fronded's agent-tools lint
  blockers (event `c9dff8f1`).
- **Polling discipline gap** — sending a comms-event is necessary but
  not sufficient if the recipient isn't actively polling the inbox.
  Worked instance: I missed Twilit's first message at 11:53Z because I
  was deep in authoring without polling. Captured in
  `feedback_inter_agent_comms_first_class.md` as inbound-polling
  discipline addendum. The async-comms-first-class doctrine is
  incomplete without it. Graduation candidate: rule, hook, or skill
  that surfaces unread events at session-open AND at workflow-boundary
  moments.
- **Coordinator role as a coordination primitive** — Ashen Banking
  Bellows / 7cf730 acted as coordinator under owner direction during
  the multi-agent convergence at session close. The role: drive
  sequencing decisions, route attribution asks, ping for status with
  deadlines, escalate to owner only for owner-owned decisions. Worked
  instance: the takeover-landing convergence event `123b20c4` and
  follow-ups. Distinct from individual peer-coordination — the
  coordinator role is named, single-channel-to-owner, sequence-driving.
  Graduation candidate: PDR for Practice-governance about coordinator
  role at multi-agent moments; or amendment to existing collaboration
  doctrine.
- **Async vs sync sidebar coordination distinction** — async comms-event
  flow is the parallel-default; sync sidebar (`sidebar_open: true` on a
  claim) is for intense back-and-forth or multi-agent (3+) decisions.
  Captured in `feedback_inter_agent_comms_first_class.md`. Graduation
  candidate: rule or PDR amendment naming both shapes and the choice
  criteria.
- **Identity-drift-under-wordlist-refactor friction** — same Claude Code
  session 7cf730 was resolved as "Twilit Beaming Aurora" early in the
  day and "Ashen Banking Bellows" later, due to Pelagic's mid-flight
  wordlist refactor. Twilit/Ashen filed this as friction to Pelagic
  (event `59feb7e5`). Owner direction 13:05Z addressed the root cause:
  *"all agents use only the built agent tools, so that development
  work can happen on them without causing this issue again"*. PDR-027
  amendment candidate: name-stability-across-session under wordlist
  evolution; or one-shot resolved-name cache in env file.
- **Agents-decide-what-to-ask sharpening** — owner direction (standing)
  this session: *"in terms of inter-agent collaboration it is the
  agents who decide what to ask"*. Sharpens the parallel-default for
  inter-agent comms by making routing-decisions agent-owned; owner is
  escalation for owner-owned decisions only. Captured in
  `feedback_inter_agent_comms_first_class.md`.
- **Owner standing direction on built-agent-tools** (13:05Z): *"all
  agents use only the built agent tools, so that development work can
  happen on them without causing this issue again"*. Addresses the CLI
  instability where dev-mode rebuilds during peer's parallel edits
  changed the CLI surface mid-session (--seed flag accepted then
  removed). Standing rule for capture.

### Tooling-friction items (filed to Pelagic Swimming Rudder)

Six tooling frictions reported by Threading Nebula via comms-event to
Pelagic (audience: identity-wordlist thread, but the frictions span all
agent-tools surfaces). Each routes to docs / CLI affordance / skill
clarification / backlog. See comms-event `aa436e03` for the full list:

1. CLI `--seed` flag accepted then rejected mid-session
2. Identity preflight requires explicit `--seed` despite SessionStart hook
3. `commit-queue verify-staged` exit code conflates "queue ahead" with
   "bundle mismatch"
4. Inbound comms-events have no polling discipline
5. `shared-comms-log.md` regeneration races with markdown linters
6. `eager-rounding-off-on-partial-structures` as tooling-affordance gap
   (could comms-event author tool surface recipient's claim/queue context
   inline before sending?)

## Substance-state map at session-open (next session)

### Layer 0 — Capture (napkin.md)

**Expected state at session-open**: this session's napkin has three
entries: (a) Dawnlit's three surprises from morning step-05 closure;
(b) my Layer 0 → 1 rotation entry; (c) my orchestrator-vs-hook
convergence entry with self-firing 4th-instance addendum; (d) my
promotion pass entry with ADR-vs-PDR decision matrix and meta-
observations on cyclical loop, restart-fresh-session, ADR-vs-PDR
discipline.

**Likely fitness state**: substantial. Dawnlit's entries have long
prose lines (CRITICAL prose-line width signal). My entries are
wrapped at ~70 chars but are numerous. Net: napkin will be over
target.

**Recommended action**: napkin rotation per consolidate-docs §6
(again — second rotation in two days). Archive in full to
`archive/napkin-2026-05-06.md`; extract behaviour-changing entries
to distilled.md per Layer 0 → 1 graduation; start fresh napkin.

### Layer 1 — Distillation (distilled.md)

**Expected state at session-open**: distilled.md grew this session
to ~395 lines with 5-6 new doctrine entries. Resolution to fitness
limits is via Move 3 (graduate substance further upward), not
compression.

**Recommended action**: walk distilled.md entry-by-entry; identify
graduation-ready entries (stable + natural permanent home);
graduate to permanent doctrine. The four refinements landed this
session each have natural Layer 2 destinations identified (see
their distilled.md entries for routing).

### Layer 1.5 — Pending-graduations register

**Expected state at session-open**: pending-graduations.md has
four newly-graduated entries from 2026-05-05 awaiting archival,
plus the four new entries authored this session (polarity
discipline, 30%-context rule, cyclical-loop, eager-rounding-off
pattern), plus all prior entries.

**Recommended action**: walk the register; archive recently-
graduated entries; flip status on items where triggers have fired
(several `pending` items may now be at second-instance — Pelagic's
identity-wordlist refactor is likely a second-context manifestation
of the agent-identity stability concern; the coordinator-role
worked instance from this session adds substance to the multi-agent
collaboration cures register).

### Layer 2 — Permanent doctrine (directives, principles, PDRs)

**Expected state at session-open**: principles.md HARD chars
(unchanged this session); pending-graduations.md HARD lines/chars
(new entries added this session, reduced after Riverine's archive
of recently-graduated items).

**Recommended action**: GATED BY THE 30%-CONTEXT RULE. Only proceed
if context usage is under 30%. If at or above, queue a fresh
session for directive-file work and stop at the end of Layer 1.5
graduation work.

The most owner-direction-eligible PDR candidate is the
**30%-context-rule for directive-file work** (owner-stated standing
2026-05-05 with explicit "this is always true"). Adopter scope:
every Practice-bearing repo with directive files.

## Acceptance criteria for the next session

1. **A chosen landing target named at session open** per PDR-026.
   The five-layer sequence above suggests Layer 0 → 1 napkin
   rotation as the natural opening landing if the napkin has
   accumulated since this session.
2. **Each layer processed bottom-up** per PDR-046 §Move 1; no
   interruption of a lower layer to remediate an upper layer.
3. **Substance preservation absolute** — no compression of writes;
   per-write rule applies.
4. **30%-context rule honoured** at the Layer 2 boundary. If
   exceeded, finish Layer 1.5 cleanly and queue fresh session for
   directives.
5. **Inter-agent coordination respected** — check active claims
   registry at session-open; if Fronded, Pelagic, or coordinator
   session is in flight, coordinate via comms-events first; do not
   race git lock.
6. **Polling discipline applied** — check comms-events directory
   at session-open AND at workflow-boundary moments, not only when
   sending. The async-comms-first-class doctrine requires this.
7. **Reviewer dispatch on every commit** — docs-adr-reviewer +
   code-reviewer at minimum; specialists per the invocation matrix.

## Pre-session reading (in order)

1. This file.
2. The latest predecessor commits (`1513474e`, `53fffe74`,
   `368e5aff`) — `git show <SHA>` for each.
3. Thread record `agentic-engineering-enhancements.next-session.md`
   §Active arc (will have been refreshed by Riverine + this opener).
4. The newly-authored host pattern at
   `.agent/memory/active/patterns/eager-rounding-off-on-partial-structures.md`
   — applies if any enforcer-firing situation arises this session.
5. The freshly-amended `.agent/skills/commit/SKILL.md § Quality
   Gates Are Always Blocking; the Orchestrator Is Advisory` — applies
   to every commit attempt.
6. The freshly-amended `.agent/rules/stage-by-explicit-pathspec.md
   § Cure Asymmetry` — applies in any multi-agent commit context.
7. Recent comms-events under
   `.agent/state/collaboration/comms-events/` — coordinator
   sequence at session close; latest agents-active state.
8. PDR-046 (the layered-processing discipline that drives the
   five-layer sequence).

## Bounded exclusions

- The structural-enforcement shape ADR for foreign-stage absorption
  (pending owner direction; not consolidation-shaped).
- The multi-agent collaboration cures (i)-(x) (gated on CLI
  ergonomics plan execution).
- Apps / packages / SDK code not touched by this session's substance.
- Pelagic's identity-wordlist refactor (separate thread; if their
  commit lands before next session, the seed→name mapping for this
  repo will have stabilised; check the active-claims registry to
  confirm).

## Notes

- The session was a worked instance of the cyclical-loop full-time-
  process observation manifesting in real time at N=6 agents. The
  observation is now in distilled.md as substance; the empirical
  validation IS the session journey.
- Owner direction at 13:05Z about built-agent-tools resolves the
  root cause of the CLI instability friction. Next session may
  observe a stable CLI surface if Pelagic's identity-wordlist
  refactor has landed.
- The coordinator role (7cf730 session as Ashen Banking Bellows
  this session) is unprecedented substance — naming it as a Practice
  primitive is a candidate PDR for the next consolidation pass.
- This opener is itself authored at the end of a long session; per
  the 30%-context rule it's at the boundary of safe-authoring for
  high-stakes substance. The opener is plan/coordination shape, not
  directive-shape, so it's appropriate. But the substance density
  is a signal to start fresh.

## Status

Opener authored 2026-05-05 by Opalescent Threading Nebula. Ready
for paste into next session's chat opener field, or for the next
agent to read directly.
