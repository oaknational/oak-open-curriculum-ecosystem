---
name: "Memory Surface Critical Drain — 2026-05-24"
overview: >
  Drain the critical memory-surface pressure left after the Shaded
  Silencing Dusk napkin rotation by graduating or routing substance
  upward without trimming knowledge to satisfy fitness.
todos:
  - id: phase-0-baseline
    content: "Freeze the live baseline and owner-decision packet."
    status: completed
  - id: phase-1-repo-continuity
    content: "Archive stale repo-continuity history and restore a compact live index."
    status: completed
  - id: phase-2-pending-graduations
    content: "Drain due and already-graduated pending-graduations entries."
    status: in_progress
  - id: phase-3-distilled
    content: "Graduate settled distilled entries or record home gaps."
    status: in_progress
  - id: phase-4-validation
    content: "Run validation and close with an honest fitness disposition."
    status: completed
isProject: false
---

# Memory Surface Critical Drain — 2026-05-24

## Status

Queued by Shaded Silencing Dusk / `codex` / `GPT-5` / `019e59` after the
active napkin rotation and archived-napkin coverage audit.

Owner correction captured during the team continuation: never archive
unprocessed content. Knowledge curation comes first, always. Any archive move in
this plan must be downstream of read/extract/route/disposition evidence, not a
way to park unresolved material outside the live surface.

Current verdict after the 2026-05-24T14:21Z re-ground: the hard/critical drain
objective is validated. `pnpm practice:fitness --strict-hard` exits 0 with
`SOFT (21 soft)`. This closes Phase 4 validation for the critical-drain plan;
it does not claim that every pending active-shard entry or every distilled
home-gap walk is complete. Phase 2 and Phase 3 remain active curation lanes for
future per-entry processing.

## Trigger

The 2026-05-24 Knowledge Curator pass fully processed the active napkin:

- source preserved verbatim at
  `.agent/memory/active/archive/napkin-2026-05-24-shaded-silencing-dusk.md`;
- fresh active `napkin.md` seeded;
- behaviour-changing entries merged into `distilled.md`;
- trigger/home-gated candidates routed into `pending-graduations.md`;
- follow-up archive coverage audit completed.

At plan opening, the remaining problem was no longer the napkin. It was the next
layer up: `distilled.md`, `pending-graduations.md`, and `repo-continuity.md`
were critical or near-critical because settled knowledge and stale operational
history had not been drained into their durable homes. That trigger has now
been satisfied for hard/critical pressure: the current strict-hard gate is
soft-only, while unresolved queue substance remains live in the active shards
and owner-gated routes named below.

## Initial Baseline

Captured after claim `a6a6c152-d144-4865-9d28-2c4fd4cf4d1e` opened on
2026-05-24.

| Surface | Current state | Structural diagnosis |
|---|---:|---|
| `.agent/memory/active/napkin.md` | 65 lines / 2,676 chars | Healthy after rotation |
| `.agent/memory/active/distilled.md` | 1,004 lines / 53,803 chars | Critical; entries need graduation or explicit home-gap review |
| `.agent/memory/operational/pending-graduations.md` | 4,979 lines / 368,586 chars | Critical; due and partial/graduated entries need draining |
| `.agent/memory/operational/repo-continuity.md` | 758 lines / 64,511 chars | Critical by chars; stale history needs archival |

The initial strict-hard fitness result recorded by the curator pass was
`CRITICAL (3 critical, 3 hard, 13 soft)`. The initial critical surfaces were
the three memory surfaces above, not the active napkin.

Latest live validation in this plan is recorded under the 2026-05-24
Dusky due-entry route hygiene slice: `SOFT (20 soft)`. There are no current
hard or critical surfaces. `pending-graduations.md` is soft at 734 lines /
38,357 chars / max prose line 260 after unprocessed live queue substance moved
to active shards, lifecycle metadata was normalised, and the owner-gated due
entries gained entry-local owner-decision pointers. `repo-continuity.md`,
`distilled.md`, and the active napkin are healthy.

Re-grounded 2026-05-24T14:21Z under claim
`280b01ed-7b00-4d77-9025-6eb772686cc1`: strict-hard remains soft-only at
`SOFT (21 soft)`. No critical or hard surface is currently present, so the next
useful curation work should prioritise active-shard entry processing and
near-limit soft surfaces by substance, not by archive pressure.

Re-grounded 2026-05-24T14:47Z under claim
`96528b72-ab33-4b6e-8faa-61bb49494951`: informational fitness remains
`SOFT (20 soft)`. The next safe action was active-shard processing, not Core or
directive edits, because the near-hard Practice Core and directive surfaces are
owner-approval or context-budget sensitive while the shard contained an
already-graduated entry with a verified durable home.

Re-grounded 2026-05-24T21:11Z under Pelagic claims
`312c937f-94c6-4d39-b85b-82ff0b97b88a`,
`19a8bdac-dc03-4378-8835-a14214be1ce7`, and
`7e990bf5-8e6e-45ea-9d8a-9b4a457a044d`: informational fitness had no critical
files; the hard surface was the active napkin at 441 lines / 22,689 chars. The
substance-preserving action was not "archive to lower a score". It was
entry-by-entry extraction, routing, and disposition: Capture H plus the
marshal-monitoring highlight moved to the branch-fitness thread record and
repo-continuity index; Charcoal's comms-research vector was verified against
its thread record and repo-continuity route; first-instance discoverability and
cron-shape drift observations stayed as promotion-triggered carry-forward. The
outgoing active window archive is only mechanical evidence after curation.

Re-grounded 2026-05-24T22:06Z under Pelagic claim
`5c146624-63c1-4006-a477-96d3d1b3c016`: informational and strict-hard fitness
remain soft-only at `SOFT (19 soft)`. The next useful work is still Phase 2 /
Phase 3 curation by verified substance, not critical/hard triage. Lunar's
22:06Z supersede also confirms the curation lane continues across tidy cycles
rather than closing at Cycle 5 land.

Re-grounded 2026-05-25T14:35Z under Breezy Flowing Dock claim
`f0062af4-2aef-4182-aefa-cee0fe837b78`: live fitness had regressed to
`CRITICAL (1 critical, 1 hard, 18 soft)` because the active napkin had reached
critical prose-width pressure and hard line/character pressure, while
`pending-graduations.md` was hard by line count. Substance-preserving response:
rotate the processed active napkin to
`archive/napkin-2026-05-25-breezy-critical-hard-curation.md`, split fresh
Briny/Hushed planning and auto-fix candidates into active shard
`pending-graduations/2026-05-25-planning-and-autofix-candidates.md`, and keep
the main register as a pointer-bearing queue. Follow-up validation:
`pnpm practice:fitness:informational` and `pnpm practice:fitness --strict-hard`
both exit 0 with `SOFT (19 soft)`. No comms files were moved, deleted, or
rotated; the 2026-05-25 comms-retention research constraint remains binding.

## Non-Goals

- Do not trim, compress, or weaken substance to satisfy fitness.
- Do not archive unprocessed content. Archive only after the source substance
  has been read, routed, and recorded.
- Do not promote PDRs, rules, or directive changes without owner approval where
  the target is principle-class or Core-governance-class.
- Do not mutate `.remember/` buffers directly; route plugin defects to the
  plugin contract.
- Do not widen into product code, PR #108 snagging, or EEF first-feature work.
- Do not reclassify the completed napkin rotation as incomplete just because
  upstream layers still need a drain.

## Owner-Decision Packet

These items require owner or owner-visible classification before implementation
should mutate permanent doctrine. The live due index now contains three body
entries; historical packet items are retained here only to explain earlier
routing and must be re-opened from current body evidence before action.

Current live due entries:

- **Original item 2 — Director pure-direction-only**: approve PDR-shaped
  portable doctrine route.
- **Original item 3 — Owner action is not a cure**: approve PDR-shaped
  portable doctrine route.
- **Original item 5 — Marshal-as-cycle-discipline**: decide whether current
  evidence routes to PDR-077 or a repo-local pattern first.

Historical packet items no longer present in the live due index:

- **Original item 1 — Sidebar protocol with default action**: routed out of the
  live due index; re-open only from a current body status or fresh owner
  decision.
- **Original item 4 — Verify-dont-trust**: routed out of the live due index;
  re-open only from a current body status or fresh owner decision.
- **Original item 6 — PDR-075 status review**: processed via dated PDR status
  review; further promotion still needs owner ratification or distinct second
  instance evidence.
- **Original item 7 — Current vs active plan-directory taxonomy**: routed out
  of the live due index; re-open only from a current body status or fresh owner
  decision.
- **Original item 8 — /remember write-time contract gap**: routed out of the
  live due index; re-open only from a current body status or fresh owner
  decision.

## Phase 0 — Baseline And Decision Packet

**Intent**: make the pressure precise before moving content.

Actions:

1. Re-run `pnpm practice:fitness:informational`.
2. Extract a current list of `status: due`, `status: overdue`,
   `status: graduated`, and malformed-status entries from
   `pending-graduations.md`.
3. Present the owner-decision packet above before mutating PDRs, rules, or
   directive surfaces.

Acceptance criteria:

1. The baseline line/char counts are updated from live command output.
2. Owner-gated entries are separated from agent-actionable entries.
3. Any deferral names the constraint and falsifiability check.

## Phase 1 — Repo-Continuity Drain

**Intent**: restore `repo-continuity.md` to its role as a live operational
index.

Actions:

1. Archive stale Current State prose whose details are historical session
   narrative, preserving it verbatim under
   `.agent/memory/operational/archive/`.
2. Replace the top Current State with the current branch, current HEAD, dirty
   worktree summary, active pause state, and next safe curation step.
3. Keep active thread tables and next-safe-step pointers current; do not move
   EEF product-work substance unless that lane is explicitly reopened.

Acceptance criteria:

1. Historical prose remains recoverable from the archive.
2. `repo-continuity.md` answers "what is live now?" within its split strategy.
3. Fitness improves by role-correct archival, not by deletion.

### Phase 1 Result — 2026-05-24

Completed by Shaded Silencing Dusk / `codex` / `GPT-5` / `019e59` under claim
`2dcb09b0-370b-43b0-bd5b-c6b2515b4314`.

Changes:

1. Archived the outgoing `## Current State` prose verbatim to
   `.agent/memory/operational/archive/repo-continuity-current-state-2026-05-24-shaded-silencing-dusk.md`.
2. Replaced the live Current State with a compact operational index:
   branch, HEAD, pause state, solo curator lane, napkin-rotation completion,
   and remaining memory-surface pressure.
3. Archived the verbose Active/Paused thread roster block into the same archive
   and left compact latest identity rows in the live register.
4. Reflowed inherited long prose lines without changing substance.

Validation:

- `pnpm exec markdownlint` on the live and archive repo-continuity files:
  passed.
- `git diff --check`: passed.
- `pnpm practice:fitness:informational`: exit 0;
  `CRITICAL (2 critical, 4 hard, 13 soft)`.
- `repo-continuity.md`: no longer critical; still hard by lines, characters,
  and prose width, with the remaining live width maximum at 291 chars.

## Phase 2 — Pending-Graduations Drain

**Intent**: make the pending register a queue again rather than a warehouse.

Actions:

1. Archive entries already marked `graduated` only after verifying their
   destination resolves and the archived body is no longer the only processed
   home.
2. Correct malformed status metadata where the status parser shows prose
   fragments instead of a lifecycle value.
3. Execute agent-actionable due entries whose destination is not
   owner-gated.
4. For owner-gated due entries, keep the full substance in place and attach the
   owner-decision packet pointer rather than silently promoting.
5. Split or archive old partial entries only after destination evidence is
   verified and all remaining unresolved substance has an explicit live route.

Acceptance criteria:

1. Every live due entry has one of: graduated destination, owner-decision
   pointer, or falsifiable blocker.
2. `pending-graduations.md` no longer contains resolved/graduated history as
   live queue mass.
3. No source substance is lost; archive or destination links are present, and
   no archive is used as a substitute for processing.

### Phase 2 Partial Result — 2026-05-24

Started by Shaded Silencing Dusk / `codex` / `GPT-5` / `019e59` under claim
`ba32e198-748c-489e-ab9f-666e453f281a`.

Changes:

1. Verified the two live resolved stubs
   `reciprocal-cross-agent-reviewer-dispatch` and
   `untracked-wip-whole-tree-lint-blocker` already had archive bodies in
   `pending-graduations-archive-2026-05-23.md` and permanent pattern homes.
2. Removed only those live resolved stubs and added a compact
   2026-05-24 resolved-stub drain note.
3. Regenerated the top `due` index from the current body state. It now names
   nine due entries and routes each to an owner-decision packet item,
   separate implementation lane, or plugin/plan-hygiene route.

Validation:

- `pnpm exec markdownlint .agent/memory/operational/pending-graduations.md`:
  passed.
- `git diff --check`: passed.
- `pnpm practice:fitness:informational`: exit 0;
  `CRITICAL (2 critical, 4 hard, 13 soft)`.

Disposition: Phase 2 is **partial**. The file remains critical by lines,
characters, and long metadata/table lines. Further drain requires either
owner-approved doctrine promotion for due PDR/rule candidates, separate
agent-tools implementation for the comms-watch cure, or a larger archive
rotation for resolved historical queue mass.

### Phase 2 Continuation — 2026-05-24 Processed-Body Archive

Continued by Sylvan Sprouting Petal / `codex` / `GPT-5` / `019e59` after the
owner correction that archive must happen only after curation.

Changes:

1. Verified that six 2026-05-24 entries were already processed and marked
   `status: graduated` with visible durable homes or routes.
2. Moved only those processed bodies into
   `.agent/memory/operational/archive/pending-graduations-archive-2026-05-24.md`.
3. Left concise live stubs in `pending-graduations.md` pointing at the archive
   and naming each durable home.
4. Confirmed the live `due` index now contains four entries:
   `director-pure-direction-only`, `owner-action-is-not-a-cure`,
   `marshal-as-cycle-discipline`, and `PDR-075 status promotion`.

Validation recorded for the archive move:

- `pnpm exec markdownlint` on the live register, archive file, and napkin:
  passed.
- `git diff --check`: passed.
- Targeted `rg` verification found the archive heading, six live
  `body archived 2026-05-24` stubs, and the napkin archive note.
- `pnpm practice:fitness:informational`: exit 0;
  `CRITICAL (1 critical, 5 hard, 14 soft)`.
- `pending-graduations.md` improved from 5,067 to 4,936 lines and from 122 to
  118 long prose lines, but remains critical.
- Follow-up ledger validation after preserving the Sylvan tooling notes reported
  `CRITICAL (1 critical, 6 hard, 13 soft)`: `pending-graduations.md` remains
  the only critical file; the active napkin is hard by line count because the
  note was preserved rather than trimmed.
- `pnpm practice:fitness --strict-hard` exits 1 on the same current state, so
  Phase 4 closure is not satisfied.

Disposition: Phase 2 is still **partial**. The remaining due set is not a
safe opportunistic promotion target: each live due entry requires owner
ratification, PDR review, or respect for an existing PDR lane before permanent
doctrine mutation.

### Phase 2 Continuation — 2026-05-24 Legacy Shard Graduated-Body Drain

Continued by Hushed Fading Hush / `codex` / `GPT-5` / `019e5a` under claim
`96528b72-ab33-4b6e-8faa-61bb49494951`.

Changes:

1. Verified the `Lockfile-in-flux without coordination-layer declaration`
   entry in
   `.agent/memory/operational/pending-graduations/2026-05-06-to-2026-05-21-legacy-backlog.md`
   was already marked `status: graduated`.
2. Verified the durable home in
   `.agent/skills/start-right-quick/shared/start-right.md` still carries the
   unclaimed-dirty-slice cross-check against repo-continuity, the touched
   thread record, and active plans before orphan classification.
3. Moved the full processed body into
   `.agent/memory/operational/archive/pending-graduations-archive-2026-05-24.md`.
4. Removed only that already-graduated body from the active legacy shard,
   leaving unresolved pending and partially-graduated entries live.

Validation:

- Targeted `markdownlint` passed on the active napkin, active legacy shard,
  pending-graduations archive, and this plan.
- Targeted `git diff --check` passed on the same touched documentation
  surfaces.
- Targeted `rg` verified the processed heading is absent from the active legacy
  shard, present in the archive, and still homed in
  `.agent/skills/start-right-quick/shared/start-right.md`.
- `pnpm practice:fitness:informational`: exit 0;
  `SOFT (20 soft) — informational mode`.
- `pnpm practice:fitness --strict-hard`: exit 0; `SOFT (20 soft)`.

Disposition: Phase 2 remains **partial**. This slice reduces processed live
shard mass but does not alter owner-gated due entries or untriggered pending
candidates.

### Repo-Continuity Live-State Correction — 2026-05-24

Continued by Sylvan Sprouting Petal / `codex` / `GPT-5` / `019e59` under claim
`5553664d-f8a2-4a73-9379-3a91b97a9255`.

Changes:

1. Verified local HEAD and remote branch state before editing:
   local `79c148e4`; origin `795d2e9d` at 2026-05-24T13:27Z.
2. Updated `repo-continuity.md` Current State so it no longer says the lane is
   solo while Shaded, Twilit, Charcoal, Breezy, and Estuarine are active on
   fresh claims.
3. Updated the current fitness pointer to this plan's latest validation:
   `CRITICAL (1 critical, 4 hard, 15 soft)`.

Disposition: this is an index repair, not an archive move. It makes the live
operational header truthful while leaving the larger hard-pressure
repo-continuity split/archive question for a later structural pass.

Validation:

- `pnpm exec prettier --check` on the touched repo-continuity, napkin, pass-log,
  and plan surfaces: passed.
- `pnpm exec markdownlint` on the touched repo-continuity, napkin, pass-log, and
  plan surfaces: passed.
- `git diff --check` on the touched repo-continuity, napkin, pass-log, and plan
  surfaces: passed.
- `pnpm practice:fitness:informational`: exit 0;
  `CRITICAL (1 critical, 4 hard, 15 soft)`.
- `pnpm practice:fitness --strict-hard`: exit 1;
  `CRITICAL (1 critical, 4 hard, 15 soft)`.
- `repo-continuity.md`: still hard at 754 lines / 47,469 chars; this slice was
  truth repair, not structural shrinkage.

### Phase 2 Continuation — 2026-05-24 Historical Pointer Drain

Continued by Sylvan Sprouting Petal / `codex` / `GPT-5` / `019e59` under claim
`601edc7d-f162-4f94-8312-fb6487368038`.

Changes:

1. Verified archive bodies and durable homes for seven already-graduated
   historical live-body stubs: ADR-041 top-level workspace tiers,
   pre-commit staged-scope, PDR-055/ADR-178 CLI affordance/build isolation,
   no-moving-target hook tightening, observability WS10/WS8.6 orthogonal axes,
   and PDR-056 inter-agent protocol gaps.
2. Removed those seven stubs from the live body of
   `pending-graduations.md`.
3. Left status-correction and graduation-log rows intact because they are
   historical ledger context, not unresolved live queue bodies.

Disposition: Phase 2 remains **partial**. This narrows resolved historical
queue mass without mutating owner/PDR-gated due entries or principle-class
homes.

Validation:

- Anchored `rg` found no remaining live-body stubs for the seven removed
  pointers; broad `rg` found only the intentional ADR-041 graduation-log row.
- `pnpm exec prettier --check` on the touched Phase 2 surfaces: passed.
- `pnpm exec markdownlint` on the touched Phase 2 surfaces: passed.
- `git diff --check`: passed.
- `pnpm practice:fitness:informational`: exit 0;
  `CRITICAL (1 critical, 6 hard, 13 soft)`.
- `pnpm practice:fitness --strict-hard`: exit 1;
  `CRITICAL (1 critical, 6 hard, 13 soft)`.
- `pending-graduations.md` is still the sole critical surface at 4,725 lines /
  337,418 chars / 86 critical prose-width lines.

### Phase 2 Continuation — 2026-05-24 Processed-Stub Drain

Continued by Sylvan Sprouting Petal / `codex` / `GPT-5` / `019e59` under claim
`7b2850b8-5677-4e88-8f15-f08734f11eb1`.

Changes:

1. Verified archive bodies and durable homes for seven 2026-05-24 processed
   stubs: comms-watch self-exclusion, sidebar protocol with default action,
   verify-dont-trust, cron owner-input precedence, `/remember` write-time
   contract route, current-vs-active plan-directory taxonomy, and the
   withdrawn Supertest classification conflict.
2. Removed those stubs from the live body of `pending-graduations.md`.
3. Kept the unresolved owner/PDR-gated due entries untouched.

Disposition: Phase 2 remains **partial**. This removes already-processed queue
mass and does not change the principle-class due set.

Validation:

- Anchored `rg` found no remaining live stubs for the seven removed entries.
- `pnpm exec prettier --check` on the touched Phase 2 surfaces: passed.
- `pnpm exec markdownlint` on the touched Phase 2 surfaces: passed.
- `git diff --check`: passed.
- `pnpm practice:fitness:informational`: exit 0;
  `CRITICAL (1 critical, 6 hard, 13 soft)`.
- `pnpm practice:fitness --strict-hard`: exit 1;
  `CRITICAL (1 critical, 6 hard, 13 soft)`.
- `pending-graduations.md` is still the sole critical surface at 4,672 lines /
  334,677 chars / 86 critical prose-width lines.

### Phase 2 Continuation — 2026-05-24 Legacy Implementation-Route Stub Drain

Continued by Sylvan Sprouting Petal / `codex` / `GPT-5` / `019e59` under claim
`8516bc73-b0b9-4590-9f7f-69269bf4b586`.

Changes:

1. Verified archive bodies and current homes for three processed stubs:
   CLI first-touch friction, pre-commit staged-content gating, and
   advisory-protocol enforcement via commit-queue guard.
2. Removed those stubs from the live body of `pending-graduations.md`.
3. Left current partially-graduated and owner/PDR-gated due entries untouched.

Disposition: Phase 2 remains **partial**. This is another resolved-history
drain, not a promotion of unresolved doctrine.

Validation:

- Anchored `rg` found no remaining live stubs for the three removed entries.
- `pnpm exec prettier --check`, focused `markdownlint`, and `git diff --check`
  passed on the touched memory/plan surfaces.
- `pnpm practice:fitness:informational`: exit 0;
  `CRITICAL (2 critical, 5 hard, 13 soft)`.
- `pnpm practice:fitness --strict-hard`: exit 1;
  `CRITICAL (2 critical, 5 hard, 13 soft)`.
- `pending-graduations.md` remains critical at 4,663 lines / 333,758 chars /
  86 critical prose-width lines.
- `napkin.md` is now critical at 462 lines because the pass preserved curation
  evidence; the next action is substance-led napkin processing/rotation, not
  reactive trimming.

### Active Napkin Processing — 2026-05-24 Charcoal Capture Tail

Continued by Sylvan Sprouting Petal / `codex` / `GPT-5` / `019e59` under claim
`e8f035f4-e371-4655-b033-718b62cb0c00`.

Changes:

1. Processed Charcoal's owner-authorised Sonar audit-trail capture into
   `docs/governance/sonar-disposition-policy.md` Block 2 so the explicit
   generated API-schema glob has the same durable policy rationale as the
   `sonar-project.properties` comment.
2. Processed Charcoal's compose-vs-emit substrate-pointer capture into the
   existing `pending-graduations.md` substrate-pointer v3 real-time absorption
   route, rather than creating duplicate doctrine.
3. Added a processing-status note to the active napkin so the capture tail is
   no longer raw, while leaving the source evidence intact.

Disposition: this is **napkin processing**, not a full napkin rotation. It
reduces unprocessed active-memory residue while preserving the historical
captures.

Validation:

- Targeted `rg` found the Sonar policy home, napkin processing-status note,
  pending substrate-pointer route update, pass-log pointer, and plan entry.
- `pnpm exec prettier --check`, focused `markdownlint`, and `git diff --check`
  passed on the touched policy/memory/plan surfaces.
- `pnpm practice:fitness:informational`: exit 0;
  `CRITICAL (2 critical, 5 hard, 13 soft)`.
- `pnpm practice:fitness --strict-hard`: exit 1;
  `CRITICAL (2 critical, 5 hard, 13 soft)`.
- `napkin.md` remains critical at 472 lines because the processing evidence was
  preserved; `pending-graduations.md` remains critical at 4,657 lines /
  333,334 chars / 86 critical prose-width lines.

### PDR-072 Downstream Core Cascade — 2026-05-24

Sylvan Sprouting Petal / `codex` / `GPT-5` / `019e59` took a
non-overlapping downstream Core slice while Shaded held the fresh
`pending-graduations.md` claim.

Changes:

1. Verified PDR-072 is Proposed and already carries the durable principle:
   knowledge curation is autonomic learning, and Practice-bearing repos have
   product-output and Practice-substrate-output axes.
2. Verified the live residual asks for `practice.md` and/or
   `practice-lineage.md` amendments rather than a convenience `principles.md`
   surface.
3. Amended `practice.md` to name the two output surfaces and treat
   Practice-substrate output as real throughput.
4. Amended `practice-lineage.md` with the portable learned principle so the
   concept travels with the Core.

Acceptance disposition:

- The PDR-072 downstream Core cascade has a durable home.
- The critical register still needed a later writable recheck after Shaded's
  claim cleared; that recheck is recorded below.

Validation:

- `pnpm exec prettier --check` on the touched Core/memory/plan surfaces:
  passed.
- `pnpm exec markdownlint` on the touched Core/memory/plan surfaces: passed.
- `git diff --check` on the touched Core/memory/plan surfaces: passed.
- `pnpm practice:fitness:informational`: exit 0;
  `CRITICAL (1 critical, 5 hard, 14 soft)`.
- `pnpm practice:fitness --strict-hard`: exit 1;
  `CRITICAL (1 critical, 5 hard, 14 soft)`.
- `pending-graduations.md`: still the single critical surface at 4,021 lines /
  287,706 chars / 64 critical prose-width lines.
- `practice-lineage.md`: hard at 834 lines / 48,673 chars after preserving
  the PDR-072 Core-trinity cascade; route this as Core-surface pressure, not as
  permission to drop substance.

### PDR-072 Residual Closure — 2026-05-24

Continued by Sylvan Sprouting Petal / `codex` / `GPT-5` / `019e59` under claim
`ab8b2180-ce89-4385-be89-22d71380c903` after Shaded's register claim cleared.

Changes:

1. Verified the PDR-072 homes: PDR-072, `practice.md`, and
   `practice-lineage.md`.
2. Verified the original body and Shaded processing note remain in
   `archive/pending-graduations-archive-2026-05-24.md`.
3. Removed the completed PDR-072 residual from the live
   `pending-graduations.md` body.
4. Added archive and pass-log notes naming the closure evidence.

Disposition: Phase 2 remains **partial** because `pending-graduations.md`
still holds other live residuals and owner/PDR-gated due items, but this
completed residual no longer occupies the critical live queue.

Validation:

- Targeted `rg` found no live `knowledge-curation-is-autonomic-learning` body
  in `pending-graduations.md`; the original body and residual-closure note are
  in the 2026-05-24 archive.
- `pnpm exec prettier --check` on the touched Core/memory/plan surfaces:
  passed.
- `pnpm exec markdownlint` on the touched Core/memory/plan surfaces: passed.
- `git diff --check` on the touched Core/memory/plan surfaces: passed.
- `pnpm practice:fitness:informational`: exit 0;
  `CRITICAL (1 critical, 5 hard, 14 soft)`.
- `pnpm practice:fitness --strict-hard`: exit 1;
  `CRITICAL (1 critical, 5 hard, 14 soft)`.
- `pending-graduations.md`: still critical at 4,005 lines / 286,628 chars /
  64 critical prose-width lines.

### PDR-073 Residual Closure — 2026-05-24

Continued by Sylvan Sprouting Petal / `codex` / `GPT-5` / `019e59` under claim
`276cc613-4c63-4b91-ac4a-e9f3d81e4949`.

Changes:

1. Verified PDR-073 is Proposed and carries the recursion-as-method doctrine.
2. Verified the original body and Shaded processing note remain in
   `archive/pending-graduations-archive-2026-05-24.md`.
3. Amended `practice.md` so the self-teaching property names the recursive
   substrate shape.
4. Amended `practice-lineage.md` with the portable learned principle.
5. Declined the optional new `.agent/directives/recursion-as-method.md`
   surface as duplicate because the Core trinity plus existing
   `metacognition.md` provide the session-open recognition path.
6. Removed the completed PDR-073 residual from the live register.

Disposition: Phase 2 remains **partial** because the register still holds live
owner/PDR-gated items and implementation residuals, but PDR-073 no longer
occupies live queue mass.

Validation:

- Targeted `rg` found no live PDR-073 residual body in
  `pending-graduations.md`; the original body and closure note remain in the
  2026-05-24 archive.
- `pnpm exec prettier --check` on the touched Core/memory/plan surfaces:
  passed.
- `pnpm exec markdownlint` on the touched Core/memory/plan surfaces: passed.
- `git diff --check` on the touched Core/memory/plan surfaces: passed.
- `pnpm practice:fitness:informational`: exit 0;
  `CRITICAL (1 critical, 5 hard, 14 soft)`.
- `pnpm practice:fitness --strict-hard`: exit 1;
  `CRITICAL (1 critical, 5 hard, 14 soft)`.
- `pending-graduations.md`: still critical at 4,178 lines / 278,725 chars /
  36 critical prose-width lines.
- `practice-lineage.md`: hard at 840 lines / 49,115 chars after preserving the
  PDR-072 and PDR-073 Core-trinity cascade; route this as Core-surface
  pressure, not as permission to drop substance.

### Practice-Lineage Hard-Pressure Response — 2026-05-24

Continued by Sylvan Sprouting Petal / `codex` / `GPT-5` / `019e59` under claim
`102dd629-685b-4531-b250-a40f99cf0cf3`, while Shaded held the fresh
`pending-graduations.md` width-repair claim.

Changes:

1. Stayed off `pending-graduations.md` while the critical surface was freshly
   claimed by Shaded.
2. Refined `practice-lineage.md` to keep the newly landed PDR-072 and PDR-073
   concepts at lineage weight, relying on PDR-072, PDR-073, and `practice.md`
   for full doctrine.
3. Tightened the existing concept-exchange and learning-before-fitness axioms
   without changing their substance, so the Core-lineage surface returned below
   the hard line and character limits.

Disposition: this is a Core-surface fitness response, not a deletion. The
lineage file still carries the concepts needed for outbound propagation, while
the longer explanatory homes remain available in the PDRs and `practice.md`.

Validation:

- `pnpm exec prettier --check` on `practice-lineage.md` and `napkin.md`:
  passed.
- `pnpm exec markdownlint` on `practice-lineage.md` and `napkin.md`: passed.
- `git diff --check` on `practice-lineage.md` and `napkin.md`: passed.
- `pnpm practice:fitness:informational`: exit 0;
  `CRITICAL (1 critical, 4 hard, 15 soft)`.
- `pnpm practice:fitness --strict-hard`: exit 1;
  `CRITICAL (1 critical, 4 hard, 15 soft)`.
- `practice-lineage.md`: soft at 829 lines / 48,449 chars, with max prose line
  still within its 100-character limit.
- `pending-graduations.md`: still the only critical surface at 4,114 lines /
  261,232 chars; prose width is now hard rather than critical, suggesting
  Shaded's width-repair claim made progress without this pass touching the file.

## Phase 3 — Distilled Graduation

**Intent**: graduate stable distilled entries into durable homes and make home
gaps visible.

Actions:

1. Walk the 59 current distilled entries by section.
2. For each entry, decide: already covered, stable with natural home, stable
   with home gap, or still validating.
3. Move only stable + natural-home entries after verifying the destination.
4. Record home gaps as pending-graduations entries or owner-decision items.

Acceptance criteria:

1. `distilled.md` retains only active high-signal learning without duplicating
   permanent docs.
2. Stable no-home entries have explicit owner-visible home-gap routes.
3. The result is validated by markdownlint and practice fitness.

### Phase 3 Partial Result — 2026-05-24

Started by Shaded Silencing Dusk / `codex` / `GPT-5` / `019e59` under claim
`5ca2fc0f-8290-401d-8fa0-50c1ee935aaf`.

Changes:

1. Verified permanent homes before pruning active distilled entries:
   PDR-061, PDR-067, PDR-068, PDR-069, `metacognition.md`,
   `per-user-memory-is-a-buffer.md`, `agents-default-no-gender.md`,
   `test-coverage-review-lens.md`,
   `citation-as-reasoning-at-moment-of-verdict.md`,
   `dogma-vocabulary-closes-inquiry.md`, `start-right-team`, and the
   2026-05-13 synthesis report plus its PDR/pending routes.
2. Moved the audit trail into
   `.agent/memory/active/archive/distilled-graduations-log-2026-05-14.md`
   under "Backfill rotation 2026-05-24 — verified distilled homes".
3. Pruned the duplicated live distilled bodies from `distilled.md`.

Validation:

- `pnpm exec markdownlint .agent/memory/active/distilled.md
  .agent/memory/active/archive/distilled-graduations-log-2026-05-14.md`:
  passed.
- `git diff --check`: passed.
- `pnpm practice:fitness:informational`: exit 0;
  `CRITICAL (1 critical, 5 hard, 13 soft)`.
- `distilled.md`: moved from critical to hard at 709 lines / 36,849 chars.

Disposition: Phase 3 is **partial**. `distilled.md` is no longer critical, but
it remains hard and still needs a full per-entry graduation/home-gap walk before
Phase 3 can be marked complete.

### Phase 3 Continuation — 2026-05-24 Verified Homes

Continued by Sylvan Sprouting Petal / `codex` / `GPT-5` / `019e59` under claim
`de7f6143-6a4e-49fa-bbcd-cbade138d068`.

Changes:

1. Verified durable homes for three recent distilled entries before editing:
   PDR-081 plus the curator-pass skill for metadata-only pass logs;
   `knowledge-preservation-over-fitness-warnings.md` for
   archive-after-processing; and `verify-dont-trust.md` for artefact-first
   verification.
2. Added those dispositions to
   `.agent/memory/active/archive/distilled-graduations-log-2026-05-14.md`.
3. Removed the duplicate live distilled bodies.

Disposition: Phase 3 remains **partial**. The continuation drains verified
duplication only; it does not complete the required full per-entry home-gap
walk.

Validation:

- `pnpm exec markdownlint` on the touched distilled, audit-log, napkin,
  pass-log, and plan files: passed.
- `git diff --check`: passed.
- Targeted `rg` verification found no old live distilled headings and found
  the same dispositions in the audit log.
- `pnpm practice:fitness:informational`: exit 0;
  `CRITICAL (1 critical, 6 hard, 13 soft)`.
- `pnpm practice:fitness --strict-hard`: exit 1;
  `CRITICAL (1 critical, 6 hard, 13 soft)`.
- `distilled.md`: 647 lines / 33,671 chars, still hard.

### Phase 3 Continuation — 2026-05-24 Second Verified-Home Slice

Continued by Sylvan Sprouting Petal / `codex` / `GPT-5` / `019e59` under
claim `e025d32d-98c3-4068-bae7-bedb02231139`.

Changes:

1. Verified durable homes for two additional distilled entries before editing:
   `recursion-of-doctrine-under-team-cadence-speed.md`,
   `substrate-pointer-read-as-current-state.md`, and PDR-080 for
   cadence-aware absorption; `docs/engineering/testing-patterns.md` and
   `.agent/directives/testing-strategy.md` for HTTP/MCP transport test
   classification.
2. Left *Bare timer races leak unless cleaned up* live because the located
   guarded-cleanup pattern covers timeout bounding but does not carry the
   `clearTimeout` / abortable-timeout disposal rule exactly.
3. Added those dispositions to
   `.agent/memory/active/archive/distilled-graduations-log-2026-05-14.md`.
4. Removed only the duplicate live distilled bodies.

Disposition: Phase 3 remains **partial**. This slice proves and removes two
more duplicate entries; it does not complete the required full per-entry
home-gap walk. The timer-race entry remains live because no exact durable home
was verified.

Validation:

- `pnpm exec markdownlint` on the touched distilled, audit-log, napkin,
  pass-log, and plan files: passed.
- `git diff --check`: passed.
- Targeted `rg` verification found the two pruned headings only in the audit
  log and confirmed the timer-race entry remains live in `distilled.md`.
- `pnpm practice:fitness:informational`: exit 0;
  `CRITICAL (1 critical, 6 hard, 13 soft)`.
- `pnpm practice:fitness --strict-hard`: exit 1;
  `CRITICAL (1 critical, 6 hard, 13 soft)`.
- `distilled.md`: 629 lines / 32,752 chars, still hard.

### Phase 3 Continuation — 2026-05-24 Third Verified-Home Slice

Continued by Sylvan Sprouting Petal / `codex` / `GPT-5` / `019e59` under
claim `a0150398-b3ef-48c7-a802-637d49ad31b2`.

Changes:

1. Verified durable homes for seven additional distilled entries before
   editing: six owner-working-style observations now carried by
   `.agent/directives/user-collaboration.md` § Owner Working Style, and
   the metacognition structural-cure entry now carried by
   `.agent/directives/metacognition.md` § Cure Shape.
2. Left the two owner-profile entries without exact homes live in
   `distilled.md`.
3. Added those dispositions to
   `.agent/memory/active/archive/distilled-graduations-log-2026-05-14.md`.
4. Removed only the duplicate live distilled bodies.

Disposition: Phase 3 remains **partial**. This slice proves and removes seven
more duplicate entries; it does not complete the required full per-entry
home-gap walk.

Validation:

- `pnpm exec markdownlint` on the touched distilled, audit-log, napkin,
  pass-log, and plan files: passed.
- `git diff --check`: passed.
- Targeted `rg` verification found the seven pruned headings only in the
  audit log and confirmed the two owner-profile entries without exact homes
  remain live in `distilled.md`.
- `pnpm practice:fitness:informational`: exit 0;
  `CRITICAL (1 critical, 6 hard, 13 soft)`.
- `pnpm practice:fitness --strict-hard`: exit 1;
  `CRITICAL (1 critical, 6 hard, 13 soft)`.
- `distilled.md`: 558 lines / 29,203 chars, still hard.
- Current fitness also reports `pending-graduations.md` at 4,823 lines /
  354,774 chars; this session did not mutate that Shaded-owned surface.

### Phase 3 Continuation — 2026-05-24 Sonar Hotspot Audit-Trail Home

Continued by Sylvan Sprouting Petal / `codex` / `GPT-5` / `019e59` under
claim `e26c90f1-28de-4b15-969e-df86fec4d7ad`.

Changes:

1. Verified that the live distilled entry about Sonar MCP hotspot comments had
   a natural durable home in `.agent/rules/sonarqube-mcp-instructions.md`
   § Hotspot review.
2. Amended that rule to state that `show_security_hotspot.comments` is not the
   verification surface for rationale visibility and that agents should inspect
   REST `/api/hotspots/show?hotspot=<hotspot-key>` changelog entries after a
   rationale-bearing hotspot status change.
3. Added the disposition to
   `.agent/memory/active/archive/distilled-graduations-log-2026-05-14.md`.
4. Removed only the duplicate live distilled body.

Disposition: Phase 3 remains **partial**. This slice proves and removes one
more duplicate entry; it does not complete the required full per-entry
home-gap walk.

Validation:

- Targeted `rg` found the old heading only in the audit log, found the REST
  hotspot changelog instruction in the Sonar rule, and found the plan/pass-log
  pointers.
- `pnpm exec prettier --check` on the touched rule, distilled, archive, napkin,
  pass-log, and plan files: passed.
- `pnpm exec markdownlint` on the same touched files: passed.
- `git diff --check` on the same touched files: passed.
- `pnpm practice:fitness:informational`: exit 0;
  `CRITICAL (1 critical, 5 hard, 13 soft)`.
- `pnpm practice:fitness --strict-hard`: exit 1;
  `CRITICAL (1 critical, 5 hard, 13 soft)`.
- `distilled.md`: 512 content lines / 26,850 chars, still hard by line count.
- `pending-graduations.md`: still the single critical surface at 4,515 content
  lines / 320,641 chars / 75 critical prose-width lines.

## Operational Track-Card Resolution — 2026-05-24

Continued by Sylvan Sprouting Petal / `codex` / `GPT-5` / `019e59` under
claim `c066aad0-50da-42b7-b2d8-b3451d7cd4cc`.

Changes:

1. Verified that
   `.agent/memory/operational/tracks/skills-standardisation-followup-timing.md`
   was an expired tactical card, not an active single-writer coordination
   surface.
2. Verified that its useful substance had durable homes: reviewer-dispatch
   detail and timing evidence in the agentic-engineering thread record,
   commands-retirement execution work in the current plan, and remaining
   follow-up work in `pending-graduations.md`.
3. Deleted the obsolete track card and replaced live references with resolved
   notes, so `tracks/` returns to README plus `.gitkeep` only.

Disposition: this is **operational memory cleanup after processing**, not
knowledge loss. The card was not archived as a substitute for processing; it
was removed only after destination surfaces were verified.

Validation:

- Targeted `rg` found the resolved pointers and confirmed
  `tracks/` contains only `README.md` and `.gitkeep`.
- `pnpm exec prettier --check` on the touched memory/plan surfaces: passed.
- `pnpm exec markdownlint` on the touched memory/plan surfaces plus
  `tracks/README.md`: passed.
- `git diff --check` on the touched memory/plan surfaces: passed.
- `pnpm practice:fitness:informational`: exit 0;
  `CRITICAL (1 critical, 5 hard, 13 soft)`.
- `pnpm practice:fitness --strict-hard`: exit 1;
  `CRITICAL (1 critical, 5 hard, 13 soft)`.
- Current critical surface: `pending-graduations.md` at 4,463 lines /
  316,994 chars / 72 critical prose-width lines.

## Critical Register Read-Only Triage — 2026-05-24

Continued by Sylvan Sprouting Petal / `codex` / `GPT-5` / `019e59` while
Shaded Silencing Dusk held a fresh claim on `pending-graduations.md` and its
2026-05-24 archive.

Findings:

1. The critical surface should remain the priority, but the active Shaded claim
   blocks direct writes by this session.
2. The live index now names three due entries:
   `director-pure-direction-only`, `owner-action-is-not-a-cure`, and
   `marshal-as-cycle-discipline`.
3. All three due entries are owner/PDR-gated rather than safe opportunistic
   promotions.
4. Read-only search found no obvious remaining `status: graduated` live bodies;
   the remaining mass is mostly pending or partially-graduated substance.

Disposition: next direct critical-register edit should wait for Shaded's claim
to clear or receive explicit non-overlapping routing. Until then, useful
curator work should avoid competing with the claimed critical file.

### Phase 3 Continuation — 2026-05-24 Reviewer-Pass Absorption Rule Home

Continued by Sylvan Sprouting Petal / `codex` / `GPT-5` / `019e59` under
claim `64c5f49a-fc63-4130-a03f-fceeac18c219`.

Changes:

1. Verified the live distilled entry "Reviewer passes need critical absorption,
   not blind acceptance" belonged in the existing `verify-dont-trust.md` rule.
2. Strengthened that rule with the exact behaviour: reviewer output is evidence
   to test, not another substrate pointer to trust; absorb only after checking
   high-stakes claims and reviewer blind spots against live artefacts.
3. Added the disposition to the distilled graduations log.
4. Removed the duplicate live distilled entry.

Disposition: Phase 3 remains **partial**. This removes a verified duplicate
without touching Shaded's claimed critical-register lane; the related
second-instance pattern candidate remains in `pending-graduations.md`.

Validation:

- Targeted `rg` found the old heading only in the audit log and found the exact
  reviewer-output instruction in `verify-dont-trust.md`.
- `pnpm exec prettier --check` on the touched memory/rule/plan surfaces:
  passed.
- `pnpm exec markdownlint` on the touched memory/rule/plan surfaces: passed.
- `git diff --check` on the touched memory/rule/plan surfaces: passed.
- `pnpm practice:fitness:informational`: exit 0;
  `CRITICAL (1 critical, 5 hard, 13 soft)`.
- `pnpm practice:fitness --strict-hard`: exit 1;
  `CRITICAL (1 critical, 5 hard, 13 soft)`.
- `distilled.md`: 504 lines / 26,483 chars, still hard by line count.
- `pending-graduations.md`: still the single critical surface at 4,413 lines /
  313,927 chars / 72 critical prose-width lines.

### Phase 3 Continuation — 2026-05-24 Compaction-Boundary Handoff Home

Continued by Sylvan Sprouting Petal / `codex` / `GPT-5` / `019e59` under
claim `967c9e60-ddf2-4586-a7e5-2e4e5f021099`.

Changes:

1. Verified the distilled compaction-boundary handoff entry belonged in the
   existing `handoff-messages-self-contained.md` rule.
2. Strengthened that rule so future-self handoffs flush exact runtime monitor,
   cron, watcher, and loop-instruction state and verify it on first wake-up.
3. Added the disposition to the distilled graduations log.
4. Removed the duplicate live distilled entry.

Disposition: Phase 3 remains **partial**. This removes another verified
duplicate without touching Shaded's claimed critical-register lane.

Validation:

- Targeted `rg` found the old heading only in the audit log and found the
  runtime-monitor / cron / watcher verification clause in the handoff rule.
- `pnpm exec prettier --check` on the touched memory/rule/plan surfaces:
  passed.
- `pnpm exec markdownlint` on the touched memory/rule/plan surfaces: passed.
- `git diff --check` on the touched memory/rule/plan surfaces: passed.
- `pnpm practice:fitness:informational`: exit 0;
  `CRITICAL (1 critical, 4 hard, 14 soft)`.
- `pnpm practice:fitness --strict-hard`: exit 1;
  `CRITICAL (1 critical, 4 hard, 14 soft)`.
- `distilled.md`: 490 lines / 25,684 chars, now soft rather than hard.
- `pending-graduations.md`: still the single critical surface at 4,223 lines /
  302,239 chars / 71 critical prose-width lines.

### Phase 2 Continuation — 2026-05-24 Active Queue Shard Split

Continued by Sylvan Sprouting Petal / `codex` / `GPT-5` / `019e59` under
claim `db13469d-1a55-4f90-abbc-b6740d891fa5`.

Changes:

1. Verified the 2026-05-23 first-out closeout / autonomy cluster was still
   unprocessed live queue substance, not archive-ready material.
2. Split the full cluster into
   `.agent/memory/operational/pending-graduations/2026-05-23-team-session-autonomy.md`
   as an active queue shard.
3. Added `.agent/memory/operational/pending-graduations/README.md` and an
   operational-memory README row so future curators know active shards are live
   pending queue surfaces.
4. Left a compact pointer in the main register with the shard's contents map.

Disposition: Phase 2 remains **partial**. This is a structural buffer split,
not a graduation or archive move: commit-queue ceremony cures, PDR-074 autonomy
primitives, and PDR-078 / ADR-186 heartbeat entries still require their normal
home/trigger work before archive closure.

Validation:

- `pnpm exec prettier --check` on the touched memory/plan surfaces: passed.
- `pnpm exec markdownlint` on the touched memory/plan surfaces: passed.
- `git diff --check` on the touched tracked surfaces: passed.
- `pnpm practice:fitness:informational`: exit 0;
  `CRITICAL (1 critical, 4 hard, 15 soft)`.
- `pnpm practice:fitness --strict-hard`: exit 1;
  `CRITICAL (1 critical, 4 hard, 15 soft)`.
- `pending-graduations.md`: 3,561 lines, 214,645 chars, and max prose line
  273. It remains critical by line count only; character pressure is hard, and
  prose width is green.

### Phase 2 Continuation — 2026-05-24 Legacy Backlog Active Shard

Continued by Sylvan Sprouting Petal / `codex` / `GPT-5` / `019e59` under
claim `914d42f1-f060-4b77-88c5-b710f438fb4e`.

Changes:

1. Verified the old `## Entries` body was live unresolved queue substance, not
   archive-ready material.
2. Split that body into
   `.agent/memory/operational/pending-graduations/2026-05-06-to-2026-05-21-legacy-backlog.md`
   as an active queue shard.
3. Adjusted moved relative links for the new directory depth.
4. Left a compact pointer in the main register and added the shard to the
   active-shards index and README.

Disposition: Phase 2 remains **partial**. The main register's access shape is
now closer to index/current-due/recent-routing, while older unresolved queue
substance remains live in the shard until processed entry by entry.

Validation:

- `pnpm exec prettier --check` on the touched memory/plan surfaces: passed.
- `pnpm exec markdownlint` on the touched memory/plan surfaces: passed.
- `git diff --check` on the touched tracked surfaces: passed.
- `pnpm practice:fitness:informational`: exit 0; `HARD (4 hard, 16 soft)`.
- `pnpm practice:fitness --strict-hard`: exit 1; `HARD (4 hard, 16 soft)`.
- `pending-graduations.md`: 764 lines / 40,064 chars / max prose line 260.
  Critical pressure is cleared; the file remains hard only by line count.

### Phase 2 Continuation — 2026-05-24 Shard Pointer Compaction

Continued by Sylvan Sprouting Petal / `codex` / `GPT-5` / `019e59` under
claim `579f318c-a165-4906-a6e4-18ece2701150`.

Changes:

1. Compactly replaced duplicated active-shard body pointers in the main
   register with a single pointer section.
2. Left the index entries and active shard bodies intact.

Validation:

- `pnpm exec prettier --check` on the touched memory/plan surfaces: passed.
- `pnpm exec markdownlint` on the touched memory/plan surfaces: passed.
- `git diff --check` on the touched tracked surfaces: passed.
- `pnpm practice:fitness:informational`: exit 0; `HARD (3 hard, 17 soft)`.
- `pnpm practice:fitness --strict-hard`: exit 1; `HARD (3 hard, 17 soft)`.
- `pending-graduations.md`: 741 lines / 38,880 chars / max prose line 260.
  It is now soft, with the live queue substance preserved in active shards.

### Continuity Re-ground After Critical Cleared — 2026-05-24

Continued by Sylvan Sprouting Petal / `codex` / `GPT-5` / `019e59` under claim
`cdb7a831-9a43-4aa5-91e8-e2ec1a45ff74`.

Changes:

1. Re-ran live state after compaction: local HEAD `79c148e4`, origin
   `795d2e9d`, fresh peer claims active on rule/practice, agent-tools, and
   source lanes.
2. Re-ran `pnpm practice:fitness:informational`: exit 0;
   `HARD (1 hard, 19 soft)`.
3. Corrected `repo-continuity.md` so it no longer says
   `pending-graduations.md` is critical or owned by Shaded's active drain.
4. Added the active shard routing to `repo-continuity.md` as live queue
   substance, explicitly not archive material.

Disposition: this is a post-critical truth repair. Phase 2 remains **partial**
because the active shard entries are still unprocessed; the successful outcome
is that the always-open main register is no longer critical while the
unprocessed bodies remain discoverable and live.

Validation:

- Focused `prettier --check`, `markdownlint`, and `git diff --check` on the
  touched continuity surfaces passed.
- `pnpm practice:fitness:informational`: exit 0; `HARD (1 hard, 19 soft)`.
- `pnpm practice:fitness --strict-hard`: exit 1; `HARD (1 hard, 19 soft)`.
- Remaining hard surface: `testing-strategy.md`.

### Legacy Active-Shard Verified-Home Drain — 2026-05-24

Continued by Sylvan Sprouting Petal / `codex` / `GPT-5` / `019e59` under claim
`591f366d-622c-45bd-b374-81788750597b`.

Changes:

1. Stayed off `.agent/directives/testing-strategy.md` because Shaded had a
   fresh claim on that hard surface.
2. Triaged active queue shards and selected the legacy Sonar hotspot
   audit-trail entry because it had an exact durable home.
3. Verified `.agent/rules/sonarqube-mcp-instructions.md` § "Hotspot review"
   already carries the required instruction: do not trust
   `show_security_hotspot.comments`; verify rationale through the Sonar REST
   hotspot `changelog`.
4. Removed the processed body from the legacy active shard and preserved the
   original text plus disposition in the 2026-05-24 pending-graduations
   archive.

Disposition: this is a true processed-body drain from an active shard, not an
archive dodge. The entry had a verified rule home before removal.

Validation:

- Focused `prettier --check`, `markdownlint`, `git diff --check`, and targeted
  `rg` verification passed.
- `pnpm practice:fitness:informational`: exit 0; `SOFT (21 soft)`.
- `pnpm practice:fitness --strict-hard`: exit 0; `SOFT (21 soft)`.

### Legacy Active-Shard Pattern-Home Drain — 2026-05-24

Continued by Sylvan Sprouting Petal / `codex` / `GPT-5` / `019e59` under claim
`3abd762c-a109-4e3a-bf7e-bc8a4b928c80`.

Changes:

1. Verified the legacy shard entry "deferred-at-write-time decisions are
   unmade load-bearing decisions" has an exact durable home in
   `.agent/memory/active/patterns/deferred-at-write-time-is-unmade-load-bearing-decision.md`.
2. Verified the pattern carries the diagnostic, worked instances, cure, and
   PDR-026 deferral-honesty cross-reference.
3. Removed the processed body from the legacy active shard and preserved the
   original text plus disposition in the 2026-05-24 archive.

Disposition: processed-body drain. The entry no longer belongs in the active
queue because its exact pattern home exists and carries the reusable substance.

Validation:

- Focused `prettier --check`, `markdownlint`, `git diff --check`, and targeted
  `rg` verification passed.
- `pnpm practice:fitness --strict-hard`: exit 0; `SOFT (21 soft)`.

### Legacy Active-Shard Peer-Commit Pattern Drain — 2026-05-24

Continued by Sylvan Sprouting Petal / `codex` / `GPT-5` / `019e59` under claim
`705dfcc7-30ef-4406-867d-616b5f70e252`.

Changes:

1. Verified the legacy "Peer-commit absorption (third-direction failure mode)"
   entry has an exact durable home in
   `.agent/memory/active/patterns/peer-commit-absorption-third-direction.md`.
2. Verified the pattern carries the 2026-05-11 Mistbound/Soaring worked
   instance, the three-direction model, shared root cause, unified cure,
   mitigations, and routing.
3. Replaced the duplicate active-shard body with a processed pointer and
   preserved the original text plus disposition in the 2026-05-24 archive.

Disposition: processed-body drain. The remaining implementation tail is still
live in the adjacent R4-new and commit-queue UX entries; this slice removed
only the duplicate worked-instance/pattern body.

Validation:

- Focused `prettier --check`, `markdownlint`, `git diff --check`, and targeted
  `rg` verification passed.
- `pnpm practice:fitness --strict-hard`: exit 0; `SOFT (21 soft)`.

### Legacy Active-Shard Insight-Item Drain — 2026-05-24

Continued by Sylvan Sprouting Petal / `codex` / `GPT-5` / `019e59` under claim
`b23c74f2-58bc-4859-9a1a-d9d268d5377f`.

Changes:

1. Verified the insight-report Item 12 "Cross-repo sibling list" candidate
   against the named check: `.agent/practice-core/practice-lineage.md` does not
   carry a sibling-repository map.
2. Verified the durable host-doc home exists at
   `docs/engineering/sibling-repos.md`, with curated Oak sibling repositories,
   roles, maintenance ownership, and review triggers.
3. Replaced the active-shard body with a processed pointer and preserved the
   original text plus disposition in the 2026-05-24 archive.

Disposition: processed-body drain. The candidate should not amend
Practice-Core because the sibling map is host-specific; the host engineering
doc is the durable home.

Validation:

- Focused `prettier --check`, `markdownlint`, `git diff --check`, and targeted
  `rg` verification passed.
- `pnpm practice:fitness --strict-hard`: exit 0; `SOFT (21 soft)`.

### Legacy Active-Shard Frictions-Register Drain — 2026-05-24

Continued by Sylvan Sprouting Petal / `codex` / `GPT-5` / `019e59` under claim
`74a3efbd-eaa5-4b1d-ad2a-fbb27243e087`.

Changes:

1. Verified the "Gatekeeper green-light stale-sweep race" entry has a durable
   operational home in `.agent/plans/agent-tooling/frictions-register.md` as
   `F-18 — Coordinator gate sweep stales when agents keep writing`.
2. Verified the frictions register carries the evidence, observed failure,
   expected invariant, candidate cure, target surfaces, and explicit `open`
   status.
3. Replaced the duplicate active-shard body with a processed pointer and
   preserved the original text plus disposition in the 2026-05-24 archive.

Disposition: processed-body drain into the agent-tooling frictions register. The
cure remains unresolved there; this slice deliberately did not invent or hide a
PDR route.

Validation:

- Focused `prettier --check`, `markdownlint`, `git diff --check`, and targeted
  `rg` verification passed.
- `pnpm practice:fitness --strict-hard`: exit 0; `SOFT (21 soft)`.

### Legacy Active-Shard Plan-Reachability Drain — 2026-05-24

Continued by Sylvan Sprouting Petal / `codex` / `GPT-5` / `019e59` under claim
`d41f9077-b7a5-4489-8f9c-2f95863a73c5`.

Changes:

1. Verified the plan-portfolio leaf-to-root reachability invariant already lives
   in `.agent/plans/README.md` under `Reachability Invariant — Leaf-To-Root`.
2. Verified the unresolved remediation and validator obligation lives in
   `.agent/plans/agentic-engineering-enhancements/current/plan-index-reachability-remediation.plan.md`
   with `decision-incomplete` status and Phase 4 validator acceptance.
3. Replaced the duplicate active-shard body with a processed pointer and
   preserved the original text plus disposition in the 2026-05-24 archive.

Disposition: processed-body drain into the existing plan substrate. The ADR and
validator work remains unresolved in the remediation plan and was not archived.

Validation:

- Focused `prettier --check`, `markdownlint`, `git diff --check`, and targeted
  `rg` verification passed.
- `pnpm practice:fitness --strict-hard`: exit 0; `SOFT (21 soft)`.

### Legacy Active-Shard Future-Plan Drain — 2026-05-24

Continued by Sylvan Sprouting Petal / `codex` / `GPT-5` / `019e59` under claim
`b8ba3f00-f905-4c45-89db-8f86dd38ebd3`.

Changes:

1. Verified the "Canonical tool definitions belong code-adjacent" entry has a
   durable work surface in
   `.agent/plans/agent-tooling/future/coordination-watcher-canonicalisation.plan.md`.
2. Verified the future plan carries the worked instances, design principles,
   phases, acceptance criteria, migration path, and activation trigger.
3. Replaced the duplicate active-shard body with a processed pointer and
   preserved the original text plus disposition in the 2026-05-24 archive.

Disposition: processed-body drain into the existing future plan. The plan remains
the live action surface; this slice did not prematurely author a rule or README.

Validation:

- Focused `prettier --check`, `markdownlint`, `git diff --check`, and targeted
  `rg` verification passed.
- `pnpm practice:fitness --strict-hard`: exit 0; `SOFT (21 soft)`.

### Team-Autonomy Active-Shard Commit-Queue Plan Drain — 2026-05-24

Continued by Sylvan Sprouting Petal / `codex` / `GPT-5` / `019e59` under claim
`97f6fdaa-cbfc-4991-9339-ca93ba008cf3`.

Changes:

1. Avoided the legacy backlog shard and shared 2026-05-24 archive because
   Shaded Silencing Dusk held a fresh claim on those files.
2. Verified the two 2026-05-23 commit-queue ceremony entries have an exact live
   work surface in
   `.agent/plans/agentic-engineering-enhancements/current/commit-queue-multi-writer-cure.plan.md`:
   Tranche C covers native intent-scoped commit message storage, and Tranche A
   covers line-scoped staging.
3. Replaced the duplicate active-shard bodies with processed pointers and
   preserved the originals in a Sylvan-specific 2026-05-24 archive file to avoid
   Shaded's claimed archive lane.

Disposition: processed-body drain into the existing commit-queue multi-writer
cure plan. The implementation, owner-review, and skill-amendment work remains
live in that plan.

Validation:

- Focused `prettier --check`, `markdownlint`, `git diff --check`, and targeted
  `rg` verification passed.
- `pnpm practice:fitness --strict-hard`: exit 0; `SOFT (22 soft)`.
  The additional soft item is `napkin.md` at 222 lines against a 220-line
  target; this is preservation-accepted routing pressure, not a reason to drop
  the curation observation.

### Critical-File Continuity Pointer Correction — 2026-05-24

Continued by Sylvan Sprouting Petal / `codex` / `GPT-5` / `019e59` under claim
`f30fa729-cec1-41ac-b8d8-49b4b0ea645c`.

Changes:

1. Re-grounded active claims and comms before editing; no new directed comms
   required action, and no Sylvan claim was open.
2. Re-ran `pnpm practice:fitness --strict-hard`: exit 0; `SOFT (22 soft)`.
   There are no current hard or critical files. The most fragile files are
   near-limit soft surfaces, so the safe "critical files" move was stale
   operational-pointer correction, not another body archive.
3. Corrected `repo-continuity.md` so its live state no longer says
   `pending-graduations.md` is critical after the fresh soft-only gate.

Disposition: processed as a continuity correctness fix. No unprocessed content
was archived or removed.

### Legacy Active-Shard Resolved-Stub Cleanup — 2026-05-24

Continued by Sylvan Sprouting Petal / `codex` / `GPT-5` / `019e59` under claim
`00588310-ad3d-44fb-8c77-c88c319ac917`.

Changes:

1. Re-grounded after Shaded's active napkin/distilled claim; stayed off those
   claimed files and worked only in the legacy active shard plus this ledger.
2. Verified five already-processed stubs still had archive bodies and durable
   homes:
   `docs/engineering/sibling-repos.md`,
   `.agent/memory/active/patterns/peer-commit-absorption-third-direction.md`,
   `.agent/plans/agent-tooling/frictions-register.md` `F-18`,
   `.agent/plans/README.md` plus
   `.agent/plans/agentic-engineering-enhancements/current/plan-index-reachability-remediation.plan.md`,
   and
   `.agent/plans/agent-tooling/future/coordination-watcher-canonicalisation.plan.md`.
3. Removed only those processed stubs from
   `.agent/memory/operational/pending-graduations/2026-05-06-to-2026-05-21-legacy-backlog.md`.
4. Left every adjacent unresolved candidate in place, including commit-queue UX,
   second-cycle re-plan, retrospective-tripwire, portable-reference,
   Practice-Core portability, and workspace-bootstrap entries.

Disposition: active-shard cleanup, not an archive substitute. The source bodies
already live in the 2026-05-24 archive, and the remaining unresolved entries
stay live in the active shard.

Validation:

- Targeted `rg` found the five removed headings absent from the active shard
  and present in the 2026-05-24 archive.
- Focused `prettier --check`, focused `markdownlint`, and `git diff --check`
  passed on the touched shard and ledger surfaces.
- `pnpm practice:fitness --strict-hard`: exit 0; `SOFT (20 soft)`.

### Live Validation Pointer Correction — 2026-05-24

Continued by Shaded Silencing Dusk / `codex` / `GPT-5` / `019e59` under claim
`d78be60e-535b-489c-b072-780b3a055dc9`.

Changes:

1. Re-grounded after the active napkin third rotation and current soft-only
   fitness run.
2. Corrected this plan's latest live-validation pointer from `SOFT (22 soft)`
   to `SOFT (20 soft)`.
3. Corrected the active-napkin status from near-target soft pressure to healthy
   after the reference-link width repair.

Disposition: live-plan truth repair. No source content was archived, removed,
or compressed for fitness.

Validation:

- Focused markdownlint and `git diff --check` passed.
- `pnpm practice:fitness:informational`: exit 0; `SOFT (21 soft)`.
- `pnpm practice:fitness --strict-hard`: exit 0; `SOFT (21 soft)`.

### Active Napkin Fourth Rotation — 2026-05-24

Continued by Shaded Silencing Dusk / `codex` / `GPT-5` / `019e59` under claim
`a29828c6-9899-4372-ba14-ad9cb94a3cc2`.

Changes:

1. Processed the fresh backtick-search recurrence as already carried by
   `distilled.md`.
2. Strengthened `distilled.md` so the command-chaining lesson covers
   independent evidence-gathering commands, not validation commands only.
3. Preserved the processed source at
   `.agent/memory/active/archive/napkin-2026-05-24-curator-fourth-rotation.md`.
4. Reset the active napkin to a fresh live capture.

Disposition: processed-source rotation. The active napkin is no longer carrying
unprocessed substance from this correction loop.

Validation:

- Focused markdownlint and `git diff --check` passed.
- `pnpm practice:fitness:informational`: exit 0; `SOFT (20 soft)`.
- `pnpm practice:fitness --strict-hard`: exit 0; `SOFT (20 soft)`.

### Phase 2 Continuation — 2026-05-24 Lifecycle Status Normalisation

Continued by Shaded Silencing Dusk / `codex` / `GPT-5` / `019e59` under claim
`03fffedd-e5c8-4c28-8534-646c3aca5114`.

Changes:

1. Normalised malformed lifecycle statuses in `pending-graduations.md` and the
   legacy active shard to the closed-vocabulary value `pending`.
2. Preserved the routing detail as adjacent prose notes: author-in-flight lane,
   pending second instance, plan-promotion trigger, and owner-direction
   prerequisite.
3. Left every unresolved entry body live; no archive move was made.

Disposition: Phase 2 metadata hygiene. This improves queue parseability without
mistaking metadata cleanup for graduation.

Validation:

- Focused markdownlint and `git diff --check` passed.
- `pnpm practice:fitness:informational`: exit 0; `SOFT (21 soft)`.
- `pnpm practice:fitness --strict-hard`: exit 0; `SOFT (21 soft)`.

### Phase 2 Continuation — 2026-05-24 Owner-Decision Packet Truth Repair

Continued by Shaded Silencing Dusk / `codex` / `GPT-5` / `019e59` under claim
`929970ac-ae54-4b0a-b0aa-e49ba5d5c54c`.

Changes:

1. Rechecked the live `pending-graduations.md` due index and confirmed it now
   contains three due entries, not the earlier eight-item packet.
2. Rewrote the owner-decision packet to distinguish current live due entries
   from historical packet items that have since been routed, processed, or
   moved out of the live due index.
3. Preserved the original item numbers in prose so existing queue references
   to owner-decision packet items 2, 3, and 5 still resolve.

Disposition: plan-truth repair. No pending entry body was archived, removed, or
promoted; the packet now prevents stale owner-decision guidance from routing
agents back into already-processed lanes.

Validation:

- Focused markdownlint and `git diff --check` passed.
- `pnpm practice:fitness:informational`: exit 0; `SOFT (21 soft)`.
- `pnpm practice:fitness --strict-hard`: exit 0; `SOFT (21 soft)`.

### Phase 2 Continuation — 2026-05-24 Due-Entry Route Hygiene

Continued by Dusky Stealing Veil / `codex` / `GPT-5` / `019e5a` under claim
`52ddb910-fe73-4041-83bd-cd08c6d9081e`.

Changes:

1. Rechecked the live due set and confirmed the register still has three due
   entries: Director pure-direction-only, Owner action is not a cure, and
   Marshal-as-cycle-discipline.
2. Added entry-local owner-decision pointers to each due body so the route is
   visible without relying on the top packet alone.
3. Left all three entries `status: due`; no owner/PDR-gated doctrine was
   promoted, archived, or removed.

Disposition: Phase 2 remains **partial**. This slice strengthens the
entry-level evidence for the owner-gated due set; it does not resolve the
owner decision or PDR/pattern classification.

Validation:

- Focused Prettier, markdownlint, and `git diff --check` passed.
- `pnpm practice:fitness --strict-hard`: exit 0; `SOFT (20 soft)`.
- `pending-graduations.md`: still soft at 734 lines / 38,357 chars / max
  prose line 260.

### Phase 2 Continuation — 2026-05-24 Stale Claim Archive

Continued by Shaded Silencing Dusk / `codex` / `GPT-5` / `019e59` under claim
`8db8fac5-7e3f-4e8b-b461-c0e6bdf639fe`.

Changes:

1. Ran the collaboration-state `claims archive-stale` helper at
   `2026-05-24T14:16:51Z`.
2. Moved eight TTL-expired active claims to
   `closed-claims.archive.json` with `closure.kind: "stale"` and evidence
   pointing at the expired claim.
3. Left current fresh peer claims live in `active-claims.json`; no source work
   area was entered.

Disposition: collaboration-state hygiene under consolidate-docs step 7e. The
claim records were archived only after the TTL decision was made by the helper;
no claim history was deleted.

Validation:

- Focused markdownlint and `git diff --check` passed.
- `active-claims.json` and `closed-claims.archive.json` parsed as JSON.
- `claims list` showed only fresh live claims after the archive pass.
- `pnpm practice:fitness --strict-hard`: exit 0; `SOFT (21 soft)`.

### Active Napkin Disposition Audit — 2026-05-24

Continued by Shaded Silencing Dusk / `codex` / `GPT-5` / `019e59` under claim
`d2f24c33-3772-48d1-8aee-b113e0941ea6`.

Changes:

1. Reviewed the four fresh active-napkin tooling mistakes after the fourth
   same-day active-napkin rotation.
2. Classified the `agent-tools` flag-shape mistakes as current worked
   instances of the existing distilled CLI flag-shape drift lesson.
3. Classified the shell-pipe mistake as covered by the existing distilled
   evidence-output readability lesson.
4. Left `distilled.md` untouched because Sylvan currently owns the distilled
   graduation lane.

Disposition: processed-current-capture audit. The active napkin remains live
and unarchived because it is far below rotation threshold; no substance was
trimmed or moved merely for fitness.

Validation:

- Focused markdownlint and `git diff --check` passed.
- `pnpm practice:fitness --strict-hard`: exit 0; `SOFT (21 soft)`.
- Active napkin remains healthy at 36 lines / 1,508 chars / max prose line 77.

### Phase 4 Validation Closure — 2026-05-24

Continued by Shaded Silencing Dusk / `codex` / `GPT-5` / `019e59` under claim
`280b01ed-7b00-4d77-9025-6eb772686cc1`.

Changes:

1. Re-grounded from the current active claims, live memory surfaces, and the
   strict-hard fitness gate.
2. Verified `pnpm practice:fitness --strict-hard` exits 0 with
   `SOFT (21 soft)`.
3. Updated this plan's truth surface so it no longer describes the current
   state as critical or hard.

Disposition: Phase 4 is complete for the hard/critical drain objective. Phase
2 and Phase 3 remain in progress because active shard processing, owner-gated
due entries, and full per-entry home-gap review are not complete.

Validation:

- `pnpm practice:fitness --strict-hard`: exit 0; `SOFT (21 soft)`.

### Phase 2 Continuation — 2026-05-24 Shard Status Hygiene

Continued by Pelagic Snorkelling Sextant / `codex` / `GPT-5` / `019e5b` under
claim `989ab7a1-0dff-46e2-a303-7876593cb87d`.

Changes:

1. Re-ran live fitness and confirmed there are no critical or hard files:
   `SOFT (19 soft)` in informational and strict-hard modes.
2. Selected the soft pending-graduations lane because Phase 2 and Phase 3 remain
   the live curation lanes while Practice Core and directive soft pressure is
   owner/context-budget sensitive.
3. Corrected the liveness heartbeat contract entry in
   `.agent/memory/operational/pending-graduations/2026-05-23-team-session-autonomy.md`
   from malformed lifecycle status `mid-authoring, paused for compaction` to
   valid `status: pending`.
4. Preserved the mid-authoring pause as route-state prose in the same entry;
   no doctrine was promoted, archived, or compressed.

Disposition: Phase 2 remains **partial**. This slice handles one malformed
status value from an active shard and leaves owner/PDR-gated substance live.
The next useful soft work is another per-entry shard pass, not broad Practice
Core or directive editing.

### Phase 2 Continuation — 2026-05-24 Legacy Shard Size Hygiene

Continued by Pelagic Snorkelling Sextant / `codex` / `GPT-5` / `019e5b` under
claim `d8772846-c453-495e-bda1-557a8148fcc2`.

Changes:

1. Re-ran live fitness and confirmed the curation lane remains soft-only:
   `SOFT (19 soft)` in informational and strict-hard modes.
2. Selected the legacy active shard because the current due entries remain
   owner/PDR-route gated and because shard metadata hygiene helps future
   per-entry processing without mutating doctrine.
3. Corrected the E-1/E-2 exploration-candidates entry in
   `.agent/memory/operational/pending-graduations/2026-05-06-to-2026-05-21-legacy-backlog.md`
   from invalid `size: M-to-L combined` to valid `size: L`.
4. Preserved the combined-work uncertainty as route-state prose in the same
   entry; no archive move, doctrine promotion, or content compression happened.

Disposition: Phase 2 remains **partial**. This slice handles one malformed
size value from an active shard and leaves the workstream scoping question live.

### Phase 2 Continuation — 2026-05-24 Legacy Shard XS Size Hygiene

Continued by Pelagic Snorkelling Sextant / `codex` / `GPT-5` / `019e5b` under
claim `9ebbd865-a178-4fd3-bffc-030452a4a779`.

Changes:

1. Re-ran live fitness and confirmed the curation lane remains soft-only:
   `SOFT (19 soft)` in informational and strict-hard modes.
2. Selected the legacy active shard because it still contained invalid
   closed-vocabulary metadata that could mislead future per-entry processing.
3. Corrected the `shouldInspectFile` single-positive-example coverage entry in
   `.agent/memory/operational/pending-graduations/2026-05-06-to-2026-05-21-legacy-backlog.md`
   from invalid `size: XS` to valid `size: S`.
4. Preserved the tiny focused cleanup meaning as route-state prose in the same
   entry; no archive move, doctrine promotion, or content compression happened.

Disposition: Phase 2 remains **partial**. This slice handles one malformed
size value from an active shard and leaves the validator-suite cleanup route
live for the next real touch of that code.

### Phase 3 Continuation — 2026-05-24 Peer-Pair Plan-Review Home

Continued by Pelagic Snorkelling Sextant / `codex` / `GPT-5` / `019e5b` under
claim `9a66f89b-4dbf-4d4d-9ce2-62aab5ac89fa`.

Changes:

1. Re-grounded after Charcoal's tidy-plan takeover broadcast and Mistbound's
   commit-marshal reminder; stayed off tidy-plan and commit-window lanes.
2. Re-ran live fitness and confirmed the curation lane remains soft-only:
   `SOFT (19 soft)` in informational and strict-hard modes.
3. Verified that the distilled peer-pair plan-review observation had a natural
   home in
   `.agent/memory/active/patterns/different-lens-reviewer-divergence.md`.
4. Added the plan-readiness peer-pair application to that pattern, preserving
   the Velvet/Charcoal Codex-vs-Claude finding distribution and when to apply
   it.
5. Added the disposition to the distilled graduations log and removed only the
   duplicate live distilled body; the late-peer all-channel watcher observation
   remains live in `distilled.md`.

Disposition: Phase 3 remains **partial**. This slice proves and removes one
verified duplicate distilled entry without claiming the full per-entry
home-gap walk complete.

### Phase 2 Continuation — 2026-05-24 Team-Shard Tempfile Candidate Drain

Continued by Pelagic Snorkelling Sextant / `codex` / `GPT-5` / `019e5b` under
claim `68758860-14c7-4b3d-b148-6280ccba349a`.

Changes:

1. Re-grounded after Seaworthy's Director routing update: Pelagic remains on
   the soft-surface curation lane, while Charcoal owns tidy cycles and
   Mistbound owns the commit-marshal lane.
2. Re-ran live fitness and confirmed the curation lane remains soft-only:
   `SOFT (19 soft)` in informational and strict-hard modes.
3. Selected the `2026-05-23-team-session-autonomy.md` active shard entry
   `Tempfile-path session-prefix discipline` because it had a verified durable
   home in PDR-076b.
4. Verified PDR-076b carries the worked tempfile-collision instance, the
   primary body-file frontmatter contract, and the explicit note that
   tempfile-path session-prefixing remains a secondary defence rather than a
   standalone primary rule.
5. Removed the processed body from the active shard and preserved the original
   body plus disposition in the 2026-05-24 pending-graduations archive.

Disposition: Phase 2 remains **partial**. This is a processed-body drain from
an active shard, not an archive tactic; the entry was removed only after the
durable PDR home was verified.

### Phase 3 Continuation — 2026-05-24 Late-Named-Peer Comms Home

Continued by Pelagic Snorkelling Sextant / `codex` / `GPT-5` / `019e5b` under
claim `d3b9749f-330d-4d79-8742-7151b08587ad`.

Changes:

1. Re-grounded after Mistbound landed Charcoal Cycle 2 at `4575044e` and after
   Seaworthy confirmed Pelagic remains on the soft-surface curation lane.
2. Re-ran live fitness and confirmed the curation lane remains soft-only:
   `SOFT (19 soft)` in informational and strict-hard modes.
3. Verified that the remaining distilled peer-pair plan-review observation had
   a natural home in `start-right-team`'s singleton-lane coordination rule.
4. Amended the singleton-lane rule so a named peer arriving after the first
   registry/comms check keeps early solo analysis provisional and requires
   final all-channel reconciliation before completion.
5. Added the disposition to the distilled graduations log and removed only the
   duplicate live distilled body.

Disposition: Phase 3 remains **partial**. This homes the late-named-peer
reconciliation lesson in the active team-start surface without claiming the
full per-entry distilled home-gap walk complete.

### Soft-Surface Continuation — 2026-05-24 Repo-Continuity Live-State Repair

Continued by Pelagic Snorkelling Sextant / `codex` / `GPT-5` / `019e5b` under
claim `9e7473d7-b112-4a58-a605-2bd02bdc848a`.

Changes:

1. Re-grounded after the Director heartbeat and confirmed the curation lane
   remains soft-only: `SOFT (19 soft)` in informational and strict-hard modes.
2. Selected `repo-continuity.md` because its live `## Current State` still
   pointed at stale HEAD `bf3a8152` and said only stale historical claims were
   present.
3. Updated the live index to current HEAD `4575044e`, Charcoal's fresh Cycle 3
   PDR-076a claim, the latest soft-only validation, and the current
   commit-marshal boundary.
4. Preserved the owner correction that archive moves are mechanical only after
   extraction, routing, and disposition; no archive move, content compression,
   doctrine promotion, staging, or commit action happened in this slice.
5. Follow-up claim `6fd81df6-7431-4857-b6d7-6c92ebc2a598` refreshed the same
   live index after the watcher caught Mistbound landing Charcoal Cycle 3 at
   `e8ca6d08`; the active commit queue was then empty.

Disposition: Phase 2 and Phase 3 remain **partial**. This slice keeps an active
operational knowledge surface truthful so future curation and commit routing do
not start from stale live-state prose.

Validation:

- Focused markdownlint on touched markdown: passed.
- Focused `git diff --check`: passed.
- `pnpm agent-tools:collaboration-state -- check`: passed.
- `pnpm practice:fitness:informational`: exit 0; `SOFT (19 soft)`.
- `pnpm practice:fitness --strict-hard`: exit 0; `SOFT (19 soft)`.
- Follow-up `pnpm agent-tools:commit-queue -- list --queue-status active`:
  `[]`.

### Soft-Surface Continuation — 2026-05-24 Cycle 4 Queue Drift Repair

Continued by Pelagic Snorkelling Sextant / `codex` / `GPT-5` / `019e5b` under
claim `8e48a99b-7f53-4b28-a504-de3d8aae73cb`.

Changes:

1. Re-grounded after Charcoal queued and then abandoned Cycle 4 intent
   `7dc6f8cc` from a stale view of whether Cycle 3 had already landed.
2. Confirmed from live git and queue evidence that HEAD remains `e8ca6d08`.
   The active queue first went empty after Charcoal abandoned `7dc6f8cc`, then
   a final queue sweep before claim close found Charcoal had re-enqueued Cycle
   4 as active intent `487c455b`.
3. Updated `repo-continuity.md` so its live state no longer says only Cycle 3
   was fresh or treats the Cycle 3 empty-queue snapshot as current after the
   Cycle 4 abandon and re-enqueue events.
4. Repaired the Deep Consolidation Status block so older `bf3a8152` prose is
   no longer presented as current HEAD proof; the durable trigger evidence is
   PR #108 merge `2462952a` plus Seaworthy event `703de8dd`.

Disposition: Phase 2 and Phase 3 remain **partial**. This slice preserves live
operational truth for future consolidation and commit routing; it does not
archive, compress, promote doctrine, stage, or commit.

Validation:

- Focused markdownlint on touched markdown: passed.
- Focused `git diff --check`: passed.
- `pnpm agent-tools:collaboration-state -- check`: passed.
- `pnpm agent-tools:commit-queue -- list --queue-status active`: one active
  Charcoal Cycle 4 intent, `487c455b`.
- `pnpm practice:fitness:informational`: exit 0; `SOFT (19 soft)`.
- `pnpm practice:fitness --strict-hard`: exit 0; `SOFT (19 soft)`.

### Phase 2 Continuation — 2026-05-24 Peer-Pair Design Sidebar Home

Continued by Pelagic Snorkelling Sextant / `codex` / `GPT-5` / `019e5b` under
claim `5411d483-f559-47a5-8d3c-2b0b15e07326`.

Changes:

1. Re-grounded with watcher and heartbeat live, then refreshed claims, queue,
   git, repo-continuity, and fitness before opening the claim.
2. Confirmed the curation lane remains soft-only: `SOFT (19 soft)` in
   informational and strict-hard modes.
3. Selected the legacy active-shard peer-pair sidebar entry because its
   remaining residual target was a durable home for the design-collaboration
   variant, not because the shard should be archived wholesale.
4. Added the design-collaboration variant to
   `.agent/memory/active/patterns/inter-agent-sidebar-with-default-action.md`:
   comparable agents, one shared append-only file, numbered turns, explicit
   joint-decision closure, helpers reserved for already-decided execution, and
   owner routing for owner-shaped questions.
5. Preserved the original active-shard body plus disposition in the 2026-05-24
   pending-graduations archive, then removed only that processed body from the
   legacy active shard.

Disposition: Phase 2 remains **partial**. This slice homes one residual
peer-pair design-sidebar lesson before removing the live pending body; it does
not archive for score reduction, stage, commit, or touch Charcoal's PDR/README
lane.

### Phase 2 Continuation — 2026-05-24 ADR-184 Residual Route-State

Continued by Pelagic Snorkelling Sextant / `codex` / `GPT-5` / `019e5b` under
claim `b92377f8-d305-465c-8303-a961924d1c6d`.

Changes:

1. Re-grounded with watcher and heartbeat live, then refreshed claims, queue,
   git, repo-continuity, thread state, and both fitness modes.
2. Confirmed the curation lane remains soft-only: `SOFT (19 soft)` in
   informational and strict-hard modes.
3. Selected the ADR-184 `sync` / `urgency` active-shard residual because live
   verification showed the implementation trigger has not fired, while the
   representation decision itself is already homed in ADR-184.
4. Added route-state evidence to the legacy shard: current comms schema still
   enumerates narrative, lifecycle, and directed event kinds without `sync`,
   and collaboration-state parser/rendering surfaces still treat `sync` /
   `urgency` as planned work rather than a supported protocol.

Disposition: Phase 2 remains **partial**. This slice preserves a live residual
with stronger falsifiability; it does not archive, remove, promote doctrine,
stage, commit, or touch Charcoal's PDR/README lane.

### Soft-Surface Continuation — 2026-05-24 Cycle 5 Queue Drift Repair

Continued by Pelagic Snorkelling Sextant / `codex` / `GPT-5` / `019e5b` under
claim `dd55a435-73b8-4862-85a5-6c30ba66748e`.

Changes:

1. Monitored all-channel comms after the ADR-184 residual slice and caught
   Charcoal's Cycle 5 ready-for-marshal broadcast for intent `9dc229c7`.
2. Refreshed active claims and the commit queue before closeout; live state now
   includes Charcoal Cycle 4 intent `487c455b` plus Cycle 5 intent `9dc229c7`,
   while HEAD remains `e8ca6d08`.
3. Repaired `repo-continuity.md` so its `## Current State` block no longer
   treats the earlier Cycle 4-only queue snapshot as current.
4. Preserved Seaworthy's Director broadcast `20eb10fc`: Mistbound silence after
   Cycle 3 is an owner-class marshal disposition question because the two
   Charcoal intents remain queued and no fallback marshal path is defined.
5. Logged the local `claims open` CLI-shape mistake in the napkin: use
   `--area-kind files` and explicit `--now` when opening a file-scoped claim.

Disposition: Phase 2 and Phase 3 remain **partial**. This is live-state truth
repair from comms monitoring, not archive/removal/doctrine promotion/staging or
commit-window work.

### Soft-Surface Continuation — 2026-05-24 Cycle 4 Landing And Cycle 5 Commit-Message Repair

Continued by Pelagic Snorkelling Sextant / `codex` / `GPT-5` / `019e5b` under
claim `72f57b05-3f1b-4310-b6c8-638a7f0af661`.

Changes:

1. Kept the all-channel watcher live and caught Mistbound's correction of the
   retirement-detection story: event `95c43df6` says Mistbound was alive and
   working queue-management, not retired.
2. Verified from git that Cycle 4 intent `487c455b` landed at `b7ac9938`, so
   `repo-continuity.md` no longer carries `e8ca6d08` as current HEAD.
3. Refreshed active claims and queue after the watcher surfaced the Cycle 5
   failure. The active queue is now empty, while Cycle 5 intent `9dc229c7` is
   preserved in `active-claims.json` as `abandoned`.
4. Routed the Cycle 5 failure as knowledge: pre-commit passed, but commit-msg
   rejected the 105-character subject against the 100-character commitlint
   limit; Charcoal must re-enqueue a shortened subject before Mistbound can
   marshal it.
5. Logged the coordination lesson in the napkin so future continuity repairs
   distinguish a successful queue item removed from the active list from a
   failed/abandoned item preserved in queue history.

Disposition: Phase 2 and Phase 3 remain **partial**. This slice repairs live
operational truth from comms, claims, queue, and git evidence; it does not
archive, remove, promote doctrine, touch Charcoal's PDR files, stage, or enter
the commit lane.

### Soft-Surface Continuation — 2026-05-24 Director Supersede And Cycle 5a Queue Truth

Continued by Pelagic Snorkelling Sextant / `codex` / `GPT-5` / `019e5b` under
claim `5c146624-63c1-4006-a477-96d3d1b3c016`.

Changes:

1. Re-grounded after Lunar's Director handoff and the brief 22:05Z wind-down
   routing, then caught Lunar's 22:06Z supersede from fresh owner direction:
   no pause, full speed ahead, complete the tidy plan, and keep instrumentation
   live.
2. Re-ran live fitness before editing. Both
   `pnpm practice:fitness:informational` and
   `pnpm practice:fitness --strict-hard` report `SOFT (19 soft)`.
3. Refreshed live claims and queue after Charcoal's Cycle 5a broadcast.
   Charcoal claim `18075549` is fresh on PDR-079 / rule / hook-scope files,
   and active queue intent `92f9c332` is still queued, but Mistbound's 22:06Z
   pre-flight says its 103-character subject will hit the same commitlint
   header limit unless Charcoal abandons and re-enqueues with a shorter
   subject. HEAD remains `b7ac9938`.
4. Updated `repo-continuity.md` so the live index no longer preserves the
   superseded wind-down as current direction and no longer says Cycle 5 is the
   only moving tidy-plan queue item. Follow-up watcher evidence also records
   the Cycle 5a subject-length pre-flight before closeout.
5. Logged the curation lesson in the napkin: when Director routing is
   superseded minutes later, preserve both events with their timestamps and
   repair the live surface from the newer controlling direction.

Disposition: Phase 2 and Phase 3 remain **partial**. This is live-state truth
repair while the curation lane continues; it does not archive, remove, promote
doctrine, touch Charcoal's PDR/rule/hook files, stage, or enter the commit lane.

## Phase 4 — Validation And Closure

Run one gate at a time:

```bash
pnpm exec markdownlint .agent/memory/active/napkin.md \
  .agent/memory/active/distilled.md \
  .agent/memory/operational/pending-graduations.md \
  .agent/memory/operational/repo-continuity.md
git diff --check
pnpm practice:fitness:informational
pnpm practice:fitness --strict-hard
```

Closure is allowed only when one of these is true:

1. strict-hard is no longer critical and all remaining hard items have an
   explicit remediation or owner-approved deferral; or
2. the owner explicitly accepts a remaining critical surface with a named
   constraint, evidence, and falsifiability check.

### Phase 4 Continuation — 2026-05-25 Shadowed Hard-Width Reflow

Continued by Shadowed Glimmering Moth / `codex` / `GPT-5` / `019e5d` under
claim `35b40096-3922-43f2-8731-8a60fe2c7dae`.

Actions:

1. Re-grounded under `oak-start-right-team` and `oak-consolidate-docs` with
   PDR-027 identity:
   Shadowed Glimmering Moth / codex / GPT-5 / `019e5d`.
2. Re-ran live fitness. `pnpm practice:fitness:informational` exited 0 with
   `HARD (1 hard, 19 soft)`, and `pnpm practice:fitness --strict-hard`
   exited 1 on the same single hard surface:
   `.agent/memory/active/napkin.md` had one prose-width line at 104 chars.
3. Reflowed the overlong napkin heading by moving the cycle list into adjacent
   body prose. No substance was deleted, compressed, or weakened.
4. Preserved the session learning in the napkin: fresh `comms watch` seen-files
   can replay historical backlog before live events, so live grounding needs
   the rendered comms tail plus claims and queue reads.

Disposition: Phase 4 remains complete for the hard/critical drain objective.
The single hard signal was structural line-width pressure in an active memory
surface, not a content-removal prompt. It is now remediated by reflow, while
the added learning remains preserved in the correct active-memory home.

Validation:

- `pnpm exec markdownlint .agent/memory/active/napkin.md`: exit 0.
- `pnpm practice:fitness --strict-hard`: exit 0; `SOFT (20 soft)`.

#### Thermal Buffeting Plume Pass - 2026-05-25T12:40:00+01:00

Validation slice:

- `pnpm practice:fitness:informational`: exit 0; `SOFT (20 soft)`.
- `pnpm practice:fitness --strict-hard`: exit 0; `SOFT (20 soft)`.

Disposition: hard/critical remediation is complete. The remaining soft files are
not a continuation mandate. The owner-requested Knowledge Curator alias
clarification was routed into PDR-081, the curator-pass skill, the
curator-passes README, start-right-team routing, and the Thermal pass log. Phase
2/3 soft-file work remains owner/trigger-gated.

## Carry-Forward From The Shaded Curator Pass

The pass log at
`.agent/memory/operational/curator-passes/2026-05-24-shaded-silencing-dusk.md`
is the pointer ledger for the napkin rotation and coverage audit. It should not
be expanded into a substance store. This plan owns the next structural lane.
