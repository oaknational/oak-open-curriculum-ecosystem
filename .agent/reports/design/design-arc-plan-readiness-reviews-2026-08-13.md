# Design-arc plan sketches — readiness review record (2026-08-13)

Two Opus reviewer passes over the born-sketch pair
`oak-identity-recognisability.plan.md` and
`showcase-information-architecture.plan.md`, run before ratification
per the plan skill's readiness step. Structural cures are absorbed into
the plan bodies (same day); every remaining finding is discharged by
the slice that owns its surface, by ID from this record. Findings were
seat-verified before adoption; one reviewer claim was refuted
first-hand and is marked so.

## Pass A — assumptions-expert (readiness, proportionality, assumptions)

- A-F1 depends_on/body sequencing contradiction → cured in bodies
  (slice-level sequencing; no plan-wide block).
- A-F3 unsatisfiable gate (targets not yet computable) → cured: gate
  ratifies set+basis+calibration procedure; thresholds carded at S2.
- A-F4 blind trials mis-typed repo-safe → cured: register-shaped,
  out-of-band execution, recomputed tally, earned authority
  (wow-verdict-register precedent).
- A-F5 wordmark gaming → cured: mark-masked lineups declared.
- A-F6 distractor licensing → cured: ablation distractors of our own
  face.
- A-F7 self-clearing exceptions → cured: DDR-008 closed predicate.
- A-F8/A-F9 threshold discipline / unlanded calibration → cured:
  method constants, S2 decision point with recorded degradation.
- A-F10/A-F11/A-F12 IA proof shapes → cured in IA body (computed-style
  cells; route-registry closure; instrument-first then owner).
- A-F13 duplication with design-system-completion (W1.4, W2.8, W2.9,
  W4.4) → cured: relationship section in both bodies; re-homing is one
  owner card, dated amendments at his word.
- A-F14 "cannot express" overclaim → cured: residual-sized S4 with
  per-dimension gap demonstrations.
- A-F15 admission rule undischared for own metrics → cured: axis +
  gaming-vector table; quality gates demoted to precondition.
- A-F16 no flap/exit policy → cured: bounded investigation cycle,
  recurrence cards, S5 round budget.
- A-F17/A-F18 lockfile pin nonexistent; DDR-006/007 collisions →
  cured: repo-URL+SHA provenance pin; manifest scoped to structure;
  expressive values as DDR-007 perceptual band with derivability.
- A-F19/A-F20/A-F21 capture regenerability/reachability/licensing →
  cured in S1 slice text (committed crops are the reference;
  first-hand reachability check; licence rows per LICENCES.md
  precedent).
- A-F22 "fleet unsanctioned, gate expires today" → REFUTED first-hand:
  the fleet plan carries a complete rev-2 ratification stamp
  (coordination `3b1e5fcce`); the stale gate ROW is a record-hygiene
  cure queued for the next coordination commit. The surviving kernel
  (name the dependency's state and a NO-GO fallback) is absorbed.
- A-F23 blanket block wastes throughput → cured: S1/S2 unblocked.
- A-F24/A-F25 dangling citations (DDR-009/010 branch-only; Director
  ruling pointer) → cured: pending-landing citations; comms event
  `4b5afe31` + thread record named.
- A-F26 repo→showcase narrowing unnamed → cured: named in Goal with
  follow-on-arc pointer.
- A-F27/A-F28/A-F29/A-F30 (S4 authorisation, owner-touch count,
  default-face derivation, first-principles section) → cured in
  bodies.

## Pass B — design-system-expert (architecture, measurement, slicing)

- B-F1 (sev 1) most named carriers ALREADY expressible — kit ships the
  double focus ring, zero-blur offset shadows, border-weight
  vocabulary, decorative-1..6 across five themes, `[data-page]`
  composition contract, icon-role indirection. True gaps only:
  illustration/geometry asset family + pack manifest; surface-class
  cascade axis; multi-tier chrome component; ramp-usage policy →
  cured: Mechanism's gap list rewritten; morphology moves to S3.
- B-F2 (sev 1) asset slots cannot be DTCG tokens (2025.10 type set
  closed; File type unresolved) → cured: sibling zod-validated
  identity-pack manifest emitting custom properties; `$extensions`
  only.
- B-F3 composite-type adoption (shadow/border/typography) inside the
  existing three tiers, not a new basis dimension → cured.
- B-F4 (sev 1) identity-pack package boundary (one package per
  identity, own licensing surface) as S4 prerequisite → cured: named
  slice prerequisite.
- B-F5 (sev 1) strategic falsifier unmeasured → cured: generality
  guard admitted (mechanism/identity ratio + identity-№N regression
  via a counter-brand exercising each new dimension differently;
  declared gaming vector "make the pack huge and call it config").
- B-F6/B-F7/B-F8/B-F9 identity-agnostic naming; surface-class axis as
  head mechanism; identity-parametric measurement rig; second token
  estate cross-reference → cured in Mechanism and S4 ordering.
- B-F10 (sev 1) fidelity pipeline is RATIFIED TRIAGE-ONLY (image-diff
  header; CLI: diff magnitude never affects exit code) → cured: layer
  3 is witness-producing register evidence adjudicated at round
  boundaries, never a CI gate.
- B-F11 (sev 1) pixel-diff fights DDR-007 perturbation → cured: layer
  3 scores structure (aligned luminance similarity) + relationship
  conformance, never colour equality.
- B-F12/B-F13/B-F14 pin wording; manifest structure-only; DDR-008
  single-regime → cured (converges with A-F17/A-F18/A-F7).
- B-F15 witness decay → cured: trial records and baselines
  digest-bound + judge-model-bound; stale witness = layer red.
- B-F16 judge-model pinning; n + confidence bound (4/5 at n=5 flaps)
  → cured: set at S2 calibration with a stated bound.
- B-F17 prohibition probes (absences: zero blur, no display face) via
  getComputedStyle across the identity × theme matrix, theme-aware →
  cured: probe classes named.
- B-F18 quality gates outside the orthogonality count → cured
  (converges with A-F15).
- B-F19–B-F23 S4 vocabulary DDR before implementation (semver
  surface); ratchet re-baseline per basis version; S5 round budget;
  head-slice order; branch dependency + A1↔S4 masthead ordering →
  cured in Todos.
- B-F24–B-F29 theme × asset strategy before content; slot
  cardinality/fallback with partial-pack proof; type-usage
  distribution probe; ramp order as composition policy; focus ring is
  S3 value delta behind an accessibility-expert gate; parity manifest
  generated from pack manifest → absorbed into the owning slice text;
  discharged at pickup by ID.
- B-F30–B-F33 IA: computed-style cells (15 = 3 identities × 5
  themes); private tier separate; a11y sweep joins; gate interaction
  (capture corpus pins identity=oak; pds/emc2 as distractors) → cured
  in both bodies.

## Disposition rule

A slice picking up S1–S5 or A1–A3 reads this record and lists, in its
PR body, the finding IDs it discharges. A finding no slice discharges
by arc close is surfaced at the final owner card, never silently
dropped.
