Files read:
- `.agent/skills/domain-craft/ui-design/ui-visual-design/SKILL-CANONICAL.md`
- `.agent/skills/domain-craft/ui-design/ui-visual-design/references/craft-fundamentals.md`
- `packages/design/oak-design-system/studio-source/preview/motion.html` (grepped for duration values)
- Directory listing of `packages/design/oak-design-system/studio-source/components/OakTag/` (checked for an existing pill/chip/tag component precedent; not read in full)

Reasoning: I followed the skill's fixed critique order (purpose, eye-path, grouping, touch/affordance, theme survival, removal) rather than free-associating a defect list. The colour-only grey→green transition is the anchor finding because the skill flags "state is never colour alone" and the colour-safe/high-contrast theme rule twice, and it's the failure that makes every other defect worse (a 600ms colour-only fade is undermined further by being colour-only). I grepped the design system's motion preview to confirm the actual system durations (120ms/200ms) rather than trusting the skill doc's stated numbers blindly, so the "600ms is invented" claim is grounded in the live system, not just the reference doc. I flagged 32px height and 11px type as likely-invented values per the skill's framing that inventing a value is the failure mode this skill exists to prevent, but I stopped short of asserting a specific correct token since I didn't load the full type/spacing scale — I told the user to pull from the system rather than naming a token I hadn't verified. I treated the ambiguity of whether the pill is tappable or passive as a first-class finding, since the skill's interaction section makes the touch/affordance question load-bearing for both the sizing and the styling verdicts.
