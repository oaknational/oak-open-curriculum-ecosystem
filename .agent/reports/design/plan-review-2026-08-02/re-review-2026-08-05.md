# Scoped near-horizon re-review — round 1 record (2026-08-05)

The first scoped re-review of the design-system-completion plan's
near-horizon slice (W0+W1 + cross-cutting sections) under the 2026-08-03
partition ruling. Run `wf_ca61fcdf-52c`: 18 agents, 0 errors, ~2.25M
subagent tokens, over pinned main `188a0c8a5` (post-#782). Reviewed and
adjudicated first-hand by the design seat (Saffron guards Hedgerow,
8a4280); Director adjudication (Petrel holds Turbulence, a0892f) same day
on the ARC channel (`.agent/collaboration/rapid-comms/2026-08-05-design-lane-petrel-holds-turbulence-saffron-guards-hedgerow.md`).
Fleet shape: five near-horizon finder lenses (design-system-expert,
accessibility-expert, test-expert, goal-alignment, internal-consistency;
far-horizon mechanism excluded BY LENS per the seam warning), two ledger
verifiers, one completeness sweep, adversarial verification with a
declared 10-verify cap (overflow reported unverified, never dropped).

## Verdict

NOT a zero round: **20 real findings** — 3 CONFIRMED under adversarial
verification, 17 beyond-cap findings all adjudicated REAL at the design
seat (each verified first-hand: pre-fleet reads, the #782 diff, and four
targeted file probes), 7 REFUTED (grounds read in full and accepted; each
refutation falsified load-bearing evidence, none leaned on scope
technicalities).

Confirmed set: the W0.2(b) scoped-role-token contradiction (MATERIAL —
manifest membership forces a dtcg leaf whose consistency-gate comparand is
top-level `:root`, which the "band scope" declaration wording forbade;
empirically reproduced, including the unnamed second re-baseline
`EXPECTED_COMPARAND_SIZE` 167→168); the axe-`incomplete` blind spot in the
harness the plan hoists (MATERIAL — violations-only assertions swallow
axe's unreviewed-potential-violations array, on a kit with a recorded
4.48:1 near-miss; the estate's own `CLEARED_INCOMPLETE` cure existed in
the MCP app and was not carried); and the twin-baseline omission (MINOR,
folded into the first cure).

Beyond-cap set (all real): v2.1-era frontmatter identity (four demos /
v2.1 against the ratified five / v2.2), two W2–W6 far-horizon ranges
omitting W7, the §Workstreams preamble's sequencing chain and
"W1 needs only" list omitting W0.5/W0.10, §Sequencing's overstated
first-pixels "only" claim, the post-#782 gate-ordinal off-by-one (three
"frontmatter gate 4" citations + the §Owner rulings narrative), the
stale "priced at the W2.7 session" decision-log row, the EX53
register-naming leftover in the first-principles check, the
"never authored" ledger sentence superseded same-day, the W1.1 vitest
base-config trap, the W0.6 retirement-semantics ambiguity, the W0.8
HTML-arm vacuity gap, the cross-demo at-rule blind-spot implication, the
W0.4/W0.7 missing describing-surface tags, and the W0.7
unearned-instrument-block escalation gap.

Ledger: `dispositions.v2.2.md` (authored from living memory 2026-08-05)
verified **57/57 rows**, one citation nit (phantom D17; corrected by dated
addendum). Completeness sweep: **113/113** round-3 rows accounted (ledger
∪ pointer tables), zero gaps.

## Adjudication and cures

Director adjudication (2026-08-05, ARC): set accepted in full. The one
design call — the W0.2(b) declaration shape — resolved as **top-level
`:root` declaration in both polarity arms + band-scoped APPLICATION**
(the band rule consumes the variable); the manifest scope-field
alternative stays the recorded fallback per the EX4 ledger row. The landed
accessibility bar does not move.

All cures landed in the change this record rides with: nine residue
truings (class A), eight specification cures (class B), the dated ledger
addendum (class C). The plan's §Review record carries the round entry.

## Next act

The scoped re-review re-runs over the cured text. The **slope rule**
binds (Director's word, from the round-3 divergence lesson): the re-run
converges to near-zero or the loop stops for structural diagnosis — never
a third full round. After a clean round: the owner's implementation word.

## Process notes

Mid-round, the owner's W0.5 enumeration sitting produced ten tweak items
and three instrument words (recorded in
`.agent/reports/design/design-sitting-records-2026-08-05.md`); per the
Director's process ruling those plan amendments adjudicate in ONE window
with this review's close, so the fleet's target text never moves under a
running round. The full finding corpus with verification grounds is in the
session's workflow output (run `wf_ca61fcdf-52c`); this record is the
durable adjudication summary.
