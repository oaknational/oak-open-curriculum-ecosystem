# Scoped near-horizon re-review — round 2 record (2026-08-06; combined window executed 2026-08-08)

Scope: the near-horizon slice (W0 + W1 + cross-cutting sections) of
`design-system-completion.plan.md`, under the 2026-08-03 partition ruling —
the round-1 record's pre-committed next act. Run `wf_bd16152b-ee8`: 15
agents, 5 lenses, 0 errors, ~1.16M subagent tokens, over pinned
`568160df7` (the round-1 cures branch head, PR #785 — since merged).
Reviewed and adjudicated first-hand at the design seat (Saffron guards
Hedgerow, 8a4280; round-2 package events `89ca156c` directed / `70f47b9d`
broadcast). Director adjudication: Petrel holds Turbulence (a0892f),
2026-08-06, mandate re-broadcast as event `a729c466`. Fleet shape: five
finder lenses; adversarial verification under the declared 10-verify cap,
the seven beyond-cap findings adjudicated real first-hand at the seat by
targeted probes on the pinned tree — overflow adjudicated, never dropped.

## Review contract (per `.agent/reports/README.md` §Review Contract)

Purpose and impact: the durable adjudication summary of scoped re-review
round 2 and the combined cure window it mandated — the record a later
reader uses to trust that the plan's zero bar was honestly reached.
Questions a review should test: does each of the nine findings carry a
verification class and a landed class-wide cure; does every confirmed
W0.5 ledger item carry its owner blocking word unaltered and a present
landing; do the delta-verify rounds show monotone convergence to zero
blockers. Evidence standard and authority boundary: statements here cite
events by uuid, runs by workflow id, code by pinned sha; this record
DESCRIBES adjudications made under the Director's mandate `a729c466` and
authorises nothing itself. Non-goals: it does not re-open refuted
findings, schedule the state-once-point-elsewhere refactor, or pre-empt
the owner's implementation word. A successful review either confirms the
accounting against the cited primaries or reports a specific mismatch
(finding, ledger item, or pointer) with its evidence; missing evidence is
reported as a gap, never inferred.

## Verdict

17 raw findings → **9 REAL (6 MATERIAL, 3 MINOR)** after adversarial
verification and first-hand adjudication, with 7 REFUTED (grounds read in
full and accepted — misread referents, false premises, inverted
arithmetic; none leaned on scope technicalities) and one two-lens
duplicate merged. SLOPE: 20 real (round 1) → 9 real (round 2) — shrinking,
NOT near-zero, so per the round-1 record's pre-committed stop condition
the LOOP STOPPED here for structural diagnosis. No round 3 ran.

Real set (each verified or first-hand adjudicated on the pinned tree):

1. **MATERIAL (verified)** — W0.3's DoD demands three non-axe cells "each
   naming its instrument from W0.8's hoist"; the hoist scoped two. SC
   1.4.12 text spacing had NO instrument anywhere in the estate
   (grep-exact, re-verified 2026-08-08 at the cure window). Verifier
   note: small to build — text-spacing override CSS + the landed
   overflow measure (`demos/oak-curriculum-hub/tools/measure-320.ts`).
2. **MATERIAL (verified)** — W0.6's hub browser suite escaped the
   both-arrays contract; its retirement ground ("browser axe measures what
   the disable compensated for") is FALSE at unresolvable-background
   nodes (in-estate proof: the MCP-app landing-page CLEARED_INCOMPLETE
   case). Reviewer-recompute assurance would have been deleted and
   replaced by an instrument blind to the same nodes.
3. **MATERIAL (verified, scope-trimmed)** — the describing-surface
   convention (EX40) was cured point-wise onto W0.4/W0.7 only; W1.4 was
   the load-bearing untagged instance (repo-safe acceptance over a
   committed artefact). Owner-held stories correctly out.
4. **MATERIAL (merged from two lenses; Director spot-verified)** — the W1
   preamble's first-pixels gate statement still read "needs only W0.2(a)
   + W0.7 v0 + W0.9" while citing the §Sequencing text round 1 had cured
   to add W0.5's blocking tweaks — the third statement of the gate left
   self-contradicting.
5. **MATERIAL** — the gate-2 discharge pointer claimed the priming
   evidence + corrected attribution "now live with W2.7 and §Decision
   log"; neither home carried them. §Direction point 9 (the off-horizontal
   attribution to the identity since renamed PDS) vs W2.7's delivered
   values was a bare reversal with no supersession record; the surviving
   attribution substance sat unnamed at W0.4.
6. **MATERIAL (Director spot-verified)** — W1.3 required recording "the
   rubric evaluation results" into the wow-verdict register whose
   W0.7-minted schema had no such field — breaking W0.7's
   earned-authority miss-rate mechanism at its only named home.
7. **MINOR** — W1.1 as specified landed no vitest-visible test while
   mandating `--passWithNoTests=false` → its own acceptance (pnpm check
   green from cold) unreachable at the landing PR.
8. **MINOR** — the demos-tier test-naming convention (charter, `.spec.ts`
   under `tests/`) sat unreconciled with `testing-strategy.md`'s
   `e2e-tests/`/`*.e2e.test.ts` rule, and the plan's ADR-obligations list
   named no testing-strategy amendment despite its own
   each-amendment-is-an-acceptance-line discipline.
9. **MINOR** — "creature" was a dangling register name: the round-1
   deletion cure removed its sole binding (the old gate-2 text's
   "EMC²/creature").

## Structural diagnosis (seat-authored, Director-accepted)

At least six of the nine are CURE-EDGE RESIDUE of round-1's point-instance
curing: cures applied to the cited statement of a multiply-stated
invariant but not the class (3, 4); two contracts cured separately leaving
their intersection open (2); deletion cures whose pointer-claimed
re-homing never landed (5, 9); an adjudicated-but-internally-asymmetric
absorption reproduced faithfully (1). The generator: multiply-stated
invariants + point-instance cures under round pressure + deletions without
landing-verification of their pointers.

## Adjudication and the combined window

The Director ENDORSED the round-2 result in full (event `a729c466`;
findings 4 and 6 spot-verified first-hand on the pinned tree, both held).
Disposition adopted: ONE COMBINED WINDOW — the nine cures applied
CLASS-WIDE (grep-enumerate every statement of each touched invariant
before editing; verify every pointer's landing), folded WITH the
owner-confirmed W0.5 amendment set (all ten items + three instruments,
CONFIRMED AS PROPOSED at event `0d5afca6`; the Director's 2026-08-05
process ruling routes the W2.9 reference-protocol note and the
content-provenance manifest's identity-values column into the same
window), closed by a scoped DELTA-VERIFY over the touched statements only
— never a full round 3. The zero bar is preserved: the owner's
IMPLEMENTATION WORD comes at the delta-verify's clean close. The heavier
state-once-point-elsewhere refactor is a named post-first-light hardening
pointer — recorded in the plan's §Review record, not scheduled.

The window SEALED at adjudication and OPENED at the owner's design
work-word, 2026-08-08 ("Time to unseal, time to make progress"). The
executing seat: Civet spins Cavern (054f5e), claim `645b9e0b`, per the
Director's ROUTE (event `b7e4b897`). Pre-authoring gates: the full Cricket
quartet ×2 over the sitting plan (5 ON-TRACK / 3 DRIFTING, all dissents
provenance-class; routed non-unanimous to the Director, events
`1c4957f1`/`9ae8fb56`; adjudicated PROCEED with every dissent dissolved on
first-hand-verified primary sources), and the class-wide statement
enumeration run as an 11-leg read-only workflow (`wf_45de2e3c-68c`, 11/11
legs, 0 errors) before any edit.

### Cure accounting (the nine, class-wide)

Each row: the finding → where the cure landed (story anchors in the plan)
→ the class sweep result from the enumeration legs.

1. Text-spacing instrument → W0.8's hoist list names it as the third
   non-axe instrument (override values, shared-harness application, the
   `measure-320.ts` pattern as precedent). Sweep: the invariant is stated
   at exactly two load-bearing sites (W0.3 DoD; W0.8 hoist) — both now
   agree; the four generic charter-DoD pointers inherit without edit.
2. Both-arrays × hub suite → four sites cured: the W0 preamble's naming,
   W0.6's suite minting, W0.6's retirement semantics (the BOTH-ARRAYS
   suite is the replacing instrument; the unfalsifiability principle
   extended to browser runners at unresolvable-background nodes), and
   W0.6's acceptance (CLEARED_INCOMPLETE allowlist line). W0.3/W0.8/W1
   statements were already consistent (charter-routed).
3. Describing-surface tags → the convention statement gains the
   pointer-story scoping clause (tags bind at story open for W2–W7);
   W1.4 gains the full tag (the W0.4 model); terse tags added at W0.2,
   W0.3, W0.9, W0.10, W1.1, W1.5; W7's heading gains the pointer-story
   marker. Consistent without tags (boundary matches preamble): W0.1,
   W0.6, W0.8, W1.2; exempt owner-held-only: W0.5, W1.3.
4. First-pixels gate → the single surviving inconsistent statement (the
   W1 preamble) gains the W0.5 blocking-tweaks arm. Sweep confirmed all
   sibling statements (workstreams preamble, W0.5, W0.8 exclusion side,
   W1.2, §Sequencing both sides) already consistent.
5. Gate-2 discharge pointer → the §Owner rulings paragraph now names the
   real homes (values at W2.7 with provenance; the corrected attribution
   record AT W0.4, now named as such); §Direction point 9 carries its
   dated supersession note in place (owner words retained); the
   §Decision log row's provenance cell trued; the discharged gate's
   three alias names unified to one.
6. Register schema → W0.7's mint gains the seventh field (instrument-leg
   results; required at checkpoint-class rows, optional at the
   W0.9/W0.10 pre-reads, which run before the instrument exists);
   §Quality bar rule 1 echoes it. W1.3 stands as written and is now
   satisfiable; the miss-rate mechanism has its per-row home.
7. W1.1 vitest reachability → the kit-copy parity rows are named AS the
   workspace's first vitest-visible co-located unit test (showcase
   precedent `tools/kit-asset-parity.unit.test.ts`) — zero added scope;
   the W0.3 charter clause pairs the flag with a named vitest-visible
   test per new workspace, curing W5.1 by inheritance.
8. Test-naming reconciliation → the W0.3 ADR obligation now carries the
   dated `testing-strategy.md` amendment DEFINING the browser-suite test
   class alongside the e2e system-test class (completing the directive's
   own "Use Playwright for UI E2E tests" clause; the shape is landed
   practice at the showcase and the MCP app's visual suite).
9. Creature binding → bound at its census use in W0.4 to the kit's own
   vocabulary (`whitelabel/creature/`, "Educate My Creature Too", the
   EMC² identity) — repo-verified: creature carries the rotations,
   the PDS-to-be identity carries none; the §Direction supersession note's earlier mention is
   self-binding ("the EMC²/creature identity").

### Fold accounting (the confirmed W0.5 set)

The W0.5 story carries the full dated ledger (ten items, the owner's
numbering — no item 5 — plus three instruments). Landings: item 1 →
W1.1 parity rows + W0.8(b) + W2.8 closure gate; item 2 → the W0.7
ordered-calm readability criterion + the W0.10 anchor definition + the
W0.5 ledger text (a first draft splicing the rule into the dated
2026-08-02 taste-calibration record was REVERTED at the delta-verify's
class-5 catch — dated records stay untouched); item 3 → the content-provenance manifest (W0.5
text; gates W1.2 first-light pages and W4.2 landings; identity-values
column for W2.9); item 4 → the W2.7 systematic-consistency constraint
row; item 6 → motion stances at W2.7, the creature reduced-motion defect
as a W0.2(b) dated triage entry (accessibility-class → FIX, blocking
wherever rendered), the EMC² motion pair as W2.7's demonstrating surface
and W1.4 page-set candidate; item 7 → the W2.5 worksheet must-catch case
+ the NEW W2.11 pointer story (with the §Out-of-scope boundary move
recorded in place); item 8 → W1.4's finalisation clause; item 9 → the
NEW W4.8 pointer story (post-first-light, localStorage-only); item 10 →
the W0.5 ledger text (owner draft wording recorded) + the W1.2
blocking-tweaks concretisation. Instruments: census-derived sitemap with
parity (W0.1 + the W0.3 axis statement's derived page list); page×theme
axe as a minimum (W0.3 axis statement); Lighthouse CI performance
budgets (cross-demo acceptance, new bullet; W1.1 wiring; W6.1
verification note). Riders: the W2.9 reference protocol (folly canonical
at DDR-007; two-sided Buttons proof pointer; the bounded folly pass over
the 68 audited values); the manifest identity-values column. Both new
stories land at pointer grade with story-open table rows; W1.2's
blocking-tweaks sentence names the concrete first-light-blocking subset.

## Next act

As pre-committed at the window's open: the scoped delta-verify over the
touched statements only — same-reviewer pattern (docs-adr-expert at opus)
plus adversarial class-completeness legs; stop condition inherited (never
a full round 3; residue routes to the Director). EXECUTED AND CLOSED
CLEAN — see §Delta-verify record below. The live next act is the OWNER'S
IMPLEMENTATION WORD, carded from the Director's seat, opening W0/W1
first light.

## Process notes

Mid-window rulings absorbed (2026-08-08 morning, Director broadcast
`2b4e5ce6`): Copilot reviews are the review FOUNDATION for source PRs —
on this docs-only window Copilot is requested selectively and treated as
bonus, never a merge condition; ADR-221 §3 adoption is pilot-first (the
DDR corpus is the pilot). The full round-2 finding corpus lives in the
session's workflow output (run `wf_bd16152b-ee8`, per-agent journal);
this file is the durable adjudication summary. The owner's sitting
verdicts and tweak-ledger pre-register ride PR #784 (a distinct artefact
class: sitting records, not round records — this file is deliberately
self-contained and cites that surface by PR number only). Round-1's
record: `re-review-2026-08-05.md` beside this file.

## Delta-verify record (2026-08-08, appended at the window's close)

Round 1 (ten adversarial class-completeness legs + the same-reviewer
docs-adr-expert leg at opus, run `wf_034bd28a-b2f`, 11 legs, 0 errors,
~1.70M subagent tokens, over the window's diff): 6 blocking findings under
the pre-committed disposition rule, adjudicated at the seat into 4 defect
classes — (a) a class-5 hit: the item-2 taste-anchor rule had been spliced
into the DATED 2026-08-02 taste-calibration record inside §Direction —
REVERTED (dated records stay untouched; the rule's homes are W0.10's
anchor definition, the W0.7 criterion, and the W0.5 ledger); (b) the
Lighthouse budget wiring was claimed at the W1.1 plumbing contract but
absent from it — the contract line landed; (c) round-2 finding 8's cure
was half-landed (the testing-strategy amendment missing from W0.3's
acceptance line against the list's own discipline clause) — the acceptance
line landed; (d) W0.5 item 10's census arm had no landing — the
framing-prose column joined the W0.1 page census. Plus two absorbed
sweep observations (the convention's owner-held exemption clause stated;
the record's cure-9 wording made precise).

Round 2 (scoped same-reviewer re-verify over the six re-touched sites
only): CLEAN — zero blocking findings; every pointer landing re-verified
(sole allowed non-lander: `demos/oak-plain-pages`, the planned W1.1
workspace). Slope 6 → 0. Of its six non-blocking observations, three were
absorbed with the reviewer's wording (W1.1's acceptance carries the
Lighthouse leg; `testing-strategy.md` cites carry the
`.agent/directives/` path; item 4 notes W2.9's inheritance of the
constraint row) and three are RECORDED, not absorbed: the W1.3 "rubric
evaluation results" / W0.7 "instrument-leg results" naming alias (same
referent, both defined at the mint); the "ten items" label spanning nine
numbered entries (faithful verbatim to the owner's numbering, which has
no item 5); and the W0.10 qualification of §Direction's dated
taste-anchor sentence (closing it further would require a §Direction
edit, which the rule bars). Pre-existing residues surfaced by the sweep
and ROUTED, not this window's to cure: the story-open pointer table has
no rows for W5.4/W5.5/W6.3/W6.4 (predates the window); three unedited
sites still say "frontmatter gate 3" for the W3.0 coverage card against
the renumbered two-gate array; the frozen `dispositions.v2.2.md` ledger's
EX32 row still describes the two-instrument hoist (a dated record —
superseded by this window's finding-1 cure, never retro-edited).

THE ZERO BAR IS MET. The owner's implementation word is the next gate,
carded from the Director's seat.
