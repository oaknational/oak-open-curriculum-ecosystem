# dispositions.v2.2 — the round-3 near-horizon + cross-cutting ledger

**Provenance (read before citing).** This ledger was NOT authored at the
v2.2 restructure: the 2026-08-03 cold pause froze before it (the thread
record's resume map lists it as remaining item 1; first-hand verified
absent from filesystem, all branches, and all history on 2026-08-05).
On 2026-08-05, at the owner's handover word, the v2.2 authoring seat —
Corsair hunts Surf (4d3282), convened with the previous design team —
authored it FROM LIVING MEMORY against `findings.v3.json` ids and the
landed v2.2 text (comms event `997a8970-a8da-42ef-bf60-c38f4a245baa`;
consolidated by the Director, Petrel holds Turbulence a0892f). Owner
discipline binds every reader: this is dated input to verify, not
settled truth — the SCOPED near-horizon re-review verifies it
row-by-row and runs the completeness sweep stated at the end before
anything treats a row's verdict as final.

**Verdict classes**: ABSORBED (cure as found, landed where named) /
RE-DERIVED (cure differs from the row's suggestion, ground given) /
CONSERVED (story-open input via §Story-open pointer tables) / VIA-782
(landed by the Director's 2026-08-05 truings PR, verified faithful by
the authoring seat). Ids index `findings.v3.json` arrays per the plan's
citation legend. Every far-horizon row not named below is
CONSERVED-AS-STORY-OPEN-INPUT via the plan's §Story-open pointer
tables, by construction.

## Landscape rows

- L0 ABSORBED → W2.0 acceptance (identity-№N pathway).
- L1 ABSORBED (confirm-falsifier arm) → W2.3 (theme-№N + pinned
  re-baseline).
- L2 ABSORBED ×2 → W1.4 named CSS-reordering variant + W7 (whole demo;
  table row).
- L3 CONSERVED → W3.1 table (the contradicted mechanism text was
  demoted; note W3.5 gives the hub switching WITHOUT app-shell
  prerequisites — resolve the as-is boundary there).
- L4 ABSORBED → W2 preamble explicit chain.
- L5 ABSORBED → W2 preamble + W2.4/W2.5 gate lines (rendering blocked
  on item-14 by construction).

## Frame rows

- FR0 ABSORBED → NEW W0.10 (pre-reads + per-identity direction
  sittings) + W1.5 probe variants + W0.7 per-identity rubric sections +
  the W2-preamble gate; W2.9 is priced on those anchors existing.
- FR1 ABSORBED → W0.7 three-leg pass (independent design-system-expert,
  opus, never the authoring seat; verdicts join the miss-rate record).
- FR2 ABSORBED → W0.7 cells-covered field + W1.3 + W4.7/W5.5/W6.2
  minimum cell sets + W2.4 instrument pass + W2.9 full-roster.
- FR3 ABSORBED, BOTH halves deliberately → W0.1 restricted page-row
  vocabulary AND W1.4 batched owner card (one without the other leaks
  seat decisions over the owner's verbatim "ALL pages").
- FR4 ABSORBED → §Sequencing first-pixels gate split + W1 preamble
  provisional-render + W0.8 "workspace landings, never first pixels".
- FR5 ABSORBED → W0.5 enumeration sitting + W1.2 landing clause.
- FR6 ABSORBED → W5.2 pointer.
- FR7 ABSORBED → W1.5 same-landing rejected-route replacement + W4
  preamble.

## Expert rows

- EX4 ABSORBED → W0.2(b) scoped role token (`text.link-on-decorative-5`
  in both polarity arms, THAT token pairs and re-baselines 42→43; the
  manifest-scope-field alternative is preserved as an authoring-time
  decision, not silently dropped).
- EX5 ABSORBED → W0.1 `$type` split (inferable = 100% gate; untypeable =
  recorded disposition with pinned count) + W4.4 scoping via its table
  row.
- EX13 ABSORBED → W0.2(b) post-axis-ruling sentence.
- EX20 RE-DERIVED as option (b) → W1.5 approval-scope
  (composition + grammar) + pixel parity via rule-6 baselines + a W4.2
  parity cell; REJECTED option (a) Tailwind-in-W1 because it re-couples
  first light to consumption machinery — the exact coupling FR4 cures;
  re-pricing that trade is legitimate but do it knowingly.
- EX22 ABSORBED → W0.1 class-domain by path + exclusions with pinned
  class count; the owner-card wording half rides W3.0's table row.
- EX24 ABSORBED → W1.5 control spec (native-select Switchboard REPLACED
  on probe pages; honest pre-hydration state; ≤2 activations incl.
  HC/colour-safe; exactly three identities) + the old modules named into
  W4.2's inventory; the two-axis-vs-preset decision is DEFERRED to W2.4
  open by design.
- EX28 ABSORBED → W0.3 envelope: variant admissible only if DOM ORDER
  ALONE satisfies its declared sequence; reading-flow rides inert in
  the binding cell; Playwright specifics conserved to W4.2's table row.
- EX30 ABSORBED → W0.6 route-by-route slicing (suite + fixes one PR, no
  red gate; bounded before W6.2).
- EX31 ABSORBED → W0.3 DoD shipped-page axis statement (specimen
  carrier demoted to additional leg, never substitute).
- EX32 ABSORBED → W0.3 DoD SC 1.4.10/1.4.11/1.4.12 cells + W0.8 hoist
  additions (focus-ring comparator, 320px reflow).
- EX34 ABSORBED → W0.3 APG control-pattern clause; W1.2 references it.
- EX35 CONSERVED → W2.4 table row (it also touches §Cross-demo
  acceptance's measured-half scope — flag at W2.4/W4.3 open).
- EX39 ABSORBED → W0.6 jsdom backstop named + color-contrast-disable
  retirement as acceptance.
- EX40+D16 ABSORBED, merged → W0.2(b) stated expiry DEFAULT (item-14's
  own interim rule: decorative-N roles directly; upgrade later, never
  gate) — a deliberate no-stopgap conversion; the gate's expiry date
  (2026-08-21) re-verified real 2026-08-05.
- EX45 ABSORBED → W0.1 injected-file-list boundary (bidirectional
  planted cases as literal-input unit tests; parity counts as walker
  acceptance lines) + the W0-preamble describing-surface truing.
- EX47 VIA-782 (Quality bar rule 6) + W1.3/W1.5 references landed at
  v2.2 authoring.
- EX49 ABSORBED → W0.8 HTML arm (style blocks + `style=` attrs through
  the pure `findLiteralDesignValues` classifier, red-proven) + W1.2 "by
  construction" sentence corrected.
- EX52 ABSORBED → W0.3 test-naming + `--passWithNoTests=false`; W1.1
  references; W5.1 table row VIA-782.
- EX53 ABSORBED → W0.7 mints the WOW-VERDICT register (distinct from
  the landed hub fidelity register, schema-validated on its precedent);
  Quality-bar rename VIA-782.
- EX55 ABSORBED → W0.3: envelope homed as a dated addition to
  `one-html-many-css-compositions.md` (charter cites, never restates) +
  ADR-213 §3 amendment (region contract's first binding moves to demos
  tier) + the clause's owner-fork carded.
- EX56 ABSORBED at ruling level → W3 preamble restates the Director's
  both-shapes ruling verbatim-substance; W3.0 acceptance requires the
  amendment "faithful to the ruling as given"; the decidable seed-guard
  choice (split vs drop-fixtures-guarded) is a story-open input via
  W3.0's table row.
- EX57/EX62/EX67/EX73/EX77 VIA-782, all verified faithful by the
  authoring seat 2026-08-05.
- EX58 ABSORBED → W0.3 decisions-not-mechanism restructure (mechanisms
  to plan body + DoD docs).
- EX59 ABSORBED → README §Charter pointer-only + the two authority
  sentences reconciled in the same edit.
- EX60 ABSORBED → correction set split by artefact kind (ADR + pointer
  at W0.3; workspace READMEs at W1.1/W4.2/W5.1).
- EX61 ABSORBED → ADR index entry as acceptance line.
- EX65 ABSORBED → W1.2 standing flag + Quality-bar note VIA-782
  (whether the W1 control re-faces after W2.4 is decided at W2.4 open,
  stated there).
- EX66 ABSORBED → stale-CI truing relocated to W0.6 (the first story
  landing new CI a11y suites); W2.5 keeps only its own §Standard
  additions.
- EX68 ABSORBED → correction set derived mechanically (grep the
  superseded description).
- EX69 RE-DERIVED → clause (c) gets its OWN design-tier test-support
  workspace with real exports + own Playwright deps; the suggested
  oak-eslint scripts home is unexecutable without breaking ADR-041's
  core row — ledgered as re-derived, not cured-as-suggested.
- EX70 RE-DERIVED → identity leg is MIGRATION-STATE-scoped (covers only
  the W2.0 home's layers; joins per identity at its W2.2 landing where
  its acceptance now lives; hub's authored CSS OUT under as-is, stated).
- EX75 ABSORBED → W1.1 turbo task entries + depcruise exclusion; W5.1
  table row.
- EX78 ABSORBED → W1.1 ADR-041 amendment as ROW AND COLUMN + outbound
  cells narrowed to today's real consumers (this SUPERSEDES EX73-r2's
  wider foundation/adapter enumeration — cross-referenced here).

## Dropped-at-cap rows absorbed

- D1 → Quality-bar rule-4 batching VIA-782 + W0.5 reference.
- D3 → W1 preamble intra-workstream sequencing (W5.1 → W5.2 is carried
  by the pointer's depends line).
- D4 → W0 preamble groundwork-vs-authored-scope clarification.
- D5 → W0.9 explicit acceptance incl. the FAIL path minting cure
  stories in-body.
- D6 → W0.7 enumerated calibration fixture corpus.
- D8 → W0.4 explicit acceptance contents.
- D9 → W1.5 explicit acceptance.
- D10 → W1.3 rubric-results-in-record.
- Every OTHER D row (D0, D2, D7, D11–D15, D17) is far-horizon →
  CONSERVED via its table row.

## Coverage claim, honestly bounded (the re-review's completeness check)

This ledgers every row in the near-horizon + cross-cutting extract the
authoring seat worked from on 2026-08-03 (the extract was mechanical:
section-field match on W0/W1/Direction/Goal/Quality-bar/Relationships/
frontmatter). A sweep of the full 113-row corpus against this list is
the cheap completeness check at re-review time — any row not named here
and not in a pointer table is a genuine gap in this ledger.

## Dated correction (2026-08-05, re-review round 1 — Saffron guards Hedgerow, 8a4280)

The Coverage claim's "every OTHER D row" enumeration cites **D17**, which
does not exist: `droppedAtCap_unverified` has 17 elements, valid indices
**D0–D16** (D16 is separately ledgered in the EX40+D16 row). Off-by-one in
the enumeration's tail; every genuine D row is independently accounted for
— verified by the 2026-08-05 completeness sweep (113/113 rows, zero gaps)
and the row-by-row ledger verification (57/57). Original text left as
written per the file's dated-provenance discipline.
