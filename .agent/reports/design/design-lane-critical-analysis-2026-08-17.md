# Design-lane critical analysis — 2026-08-17

Owner-invoked sitting (session `ab1066`, Yarrow stirs Undergrowth): a critical
analysis of the design lane's records, plans, and intents ahead of new design
work, run as three cognition lenses (metacognition, free-play,
concept-exploration) plus a five-leg expert review panel — assumptions,
design-system, accessibility, docs-records, and an adversarial
frame-challenger, all on opus, all instructed to verify mechanism facts
first-hand and to challenge seat-authored structure while treating owner
verbatims as governing. This record is the panel's durable home; the full leg
reports lived in the session and are compressed here without loss of any
actionable finding. Same-day sequels: seven owner card rulings (captured in
comms event `ec2c307b`, per-user memory, and the design-system-integration
thread record's 2026-08-17 entry) and the records-truth pass (MCP-613, lane
commit `6e88cb407`) which cured the record-layer findings. Disposition tags
below: **cured** (landed in MCP-613), **ruled** (settled by an owner card the
same day), **open-input** (a design input for the named future work),
**noted** (recorded, no carrier needed).

## Panel verdicts (one line each)

- **Assumptions**: the plan graph was not safe to build on — resume map
  future-tense over landed work, one execution slice self-contradictory, a
  published contract claiming a guard that does not exist. (Record layer:
  cured; sequencing: ruled.)
- **Design-system**: tranche 1 landed faithfully and its mechanism is sound —
  but the composition axis is one hardcoded seam no brand can reach, and
  below it all identities converge on the base composition, so the
  demonstration instrument was broken at narrow. (Ruled: capability +
  per-identity choice; execution open.)
- **Accessibility**: the instruments that exist are unusually rigorous but
  cover two of five live routes; the plan claimed a target-size instrument
  that does not exist; one genuinely new Level A risk found in the planned
  W3 control. (Open-inputs below.)
- **Docs-records**: 20 owed true-ups, one a false enforcement claim on the
  published white-label contract; the DDR corpus sound except one record
  with no frontmatter. (Cured.)
- **Frame-challenge**: sound record-keeping, broken at two load-bearing
  joints — the owner-verdict loop routes into an instrument that has never
  taken a row and cannot cheaply, and the kernel property the lane exists to
  prove (identity as configuration) had no instrument, no shipping
  mechanism, and no owner. (First joint: named honestly in the plan now;
  second: ruled — the identity-pack programme.)

## Findings by leg (compact; every actionable item)

### Assumptions (10 findings)

| # | Sev | Finding | Disposition |
| --- | --- | --- | --- |
| F1 | 5 | `brand.css` §1b + CHANGELOG claim a brand-admission guard that does not exist | **cured** (claim struck; admission ruled to the pack manifest) |
| F2 | 5 | Recognisability plan calls the completion sketch "that ratified plan" and routes work into it | **cured** (corrected; re-homing to the pack node named) |
| F3 | 4 | Resume map reads future-tense over W1 slice A + the W5 masthead cure (both in #846) | **cured** (dated landed-state surface + landed markers) |
| F4 | 4 | A2 rhythm specified two contradictory ways in one section; acceptance cell gated the superseded shape | **cured** (re-cut under R15) |
| F5 | 4 | Front page / token-reference route contradictions across the three 2026-08-13 plans, zero cross-references | **cured** + **ruled** (showcase-experience governs) |
| F6 | 4 | "Rides its cause" true-ups: causes landed, true-ups did not — measured zero fire rate | **cured** (reclassified; discharged; mechanism corrected to land-at-the-landing-boundary) |
| F7 | 3 | W5's "F-numbers have no locatable home" claim false — the fleet ledger landed 7h before ratification (`report.md:103-108`); four register-integrity findings uncarried | **cured** (withdrawal note; F14 closed by the register row) |
| F8 | 3 | R16 invalidates the completion sketch's W3.3 kit-owned identity runtime framing | **cured** (R16 landed; sketch stripped; runtime is demo equipment per the ruling) |
| F9 | 3 | Four spent-or-stalled nodes live in the corpus | **cured** (three archived; review-debt trued, slice 2 carried open) |
| F10 | 2 | Headline acceptance at 0/4 register rows; the only row points at a deleted page | **noted** (named honestly in the plan's instrument-ownership block; feeding the register is open) |

Assumptions recommendation not yet ruled: **sequence W2 (composition demo)
ahead of the W1 remainder** — W2 is the only tight-scope outcome with nothing
delivered and depends on no W1 output by the plan's own words. Standing as a
sequencing input for the owner's next word (the Tango programme may
supersede).

### Design-system (12 findings + the R15 gap analysis)

| # | Sev | Finding | Disposition |
| --- | --- | --- | --- |
| 1 | 5 | Composition axis entirely fixed and unreachable from configuration: one `@media (max-width: 840px)` seam (`components.css:1339`), zero container queries, `grid-template-areas` assigned directly — in no fixedness register | **ruled** (capability + per-identity choice; execution = the composition tranche, sequenced by Tango's measured needs) |
| 2 | 5 | Below 840px both counter-identities fall through to Oak's composition (neither declares narrow maps; partial override silently inherits the base) — the specimen demonstrates convergence on every phone | **open-input** (cheap immediate cure: author counter-identity narrow maps where their briefs warrant; structural cure rides the tranche) |
| 3 | 4 | The false guard claim (independent confirmation of assumptions F1) | **cured** |
| 4 | 4 | Reduced-motion collapse defeated by any brand by load order; EMC² ships 160/260ms that survive a reduced-motion request; `brand.css` self-contradicts on whether motion is brand surface | **open-input** (W6 item 3: `!important` floor on property values + re-pointed override comment; the guard arm superseded by the manifest ruling) |
| 5 | 4 | Decomposition narrowed the slot contract from any-scope to `:root`-only (subtree-scoped `--type-heading-*` branding no longer reaches the composite); CHANGELOG prices the change as purely additive | **open-input** (contract note owed at next kit touch; KNOWN-ISSUES class; iframes are the current escape) |
| 6 | 4 | EMC² ships px slot values against the contract's "Slots stay in rem" and the register's sovereignty reason | **open-input** (slice B px→rem, already queued) |
| 7 | 3 | Kit-side fixed-point register lacks revisit conditions and R15's fifth row | **open-input** (kit DECISIONS truing at next kit touch; machine-readable register proposed, not ruled) |
| 8 | 3 | `.oak-heading-light-1..3` now render LARGER than `.oak-heading-1..3` below saturation (fixed 56/48/40px vs fluid floors) — 56px display type on a 320px line survives on a kit class | **open-input** (tranche-2 pricing, register row exists) |
| 9 | 3 | DECISIONS §Ramps never took W1's dated note; `--container-max` still called a "candidate lever" though shipped | **open-input** (kit DECISIONS truing) |
| 10 | 3 | `components.css:108-113` documents four breakpoint constants none of which any rule uses; the real seam (840) absent; DDR-009's "canvas and main grids" over-claim | DDR-009 side **cured**; comment truing **open-input** |
| 11 | 3 | `--flow-columns` is a de facto brand lever not on the documented surface, never collapsed at narrow by the kit; both brands patch it themselves | **open-input** (W6 item 2, specified) |
| 12 | 2 | Smaller: `brand.css` map-scoping illustration contradicts its own rule; "accept the default single-column" is false for `unit`; `oak-flat.generated.css` stale (old fixed heading-1, absent from exports); DTCG `number` leaves lose rem-multiplier semantics downstream | **open-input** (batch at next kit touch) |

**What R15's EMC² brief cannot express today (the seven inexpressibles)** —
the gap analysis behind the narrow-range ruling, all verified first-hand:
(1) any seam but 840px, or more than two arrangements; (2) a responsive
outer shell (`--canvas-areas` has no narrow counterpart — masthead-as-rail
inexpressible); (3) a new region (eleven hardwired `[data-region]` rules);
(4) a partial map delta (whole-string maps; partial override inherits the
base silently — finding 2 live); (5) fluid composition (every track a
literal); (6) composition responding to content (zero container queries);
(7) guaranteed reordering (`reading-flow` is `@supports`-gated). Items 1-3
are DDR-001 framework-level violations: the base identity's seam, shell, and
region vocabulary are compiled where configuration cannot reach.

### Accessibility (12 findings)

| # | Sev | Finding | Disposition |
| --- | --- | --- | --- |
| F1 | 5 | `/composition` live + linked with ZERO cells; 30 triplicated `region` landmarks (10 stubs × 3 exhibits, `landmark-unique` invisible to the wcag-tagged run) | **open-input** (W2 rebuilds the page; cells land with it — R2 makes a11y on kept pages delivery) |
| F2 | 4 | The plan's "landed target-size instrument" does not exist — no size assertion anywhere in the showcase tests | **open-input** (land the instrument; must go RED first on the white-labelling frames or it is vacuous) |
| F3 | 4 | NEW Level A risk in planned W3: held-arrow key-repeat on a selection-follows-focus radio + a dark-first identity ≈ 10 whole-page polarity flips/sec — SC 2.3.1; "instant" reasoning covers motion, not luminance | **open-input** (W3 spec: coalesce applied re-skin under key-repeat, keep discrete presses instant, do NOT wrap traversal; falsifier cell specified) |
| F4 | 4 | W4's decided mechanism yields two comboboxes both named "Theme" on one route (band naming ≠ control naming; four landed resolutions go strict-mode-ambiguous) | **open-input** (W4 spec: scope-disclosing accessible names on the CONTROLS) |
| F5 | 4 | W2's reordering answer rests on `reading-flow: grid-rows` — Chromium-only, and the Playwright config is single-browser, so the fallback branch (SC 2.4.3 mismatch in Safari/Firefox) is never rendered by any cell | **open-input** (W2 evidence plan; browser-matrix question surfaced) |
| F6 | 3 | `/identity-switchboard` + `/identity-white-labelling` carry keyboard cells but no axe/forced-colours/reflow/theme coverage | **open-input** |
| F7 | 3 | Picker live region: partial-update announcer, three interleaved values, no `aria-atomic`; W3's `role="status"` mitigation as specified duplicates native radio announcements (SC 4.1.3 chattiness) | **open-input** (W3 spec: announce the CONSEQUENCE, e.g. layout recomposition, not the identity name) |
| F8 | 3 | SC 3.2.2: help text "beside" the group does not satisfy before-use advisement — must precede in DOM order + `aria-describedby` on the fieldset | **open-input** (W3 spec) |
| F9 | 3 | Plan leaves a roving-tabindex fork open — the exact `tabindex=-1`-under-reading-flow mechanism of the cured F01/F02 blackout; the kit's native `.oak-choice`/`.oak-radio` already satisfies every control-pattern clause | **open-input** (close the fork to native radio; probe the constraint if reopened) |
| F10 | 3 | White-labelling scaled frames render 48px controls at ~15 CSS px (below WCAG's own 24px floor; "equivalent" exception does not reach a different page) and the frames are not `inert` | **open-input** (route disposition owed — the plan keeps the route working-only) |
| F11 | 2 | The specimen's comment says a `.oak-skip-link` reading-order pin was routed to the kit; the kit has no such pin and its README recommends the class with no placement constraint | **open-input** (kit cure + the charter narrowing below) |
| F12 | 2 | "Identity default" under OS `prefers-contrast: more` shows high-contrast while the control reads Identity default — conformant but a comprehension trap; one-line `aria-describedby` hint | **open-input** (W4) |

**Skip-link charter disposition (winddown pickup row 5), specified:** narrow
the kit charter's unconditional "visible skip links" clause, dated, to SC
2.4.1's actual trigger (repeated focusable content before `main` across
pages) plus the two local rules — first focusable in the document; outside
`.oak-canvas` or with a kit-owned reading-order pin; target must not itself
be a reading-flow item. The specimen's in-region-target pattern generalises
as "first non-reading-flow-item descendant", not as "the headline".
Refutation kept honest: the earlier a11y review was wrong that APG forbids
selection-follows-focus radio (it is the APG default variant); it was right
about the consequence, which is F3.

### Docs-records (20-row ledger + 7 findings)

All 20 ledger rows **cured** in MCP-613 (contract claim ×2; stale showcase
README §The page, stale route note, matrix count; demos README; DDR-003
hedge; DDR-004 pointer; rubric ×3; R16; print-cell `:root`; stale
decision-log rows ×2; outcomes table; fidelity-register masthead row;
review-debt slice states; thread-record pickup row 6 noted discharged;
DDR-009 amendment; DDR-010 frontmatter). Beyond the ledger: the rubric was a
live blocking instrument contradicting R13 (**cured**, v0.1 — recalibration
OWED before any blocking verdict); DDR-010 was the corpus's one
non-conformant record (**cured** — and it would have been the ddr-graph
validator's natural red-first fixture, which remains the review-debt node's
one live question, un-carded); DDR-011/DDR-007/DDR-003 status-grades
understate their authority (**open-input**, minor); the completion sketch's
gates sat on the owner's clock (**cured** — retired). Refutations that
corrected THIS seat's own earlier statements: PDR-138's prediction line HAD
landed (2026-08-14, `659331a69`); review-debt slices 3/4 were already
discharged; the showcase README's known-red block was already rewritten.

### Frame-challenge (8 findings; 6 survived challenges)

| # | Sev | Finding | Disposition |
| --- | --- | --- | --- |
| 1 | 5 | All eight owner-held proofs route to a register that has taken zero verdicts; a checkpoint row costs three instrument legs (two opus) against a rubric with no per-identity language for two of three identities — the cost no document declared | **partially cured** (cost + unfed state now stated in the plan; feeding path open — the conversational-verdict row-class idea is unruled) |
| 2 | 5 | Three ratified plans, one surface, zero edges; one asserts a false status | **cured** + **ruled** |
| 3 | 4 | R16's verbatim survived only machine-locally (per-user memory + untracked handoff) — a rotation from a tracked home | **cured** (landed verbatim in the rulings table) |
| 4 | 4 | Resume map future-tense (independent confirmation) | **cured** |
| 5 | 4 | Identity-№N untested/unshipped/unowned: kit exports no identity; counter-identity sheets are demo-tier hand-copies in `public/`; R16's real-app shape (one statically-included identity) had no mechanism and no owner | **ruled** (the identity-pack programme: all four identities become packs, Tango first, identity-№N regression lands with the pack work) |
| 6 | 3 | Brittleness ranking against the next re-scope: hard-coded counts in acceptance cells > the coherence-table's workstream-keyed carriers > a register revisit-condition pointing at a slice in no tracked doc; the rulings table + authority classes are the robust core | **noted** (design input for the pack node's acceptance style) |
| 7 | 3 | The sketch's re-contamination surface was larger than the composition envelope — and travels through instruments (real code), which the authority-class defence does not cover | **cured** (authority stripped, instruments re-homed) |
| 8 | 3 | Plan mass is load-bearing, not bloat; what was missing was a state surface | **cured** (dated landed-state surface, ~20 lines) |

Survived challenges (the frame-challenger tried and failed to refute):
the 1,428-line plan's mass; the recorded self-corrections' value; the
pickup queue's durability on main; R16-vs-DDR-004 compatibility ("at
least" is a floor); W1's no-escalation call on the brand contract; the
`beneficial` edge to the sketch.

## The seven owner rulings (2026-08-17, this seat's cards)

1. Fourth identity commissioned, owner-named **Tango**; ALL FOUR identities
   come up to standard as identity PACKS (own package: zod manifest + brand
   CSS + assets + own licensing surface; kit gains an identity-consumption
   surface; identity-№N regression with the pack work), Tango first.
2. Tango's anchor reference is OWNER-PRIVATE: anchor materials and
   derivation notes reference-local/untracked; tracked surfaces describe
   Tango purely in its own terms.
3. Records-truth pass: full, one sitting, first (executed as MCP-613).
4. Brand admission via the pack manifest; the false contract claim struck.
5. Narrow range: capability + per-identity choice — the kit makes
   composition divergence expressible from the identity surface, sequenced
   by Tango's measured needs; a shared narrow base stays a legitimate
   per-identity choice.
6. `design-showcase-experience` governs the showcase surface; siblings
   amended with cross-references.
7. `design-system-completion`: authority stripped, knowledge conserved.

## Where the lane goes next (the standing sequence)

1. **At the GitHub all-clear** (owner hold 13:54Z 2026-08-17): push the
   MCP-613 lane branch, open its PR, push the coordination branch.
2. **The Tango identity-pack programme node** (oak-plan authoring act):
   inputs = this record's open-input corpus, the seven rulings, the
   recognisability plan's identity-parametric instruments, the conserved
   completion-sketch story tables, and the owner-private anchor materials.
   Early rendered checkpoints bind (wow-first stands); the composition
   tranche is sequenced by Tango's measured needs; W2/W3/W4 open-inputs
   above fold into whichever node builds those surfaces.
3. Standing smaller items: the small code cures (png-codec crop bound;
   check-rollup undated-tie; e2e static-root fixture race), the skip-link
   charter narrowing, the ddr-graph validator question, rubric v0.1
   recalibration before any blocking verdict.

## Lens outputs (summary; full harvest in the napkin's 2026-08-17 entries)

**Metacognition**: the estate's recurring class this sitting was again
surfaces-that-misinform-without-failing (the false guard claim; the unfed
register; the future-tense resume map) — the cure adopted is structural
(truth at the landing boundary: the dated state surface updated as the
first act of every landing). This seat's own instance: transmitting the
resume map's stance before verifying it (anchor propagation), corrected by
the panel. **Free-play** (discards visible in the napkin): the moulting
analogy matured — the records pass was a deliberate exoskeleton-shed, and
re-foundings may deserve a shed step the way folds carry rotation; the
panel→cards→execution pipeline ran end-to-end in one sitting.
**Concept-exploration**: run in full for the analysis (problem frame:
centre-of-gravity + record-truth, not delivery quality); at the freeze a
further pass is a justified no-run — every open concept is formed and
carried (the pack node is a named planning act with its input corpus here).
