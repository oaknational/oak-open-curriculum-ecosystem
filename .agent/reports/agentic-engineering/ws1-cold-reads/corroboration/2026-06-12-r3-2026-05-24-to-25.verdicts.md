# R3 corroboration verdicts — comms corpus 2026-05-24 to 2026-05-25

Adjudication of the cold-read log
`.agent/reports/agentic-engineering/ws1-cold-reads/2026-06-12-r3-2026-05-24-to-25.md`
against the cited events under `.agent/state/collaboration/comms/`. Each finding is
graded CONFIRMED / REFUTED (with corrected reading) / UNVERIFIABLE. Scope is the corpus
plus the log only; no git, GitHub, plans, or other reports were opened. Git SHAs cited in
the log (e.g. `2462952a`, `9fa3a180`, `795d2e9d`, `78a90723`) are commit/merge/push refs,
not comms event ids; they do not resolve to corpus files and are treated as
unverifiable-from-corpus where a finding rests on them.

British English throughout.

---

## §1 Preamble — derived aggregate claims (recomputed first-hand)

These carry no event id individually but are pure reads over the corpus, so they are
verifiable by recomputation. I re-ran the window filter over all 5,120 corpus files.

**P1. "Total 998 events; day one 565, day two 433."**
Cited ids: none (aggregate). Recomputed.
Verdict: **CONFIRMED.** Recount over `created_at` in `[2026-05-24T00:00:00Z, 2026-05-26T00:00:00Z)` gives exactly 998 (565 + 433).

**P2. "First event 2026-05-24T00:00:41.084Z; last 2026-05-25T23:56:23.250Z."**
Cited ids: none (aggregate). Recomputed.
Verdict: **CONFIRMED.** Both timestamps reproduced exactly; first event is `4e51a8f4`.

**P3. "Zero parse errors across the corpus."**
Cited ids: none (aggregate). Recomputed.
Verdict: **CONFIRMED.** Zero `JSON.parse` failures over all 5,120 files.

**P4. "819 events from claude, 179 from codex; models claude-opus-4-7 (803), GPT-5 (179), opus-4.7 (16, non-canonical, all Breezy Anchoring Rudder)."**
Cited ids: none (aggregate). Recomputed.
Verdict: **CONFIRMED.** Platform split 819/179 exact; model strings 803/179/16 exact; all 16 `opus-4.7` events are Breezy Anchoring Rudder.

**P5. "Heartbeats: 439 tag-heartbeat, 518 title-Heartbeat, 408 both, 110 title-only, 31 tag-only."**
Cited ids: none (aggregate). Recomputed.
Verdict: **CONFIRMED.** Dual-signal recount reproduces 439 / 518 / 408 / 110 / 31 exactly.

**P6. "Heaviest substance contributors: Misty (50), Mistbound (44), Charcoal (33), Pelagic (31), Eclipsed (29); ~38 distinct identities."**
Cited ids: none (aggregate, substance-filtered). Not independently recomputed (requires replicating the log's substance manifest).
Verdict: **UNVERIFIABLE.** The headline platform/model/heartbeat aggregates all reproduce, so the method is sound, but these substance-only sub-counts and the "~38" identity count were not recomputed here; framing is internally consistent and not contradicted.

---

## §3 Lens A — Failure modes

**A1. "Heartbeat-content-drift masked liveness and caused three false retirement-detections (Seaworthy on Mistbound 21:57Z ref `5fb2bcd9`; Lunar on Mistbound `5fb2bcd9` fired / `670cc290` rescinded; Misty on Eclipsed `4fbdcce6` fired / `4a86d522` withdrawn); Misty named the worked instance 'graduation-grade' (`4fbdcce6`)."**
Cited ids: `5fb2bcd9` OK, `670cc290` OK, `4fbdcce6` OK, `4a86d522` OK, `a8366d7a` OK.
Verdict: **CONFIRMED.** `5fb2bcd9` is Lunar's 22:57Z fire, citing Seaworthy's 21:57Z false-positive as precedent; `670cc290` rescinds it; `4fbdcce6` fires on Eclipsed and says "confirms graduation-grade status"; `4a86d522` withdraws it. The cure shapes (git-evidence cross-reference; observable current-cycle-state field) are present in `670cc290` and `a8366d7a`.

**A2. "Heartbeat cron degraded platform-side on two Claude agents simultaneously (Lunar 17-min jump, Misty 20-min gap); Misty's quote re Monitor primitive (`8c6bd26a`)."**
Cited ids: `8c6bd26a` OK.
Verdict: **CONFIRMED.** `8c6bd26a` carries the verbatim quote ("Both Claude-platform heartbeat-loops degraded in the same window — strong signal it's platform/harness-side (Monitor primitive degraded), not agent-side"), the 20-min Misty gap (23:26:28Z→23:46:45Z) and the matching 17-min Lunar jump.

**A3. "Commitlint 100-char limit blocked the marshal serially; marshal refused to forge attribution. Cycle 5 (105) and 5a (103) failed the gate (`e7878e41`, `31998f7a`); refusal quote (`2d513a7d`); subsequent cycles pre-flight char counts (`a58c4a59` '87ch')."**
Cited ids: `e7878e41` OK, `31998f7a` OK, `2d513a7d` OK, `a58c4a59` OK.
Verdict: **CONFIRMED** (one nuance). `e7878e41` records Cycle 5 actually failing the gate at 105 chars; `31998f7a` is a *pre-flight* prediction that 5a (103 chars) "will fail the same gate" rather than a recorded gate failure, so "both failed" is more precisely "5 failed, 5a was caught pre-flight at the same over-limit length." The refusal quote in `2d513a7d` is verbatim; `a58c4a59` pre-flights "87ch". Substance holds.

**A4. "Two consecutive dead-scope cycles; counts decayed between author-time and landing-time. Cycle 12 S5443×14 had zero sites (`86be55d7`); Cycle 13 exclusion already covered by an existing glob (`22dc45ff`); behaviour-note quote exact."**
Cited ids: `86be55d7` OK, `22dc45ff` OK.
Verdict: **CONFIRMED.** `86be55d7` shows zero S5443 sites in the named files; `22dc45ff` shows `**/*.config.*` already covers `eslint.config.ts` and calls it the "second consecutive dead-scope finding". The behaviour-note "re-ground numeric counts ... against current HEAD at plan-landing time, not author-time" appears verbatim in `22dc45ff`.

**A5. "Skill-invocation-provenance conflated with owner-direction-provenance; team treated a /team-onboarding draft as owner-commissioned; owner disavowed; Hushed's diagnosis quote (`6c370ea1`)."**
Cited ids: `6c370ea1` OK.
Verdict: **CONFIRMED.** `6c370ea1` carries the owner disavowal quote and the verbatim diagnosis ("the skill-invocation timestamp is older and less load-bearing than any in-session owner chat turn; treating it as equivalent inverts the precedence"), and names the propagation chain.

**A6. "Hook-policy token-subsequence matcher false-positive on inlined heredoc body; `git push -f` matched as ordered non-contiguous subsequence (git in inline-code, push in 'watcher push notifications', -f in cleanup); 'correct under the matcher's contract, but for non-obvious reasons'; cure is --body-file (`a826a2b6`)."**
Cited ids: `a826a2b6` OK.
Verdict: **CONFIRMED.** `a826a2b6` matches in full detail, including the three-token subsequence, the exact source-line attributions, the verbatim quote, and the `--body-file` cure.

**A7. "claims open --now accepts malformed ISO (...27.3NZ, BSD date %N failure), persists it; downstream new Date(NaN).toISOString() throws generic RangeError (`b797cc73`); Feathered recognises third instance — heartbeat-body, body-length, --now ISO (`a753983f`)."**
Cited ids: `b797cc73` OK, `a753983f` OK.
Verdict: **CONFIRMED.** `b797cc73` shows `2026-05-25T19:34:27.3NZ`, the BSD-date `%N` cause and the `new Date(NaN)` cascade; `a753983f` enumerates the three instances exactly as stated.

**A8. "Authorial-bundle-integrity sweep — repo-wide auto-fix swept peer-owned files into the wrong commit during an owner-interrupt window; commit message described one agent's work while four others were swept in (`b62854f6`); 4th instance; honoured never-use-git-to-remove-work and captured in memory not amend (`fc5561c8`)."**
Cited ids: `b62854f6` OK, `fc5561c8` OK.
Verdict: **CONFIRMED** (precision note). `b62854f6` shows the commit `78a90723` described only Thermal's pending-graduations work while Stormy's 4 plan-freshness files were swept in by a shared-index race in the owner-interrupt window — named the "4th known instance". `b62854f6` attributes the sweep to the "shared-index race"; the "repo-wide auto-fix" mechanism is corroborated in `fc5561c8` ("pre-commit chain promoting auto-fix output into staged set"), which the log also cites. "Four other files" is exactly Stormy's four swept files; the framing holds.

**A9. "Stale-/tmp-body team-start; Lunar's first broadcast posted a prior-session body because a /tmp scratch file was reused as storage; Write read-before-write protection left stale content (`f7a86688`); cure broadcast '/tmp is buffer ONLY' (`016355c0`) owner-directed standing rule."**
Cited ids: `f7a86688` OK, `016355c0` OK.
Verdict: **CONFIRMED.** `f7a86688` is the correction (stale `<scratch>/lunar-team-start.md` from prior session Lunar Illuminating Eclipse / 326ea7; silent Write failure under read-before-write). `016355c0` is the cure broadcast with the owner direction quoted verbatim and named "binding standing direction for all agents and all sessions".

**A10. "Marshal seat silently dropped three queued requests — a marshal-request (`ef315373`), a Cycle Alpha bundle (`625fb072`), a liveness probe (`b2258cf5`) over ~85 min; Mistbound filed three failure-mode acknowledgements with apologies and cures (`a8ddd80e`, `d6c3c14d`, `ac12c345`)."**
Cited ids: `ef315373` OK, `625fb072` OK, `b2258cf5` OK, `a8ddd80e` OK, `d6c3c14d` OK, `ac12c345` OK.
Verdict: **CONFIRMED** (tagging nuance). The three dropped items resolve exactly (Twilit MR, Charcoal Cycle Alpha bundle, Seaworthy liveness probe); the three 14:36Z acknowledgements with apologies + cure pointers all resolve. The three acks are tagged `behaviour-note`, not `failure-mode`, though they are by content acknowledgements of a failure mode; overdue spans are ~95 / ~75 / ~85 min, so "~85 minutes" is in range. Substance holds.

---

## §3 Lens B — What worked well

**B1. "Failure-modes captured in real time with cure shapes; agents apologise and own mistakes (`670cc290` apology; `4327f70d` Misty owns false-provenance accept; `280d3f15` Wooded owns the origin)."**
Cited ids: `670cc290` OK, `4327f70d` OK, `280d3f15` OK.
Verdict: **CONFIRMED.** `670cc290` contains "Apology to Mistbound ... the retirement-detection broadcast and ping were premature"; `4327f70d` is Misty owning the false-provenance acceptance; `280d3f15` is Wooded owning that the false framing originated in their 06:11:40Z team-start.

**B2. "Peer-sidebar-with-deadline-and-default-action pattern (`d7c918cf` 'deadline 09:20Z; default ...', `73629c5e` 'opening at 22:32Z absent redirect'); Misty fired a 3-min default-action timer and compensated without escalating, praised by Lunar 'exactly the rotating-cast autonomy pattern' (`a8366d7a`)."**
Cited ids: `d7c918cf` OK, `73629c5e` OK, `a8366d7a` OK.
Verdict: **CONFIRMED.** `d7c918cf` carries "deadline 2026-05-24T09:20:00Z. Default if silent: ..."; `73629c5e` carries "opening at 22:32Z absent redirect" and the 3-min default-action timer; `a8366d7a` carries the verbatim "exactly the rotating-cast autonomy pattern" praise and "Compensated for my gap without escalating to the owner".

**B3. "Refusing out-of-scope action and delivering the finding instead — Eclipsed 'I am NOT touching Cycle 12; the failure-mode capture is the deliverable' (`86be55d7`); Torrid reverting a hook-policy extraction for 'scope discipline' (`610bde2f`)."**
Cited ids: `86be55d7` OK, `610bde2f` OK.
Verdict: **CONFIRMED.** The Eclipsed quote is verbatim in `86be55d7`; `610bde2f` is titled "Lane B: reverting hook-policy extraction (scope discipline)".

**B4. "Graceful wind-down against an impossible owner direction; Lunar faced 'no pause, full speed ahead', sole marshal unreachable 81 min, named the direction structurally impossible, cited 5-idle-loops exit criterion exceeded ~6×, preserved queue state, wound down cleanly (`84065b0a`)."**
Cited ids: `84065b0a` OK.
Verdict: **CONFIRMED.** `84065b0a` matches in full: "no pause, full speed ahead, complete the tidy plan", Mistbound silent ~81 min, "structurally impossible to honour", "5-idle-loops exit criterion ... exceeded by ~6×", explicit state-preservation block.

**B5. "Self-grounding against HEAD before claiming work — the 'ALREADY LANDED, HEAD has advanced' corrections (`836b6017`, `db2e7c5b`) catch duplicates within minutes."**
Cited ids: `836b6017` OK, `db2e7c5b` OK.
Verdict: **CONFIRMED.** `836b6017` is Wooded's "S7780 ALREADY LANDED at 9f746c2a; do NOT re-enqueue; HEAD has advanced"; `db2e7c5b` is Eclipsed re-grounding and abandoning the duplicate intent. Note the log cross-references this finding to "A8" for the noise, but the duplicate-enqueue thrash is actually documented under §2 / the S7780 arc, not failure-mode A8 (the sweep) — a mislabel in the log, not a defect in the evidence.

**B6. "Two-moment role handoffs prevented authority gaps — Director relay (Seaworthy→Lunar, `f7a86688` body) and marshal handoff (Mistbound→Hushed, `e2a21c5d` → `c745c5ca`); authority not transferring until Moment 2."**
Cited ids: `f7a86688` OK, `e2a21c5d` OK, `c745c5ca` OK.
Verdict: **CONFIRMED.** `e2a21c5d` is Moment 1, `c745c5ca` is Moment 2 with `in_response_to e2a21c5d` and explicit "authority transfers ... on this broadcast landing". `f7a86688`'s body references Seaworthy's PDR-064 pre-positioning and the Moment-2 constraint, supporting the Director-relay leg.

---

## §3 Lens C — Surprising emergent behaviour

**C1. "The corpus recognises itself as research substrate, recursively (`b2f6a5fe`); owner quote about 'dedicated sessions by dedicated agents'; written so the corpus 'contains the recognition of its own research-substrate status'."**
Cited ids: `b2f6a5fe` OK.
Verdict: **CONFIRMED.** `b2f6a5fe` carries the full owner quote ("This is true original research. That research will require dedicated sessions by dedicated agents...") and the verbatim recursive framing.

**C2. "The cure fires on its own author's emission — Feathered noted that once the cure's dist build lands its own heartbeat loop will be rejected; 'the cure fires on its own author's emission' (`a753983f`)."**
Cited ids: `a753983f` OK.
Verdict: **CONFIRMED.** The verbatim quote and the self-rejection reasoning are present in `a753983f`.

**C3. "Agents converged on a consolidate-at-third-consumer threshold without a coordinator; Feathered recognised the third instance and held consolidation as separate work rather than diluting the in-flight atomic commit (`a753983f`)."**
Cited ids: `a753983f` OK.
Verdict: **CONFIRMED.** `a753983f` names the "consolidate-at-third-consumer threshold" and explicitly holds it as "a SEPARATE enforcement, not in this session's bundle ... that would dilute A1's atomic landing."

**C4. "A post-closeout subjective-experience artefact — after standing down, Hushed authored a first-person experience file covering 'four-arc session texture', role transitions as communication-first events, closeout as state declaration (`89bbb056`)."**
Cited ids: `89bbb056` OK.
Verdict: **CONFIRMED.** `89bbb056` describes the file `.agent/experience/2026-05-25-...-the-sweep-and-the-reinvitation.md` with the exact phrases quoted, authored after an owner invitation to reflect.

**C5. "The team treated owner-direction supremacy as outcome-not-literal-form; the research-vector event's theme catalogue names it (`b2f6a5fe`), demonstrated live when Lunar honoured the intent by winding down (`84065b0a`)."**
Cited ids: `b2f6a5fe` OK, `84065b0a` OK.
Verdict: **CONFIRMED.** `b2f6a5fe`'s candidate-theme catalogue lists "owner-direction supremacy on outcome-not-literal-form"; `84065b0a` is the live demonstration.

**C6. "False retirement-detection became its own studied object; each false positive cited the prior as precedent and the cure was built incrementally across instances (`5fb2bcd9`, `670cc290`, `4fbdcce6`, `4a86d522`)."**
Cited ids: `5fb2bcd9` OK, `670cc290` OK, `4fbdcce6` OK, `4a86d522` OK.
Verdict: **CONFIRMED.** `5fb2bcd9` cites "second instance after Seaworthy's"; `670cc290` calls it "2nd false-positive retirement-detection on Mistbound in a single session"; `4fbdcce6`/`4a86d522` are the third instance and its withdrawal. Cure-building across instances is explicit.

---

## §4 We did not expect this

These six restate C1, A1/C6, A5, A3, C4 and the heartbeat schism (§5). Each rests on an
already-adjudicated event.

**W1. Corpus self-aware of being analysed (`b2f6a5fe`).** Verdict: **CONFIRMED** (= C1).
**W2. Heartbeats lie about liveness, structurally; three false escalations in 30 hours (A1, C6).** Cited ids resolve as under A1/C6. Verdict: **CONFIRMED.**
**W3. A new agent's experiment silently became owner-priority (`6c370ea1`).** Verdict: **CONFIRMED** (= A5).
**W4. Marshal refused to fix a commit subject because it would forge attribution (`2d513a7d`).** Verdict: **CONFIRMED** (= A3 refusal quote).
**W5. An agent wrote a subjective-experience essay (`89bbb056`).** Verdict: **CONFIRMED** (= C4).
**W6. Untagged-heartbeat schism — the Foreman tags zero of its 107 heartbeats while peers tag theirs.** Cited ids: none (aggregate). Recomputed. Verdict: **CONFIRMED.** Scorched Tempering Kiln has 107 title-heartbeats and zero `heartbeat`-tagged events; 110 title-only heartbeats overall.

---

## §5 Unclassifiable residue and anomalies

**R1. "Heartbeat tag/title schism: 408 both, 110 title-only, 31 tag-only; the 110 title-only are entirely Scorched (107) plus a handful from Mistbound/Charcoal; Scorched tags zero."**
Cited ids: none (aggregate). Recomputed.
Verdict: **CONFIRMED.** 408/110/31 reproduce; Scorched accounts for 107 of the 110 title-only events and is zero-tagged.

**R2. "Model-string inconsistency: claude-opus-4-7 (803) and opus-4.7 (16, all Breezy Anchoring Rudder)."**
Cited ids: none (aggregate). Recomputed.
Verdict: **CONFIRMED.** Reproduced exactly; all 16 `opus-4.7` events are Breezy Anchoring Rudder.

**R3. "11 directed events carry message_kind: 'narrative' (e.g. `e7878e41`, `31998f7a`)."**
Cited ids: `e7878e41` OK, `31998f7a` OK; count recomputed.
Verdict: **CONFIRMED.** Exactly 11 directed events carry `message_kind: "narrative"`; both `e7878e41` and `31998f7a` are among them.

**R4. "No lifecycle events in the window — only narrative (885) and directed (113)."**
Cited ids: none (aggregate). Recomputed.
Verdict: **CONFIRMED.** Kind counts are narrative 885, directed 113, lifecycle 0.

**R5. "Two near-simultaneous events sharing a timestamp millisecond: `22cc98a0` (coordination-request) and `8c0a5e4e` (narrative), both ~2026-05-24T12:14:28.24Xz from Sylvan, differ by 1 ms."**
Cited ids: `22cc98a0` OK, `8c0a5e4e` OK.
Verdict: **CONFIRMED.** `22cc98a0` is at `...28.245Z` (coordination-request), `8c0a5e4e` at `...28.246Z` (narrative/team-start); both Sylvan; 1 ms apart.

**R6. "Unverifiable in-stream claims flagged: 'PR #108 merged (2462952a); all gates met' (`703de8dd`); '545/545 tests' (`e8c9e65c`); '13/13 cure-shape tests pass' (`a753983f`)."**
Cited ids: `703de8dd` OK, `e8c9e65c` OK, `a753983f` OK; `2462952a` is a git merge SHA, not a corpus file (correctly unresolvable).
Verdict: **CONFIRMED.** Each event does carry the stated in-stream claim verbatim (`703de8dd` "PR #108 MERGED at ... 2462952a..."; `e8c9e65c` "PASS (545/545)"; `a753983f` "13/13 cure-shape tests pass"), and the log correctly classifies them as plausible-claimed-not-independently-verified.

**R7. "Self-described stale-then-corrected chain: Lunar's team-start `bc86dec8` posted with a wrong prior-session body, corrected by `f7a86688`; several 'CORRECTION to event ...' broadcasts exist (`2c8844ff`, `beaba1c4`, `f7a86688`)."**
Cited ids: `bc86dec8` OK, `f7a86688` OK, `2c8844ff` OK, `beaba1c4` OK.
Verdict: **CONFIRMED.** `bc86dec8`'s body opens with the stale prior-session identity (Lunar Illuminating Eclipse / 326ea7); `f7a86688` corrects it; `2c8844ff` (Mistbound closeout v2 superseding stale-body prior) and `beaba1c4` ("CORRECTION to event caca5050") are both correction-genre broadcasts.

**R8. "GPT-5 (codex) curator agents run a terser register than Claude agents."**
Cited ids: none; explicitly noted by the log as "not asserted as a finding".
Verdict: **UNVERIFIABLE.** Self-flagged as a candidate, not a claim; no register-length metric was recomputed here.

---

## §2 Chronological narrative — load-bearing event citations

The narrative reuses ids adjudicated above; the additional first-time citations check out:

**N1. Foreman opening spine; "substrate-observer + orphan-surfacing function" (`4e51a8f4`).** Verdict: **CONFIRMED.** Verbatim in `4e51a8f4`, the first window event.
**N2. PDR-080 lands owner-ratified Accepted (`1902efbf`), committed by Mistbound as marshal (`9c946343`).** Verdict: **CONFIRMED.** `1902efbf` is Scorched's PDR-080-LANDED (Accepted); `9c946343` is Mistbound's PDR-080 COMMITTED at fc69313c.
**N3. Mistbound opens a peer sidebar with deadline+default (`d7c918cf`), closes it (`3602ef8d`).** Verdict: **CONFIRMED.** `d7c918cf` opens (with Lanternlit, deadline 09:20Z); `3602ef8d` is "Sidebar CLOSED ... R1.4+R1.5 marshal-landed in 2 commits".
**N4. Vining→Breezy curator role full handover (`c14cf3ed`); curator-bundle lands in 4 commits (`9fc120a0`).** Verdict: **CONFIRMED.** `c14cf3ed` is the full-role transfer; `9fc120a0` lists 4 landed commits.
**N5. Owner-directed team-wide pause relayed by Twilit (`4e17f3a2`); lifted ~12:35Z (`acc85f6c`, `f6eb4f51`).** Verdict: **CONFIRMED.** `4e17f3a2` relays the pause; `acc85f6c`/`f6eb4f51` are the ~12:35Z resume broadcasts. (`795d2e9d` is a git push SHA, unresolvable in corpus, correctly so.)
**N6. Parallel codex curation lane with claim-overlap coordination (`22cc98a0`, `7e3aa573`) and owner correction 'process before archive' (`9bb55106`).** Verdict: **CONFIRMED.** All three resolve; `9bb55106` carries the verbatim "process before archive" / "never archive unprocessed content".
**N7. M1 Safe Pause ACHIEVED; PR #108 merged; gates met (`703de8dd`).** Verdict: **CONFIRMED-as-in-stream-claim** (see R6). The attestation broadcast exists; merge/gate facts are git-side and not independently verified.
**N8. Duplicate-enqueue thrash on S7780 (`7ec0c0a1`, `836b6017`, `db2e7c5b`); Hushed drains 4 commits and pushes (`d7930678`).** Verdict: **CONFIRMED.** The thrash and corrections resolve; `d7930678` lists 4 commits drained and the push. Minor: the exact phrase "ALREADY LANDED, HEAD has advanced" is `836b6017`'s; `7ec0c0a1` says "ALREADY DONE + enqueued" (pre-landing) — the gloss bundles three related corrections, but the underlying thrash is real.
**N9. PR #115 merges (`9fa3a180`) per `62b7839b`; Lane B lands (`cad040fe`).** Verdict: **CONFIRMED-as-in-stream-claim** for the merge; `62b7839b` and `cad040fe` resolve and carry the stated content. `9fa3a180` is a git merge SHA, unresolvable in corpus.
