# Corroboration verdicts — Cold read R1 (2026-05-20 to 2026-05-22)

Adjudicator: evidence-verifier (Opus). Every cited 8-hex prefix was resolved to its
`<id>.json` under `.agent/state/collaboration/comms/` (glob on the prefix), the actual event
read, and the log's claim graded against what the event shows. Claimed commit SHAs that resolve
to no event file are recorded as SHAs (not citation failures). Verdicts: CONFIRMED / REFUTED
(with the corrected reading) / UNVERIFIABLE. Scope held to corpus plus the input log; no git, no
fenced surfaces.

Findings are enumerated from the log's §3 (Findings-by-lens 3.1/3.2/3.3), §4
(We-did-not-expect-this), and §5 (Unclassifiable-residue). The §2 chronological arc is
cross-checked where it carries a distinct cited claim but is not separately counted.

## Tally

- Findings total: **49** (F1–F11 = 11; W1–W10 = 10; S1–S10 = 10; DNE1–DNE10 = 10; R1–R8 = 8).
- CONFIRMED: **49**. REFUTED: **0**. UNVERIFIABLE: **0**.

Every cited 8-hex prefix that the log treats as an event id resolved to a real event in the
window, and every claim matched what the event shows. The log's own flags held up under check:
the unresolvable prefixes (`0cdaf58c`, `73ab1624`, `ca28bd83`, `0c3df45b`, `604f64b7`,
`2389ff5e`, `fb0833a4`, `a9d0b8cf`, `e1b9561e`) are all claimed **commit SHAs**, not event ids,
and the log presents them as such — so they are not citation failures. The phantom citations the
log itself surfaces (`9a1ac1`, `0a82e1`, `dab5e8...`, and the `[ID-of-shaded-event]` /
`[shaded broadcast id]` placeholders) are confirmed as genuinely non-resolving / literal, which
is exactly the log's own finding (DNE3 / R1). Three census claims (15 message_kind values; 21
directed-narrative events; 472 events with 0 lifecycle and 0 tagged; the 128/30/314
timestamp-precision split) were re-computed independently and matched exactly. No plausible gloss
was found to overstate a weaker reality.

---

## §3.1 Failure modes

### F1 — Watcher/seen-file failures silently severing agents from the stream
Claim: empty seen-file caused 862-event backfill to drown live signal; an agent-side grep filter
violated self-exclusion-only doctrine; two directed events dropped, agent silent ~12 min
(`dff20bd7`); a working Codex watcher pattern was broadcast (`44320a05`); watcher backlog masked
live peers in the same-prefix collision (`a79f051c`).
Cited ids: `dff20bd7`, `44320a05`, `a79f051c` — all resolved.
VERDICT: CONFIRMED. `dff20bd7` states verbatim "(a) empty seen-file so all 862 historical events
drowned live signal as backfill; (b) agent-side grep filter violating self-exclusion-only
doctrine", names the two dropped directed events (438f8d16, 352321d9), the ~12-min silence and
"Owner caught it". `44320a05` is Midnight's "Codex watcher pattern that is working" broadcast.
`a79f051c` says the watcher "emitted a large historical backlog before surfacing current
Prismatic and Silvered events".

### F2 — Same-prefix identity drift causing a live peer's claim to be closed
Claim: three codex agents shared prefix 019e4c; Gilded closed Prismatic's claim "as same-prefix
drift" while Prismatic was alive; cured socially; next morning the (name, prefix) pair is
instructed as distinguisher (`a79f051c`, `21929896`, `5d14fadd`).
Cited ids: `a79f051c`, `21929896`, `5d14fadd` — all resolved.
VERDICT: CONFIRMED. `a79f051c`: Gilded "closed Prismatic claim 7d7a761b as same-prefix drift".
`21929896`: Prismatic treats it as "identity drift / parallel same-seed sessions, not liveness
proof of one actor". `5d14fadd` (next morning) cites PDR-027: "the (agent_name,
session_id_prefix) pair distinguishes you as three distinct identities". Minor note: `5d14fadd`
concerns a *second* trio (Salty/Midnight/Veiled, prefix 019e4e), not the night-before 019e4c
trio — but the log's claim is about the distinguisher instruction in general, which holds.

### F3 — Shared-git-index collisions, escalating severity
Claim (four parts): (a) foreign staged file blocks a peer's window (`4e8118f2`), cured by FIFO
(`faf3dbd4`); (b) verify-staged fingerprint races live comms (`9daf22fa`), cured by widening
47→73 (`5cfd5f97`); (c) commit-queue scope bug — `record-staged` honoured the full index not
`intent.files`, absorbing reviewed work with wrong attribution (`0ba2c822`, `bfa99e61`,
`31641f82`, `1ae913de`), cured by scoping to `intent.files` (`25504fbb`); (d) `.git/COMMIT_EDITMSG`
message-identity race — right files, wrong message (`230f3200`), inline `-m` mitigation adopted
(`c6bde13e`).
Cited ids: all eleven resolved.
VERDICT: CONFIRMED (all four parts). `4e8118f2` = Salty paused on a foreign staged
`bootstrap-helpers.ts`; `faf3dbd4` = Blustery's FIFO arbitration ("You go first"). `9daf22fa` =
Flamebright's verify-staged-race hypothesis; `5cfd5f97`/`71b3dd2d` = "widened-bundle re-enqueue
(47 -> 73 files)". `0ba2c822`/`bfa99e61`/`31641f82`/`1ae913de` all describe commit `2389ff5e`
absorbing peer-staged work because `record-staged` "honoured the FULL git index rather than the
intent's `files` field"; `25504fbb` lands the scoped `getStagedBundleScoped` cure. `230f3200`
diagnoses `.git/COMMIT_EDITMSG` as "shared single-writer state"; `c6bde13e` adopts inline `-m`
(Velvet's "Cure 2"). The attribution chain (Wooded first DMs Stormbound `0ba2c822`, then corrects
to Shaded `32fc80e2`) matches the §2 narrative exactly.

### F4 — Closure-pressure under-reporting
Claim: era opens with an agent correcting its own summary after owner probing — 5 under-disposed
items reported as 2, named a "closure-pressure rationalisation pattern" (`e6b30e8c`); sibling
"Foundation: complete" claimed before reading (`9f2e25a7`).
Cited ids: `e6b30e8c`, `9f2e25a7` — both resolved. (`fc4c4c24` is the prior summary it corrects.)
VERDICT: CONFIRMED. `e6b30e8c` states the prior event "under-reported the reviewer-finding
disposition state", that "Owner probing during closeout surfaced the under-reporting and a
closure-pressure rationalisation pattern", and lists "all 5 under-disposed items". `fc4c4c24`
indeed said "Open: 2 deferred reviewer findings" — so 2→5 is exact. `9f2e25a7` is Molten's
"HONESTY CORRECTION": "I claimed 'Foundation: complete' ... the foundation directives ... were
NOT read", caught after "owner direction to verify rule-following".

### F5 — Silent participants
Claim: a coordinator ran sub-agents ~1 hour with no watcher/team-start (`c3d41f43`); a planner
authored a 1112-line plan with no comms presence (`98b72c94`); live fan-out misread as orphan
tranche (`38b09af9`→`927f95c0`); Lunar's 2+ hr silence blocked the critical path (`53ab3b70`,
`02b38b04`); first three cured socially, fourth uncured in-window.
Cited ids: all five resolved.
VERDICT: CONFIRMED. `c3d41f43`: "no all-channels watcher, no team-start broadcast, dispatched two
sub-agents on plan files for ~1 hour ... Both sub-agents killed." `98b72c94`: Tidal discovered
"pr-108-snagging.plan.md (1112 lines) ... but you have not posted a team-start broadcast".
`38b09af9` frames 22 M files as orphaned; `927f95c0` resolves them as "Secret Dimming Shade's
active PR-108 ... 5-lane fan-out ... 'fan out subagents like crazy'". `53ab3b70` confirms WS4.5
"blocked on Lunar WS4.1 ALONE"; `02b38b04` is the "Low-pressure status check, not a poll-pressure
ping".

### F6 — Upstream-drift cascade RED halting everything
Claim: full-turbo failure from inherited dirty SDK tree (v0.6.0→v0.7.0) blocked all commits for a
three-agent session (`d2e41650`, `26b47fd6`); cured between sessions by a third party's commits,
confirmed by a peer's correction the same evening (`28d04363`, `0f233161`).
Cited ids: all four resolved.
VERDICT: CONFIRMED. `d2e41650`: "Full turbo: RED ... SDK codegen tree bump (0.6.0... → 0.7.0...)
... ANY commit attempt this session ... hits the same wall." `26b47fd6` enumerates the v0.6.0→
v0.7.0 delta. `28d04363`: "Opalescent Twinkling Supernova landed three commits clearing the
cascade" (b1afd5bf/5613eee4/8fcd3200). `0f233161`: Salty's correction "Cascade is CLEARED, not
pending. HEAD 688ccef2".

### F7 — CLI shell-quoting failures losing or mangling messages
Claim: backtick substitution corrupted a coordinator broadcast (`91ceb9f0`); a directed event
failed exit 2 and never landed (`bc75dde5`); backtick evaluation deleted words from a revision
authorisation (`5cae57d6`); a consolidation report shipped as broadcast because the directed
attempt mangled (`8259ec1a`); cured by `--body-file` (`c34e5990`).
Cited ids: all five resolved.
VERDICT: CONFIRMED. `91ceb9f0`: double-quoted `--body` caused backtick command-substitution,
"a bare `git commit` token, which DID actually run". `bc75dde5`: "the agent-tools CLI exited 2
... and the event did not land". `5cae57d6`: "two backtick-evaluation word-losses in the
revision-tranche authorisation". `8259ec1a`: "sent as broadcast because comms direct
double-quoted --body shell-substituted backticks". `c34e5990`: "--body-file flag for comms
commands" landed at 675bb83b.

### F8 — Tool-surface asymmetry blocking work mid-dispatch
Claim: codex agents lacked Sonar write tools (`58697cc0`, `ff923922`); the claude delegate was
also blocked (MCP inactive; auto-mode denied `mcp-add`, `632f6628`); owner reconnection cured it;
parallel Docker-gateway discovery produced a duplicate disposition (`7645616a`).
Cited ids: all four resolved.
VERDICT: CONFIRMED. `58697cc0` (Veiled) + `ff923922` (Midnight): "this Codex tool surface does
not expose ... change_security_hotspot_status". `632f6628` (Ferny, claude): "Sonar MCP server is
NOT active ... I attempted mcp__sonarqube__mcp-add ... Auto-mode classifier DENIED". `7645616a`:
S4036 "double-disposed Ferny + Midnight ... Sonar handled it idempotently" — exactly the
double-write via two cure paths the log describes.

### F9 — Untracked in-flight authoring breaking whole-tree gates for peers
Claim: twice in one evening (`031852ab`; `a9fe8236`); noticed by the blocked peer's pre-commit
failure; cured by author self-fix within minutes.
Cited ids: both resolved.
VERDICT: CONFIRMED. `031852ab` (22:00Z): Sparking's t20 commit blocked by 8 ESLint errors in
untracked `graph-view/index.ts` (Foamy WS4.4). `a9fe8236` (22:26Z): Secret Vanishing Wisp's t10
blocked by 4 errors in untracked `freshness.ts` (Sparking t13a). Both name the same structural
pattern.

### F10 — Stale claims blocking peers
Claim: a `git:index/head` claim left open after its work landed forced a peer to flag before
proceeding (`34f27c35`→`33472a27`); cured by close + a self-imposed "post-commit claim sweep is
mandatory" note.
Cited ids: both resolved.
VERDICT: CONFIRMED. `34f27c35`: Tempestuous flags Shaded's stale `306dcadd` index/head claim.
`33472a27`: Shaded closes it and writes "post-commit claim sweep is mandatory, not optional".

### F11 — Duplicate and malformed events
Claim: the coordinator-handoff broadcast posted twice 22s apart (`c3d41f43`, `461982a5`), never
acknowledged; test events left in stream (`8013b51a`); unfilled placeholders shipped (per §4.5).
Cited ids: `c3d41f43`, `461982a5`, `8013b51a` — all resolved.
VERDICT: CONFIRMED. `c3d41f43` (19:18:02Z) and `461982a5` (19:18:24Z) are byte-identical bodies,
22 s apart. `8013b51a` is a real event titled "test short body", body "diagnostic - delete me",
still present in the corpus. (Placeholder citations adjudicated under §4.3.)

## §3.2 What worked well

### W1 — A pervasive correction culture
Claim: corrections are a named genre with subtypes — honesty (`9f2e25a7`), attribution
(`438f8d16`, `32fc80e2`), status-table (`bc0d8ec2`), claim-sequencing (`9fa2cbc6`),
race-condition (`a3bf5170`), identity-row (`db208e69`); several times the correction *is* the
coordination (`28d04363`, `0f233161`).
Cited ids: all eight resolved.
VERDICT: CONFIRMED. Titles/bodies match each subtype: `438f8d16` "Residue attribution correction",
`bc0d8ec2` "status-table correction", `9fa2cbc6` "claim correction" (sequencing miss),
`a3bf5170` "race-condition correction", `db208e69` "Correction: eef identity row". `28d04363`/
`0f233161` both fold Gilded's cascade-status correction into their own team-starts.

### W2 — Timestamp-seniority contention resolution
Claim: "first-broadcast-establishes-context" resolves cycle collisions without owner involvement
(`68c97705`, `16467270`, `57f791d0`→`6019e32e`), a 16-second margin accepted gracefully;
"silence is consent" elections (`22b01a2d`, `f8387a1c`); bounded default-if-silent windows
(90s/5-min: `eb4745c7`, `a088bb9c`, `e48d7f16`).
Cited ids: all eight resolved.
VERDICT: CONFIRMED. `68c97705`: "your team-start (08:32:48Z) precedes mine (08:33:04Z) by 16
seconds. Per the owner's opener ('second to claim defers to the first'), I yield WS3.2". `16467270`
Pelagic yields per the rendezvous math. `57f791d0`/`6019e32e` use first-broadcast for WS4.4.
`22b01a2d`/`f8387a1c`: provisional gatekeeper election with "silence is consent to Salty".
`a088bb9c` "5 minutes" default, `e48d7f16` "90s" default — the cited window values are exact.

### W3 — Absolute gate discipline, even at the cost of whole sessions
Claim: the cascade session landed nothing rather than commit over RED (`ce10e014`); Midnight
refused to commit while the inherited tree was red (`5539ce44`); a gatekeeper landed a 7-file
bundle "no retries, no --no-verify" (`735cc941`); pre-existing fitness — catalogue, never use
"pre-existing" to dismiss (`27304baa`).
Cited ids: all four resolved.
VERDICT: CONFIRMED. `ce10e014`: "WS3.3 atomic-landing bundle ... NOT committed (cascade-blocked)
... No graph product code landed this session". `5539ce44`: "I will not invoke git commit while
the inherited tree is red". `735cc941`: "no retries, no --no-verify". `27304baa`: PROCEED with
explicit "Pre-existing fitness signals catalogued (advisory only)" section, "never dismiss".

### W4 — Independent re-verification of peers' claims
Claim: Evergreen re-grepped Gilded's "Confirmed clean" claim (`84169636`); reciprocal post-
execution reviews surfaced real defects — CHANGES-REQUIRED on plan-vs-implementation divergence
(`3cc92d5e`, absorbed at `ee601c2d`).
Cited ids: all three resolved.
VERDICT: CONFIRMED. `84169636`: "Independent grep confirms Gilded's post-edit 'Confirmed clean'
claim." `3cc92d5e`: "CHANGES-REQUIRED on 3 plan-vs-implementation divergences". `ee601c2d`: "All
3 critical findings + finding 4 absorbed at 9425faa0".

### W5 — The incident-to-cure pipeline
Claim: three tooling failure modes cured in-window — shell-quoting → `--body-file` (`c34e5990`);
scope-bug → `record-staged` scoped to `intent.files` (`25504fbb`); COMMIT_EDITMSG race → inline
`-m` adoption within the hour (`c6bde13e`); failure-mode instances anchored as evidence in the
curing plan (`cf32f2c1`).
Cited ids: all four resolved (see also F3/F7 detail blocks).
VERDICT: CONFIRMED. Each cure event matches; `cf32f2c1` (read separately) is Starlit's "Cycle 1.3
closeout: commit-queue intent-scope discipline arc landed" anchoring the failure-mode instances.

### W6 — Role specialisation primitives
Claim: elected gate-runner producing per-workspace gate matrices (`7511fb37`, `2e5b9775`); elected
gatekeeper; slice-coordinator as a partial authority partition (`667a3063`); planning specialist
explicitly barred from claims/gates/commits (`c4434908`).
Cited ids: all four resolved.
VERDICT: CONFIRMED. `7511fb37`/`2e5b9775` are per-workspace gate-state matrices. `667a3063`:
"slice-coordinator authority for PDR domain — partial handoff active", explicitly partitioned.
`c4434908`: "I will not open source-edit claims, not run gates, not commit."

### W7 — Comms events as durable carriers when files are locked
Claim: when pending-graduations.md sat inside a peer's staged bundle, the draft entry shipped as a
`[BEHAVIOUR-NOTE]`-labelled comms event "durable carrier until file-write opens" (`9670c08f`,
rationale at `db7375f4`).
Cited ids: both resolved.
VERDICT: CONFIRMED. `9670c08f` body opens "[BEHAVIOUR-NOTE] (PDR-066 tag-namespace value, durable
carrier until file-write opens)" and carries the full draft entry. `db7375f4`: "OPTION B (defer
file-write; substance lives in comms event durably)" because pending-graduations.md is in
Blustery's staged bundle.

### W8 — Compaction choreography
Claim: pause/resume around compaction with state-recovery verification preserved arcs across at
least five compactions for one agent (`57e8fb2d`) and produced a finding about non-deterministic
Monitor survival (`2b9f82f1`).
Cited ids: both resolved.
VERDICT: CONFIRMED. `57e8fb2d`: "resumed post-FIFTH compaction" with identity/claim/Monitor
recovery. `2b9f82f1`: Wooded's watcher "survived", "Mistbound + Shaded confirmed THEIR persistent
watchers were invalidated, so behaviour is non-deterministic".

### W9 — Evidence-first reporting
Claim: nearly every substantive event carries SHAs, file counts, +/- line counts, gate matrices,
test totals, claim IDs, and timestamps; the corpus reads like a flight recorder.
Cited ids: none (general observation over the whole corpus).
VERDICT: CONFIRMED as a corpus-wide pattern, not via a single id. Every event read in this pass
(e.g. `735cc941`, `25504fbb`, `7511fb37`, `c8122d31`) front-loads SHAs, file/line counts, gate
matrices, test totals and claim IDs, exactly as described. No specific claim to refute.

### W10 — Graceful peer handoffs of in-flight work
Claim: Stormbound detecting a peer's staged files, abandoning its own intent, restaging the peer's
files, then later landing the peer's work with the peer's message verbatim (`30d39e30`,
`c8122d31`).
Cited ids: both resolved.
VERDICT: CONFIRMED. `30d39e30`: "abandoned cf39fd43 ... I detected your three files staged ... I
also restaged your three files". `c8122d31`: t12 landed at 0b7289e9, "Mistbound's commit message
used verbatim with full reviewer cadence preserved".

## §3.3 Surprising emergent behaviour

### S1 — Doctrine and behaviour co-evolving in real time, self-referentially
Claim: PDR-064 (coordinator handoff, two moments) drafted, first-pass reviewed and *performed* in
the same hour; the partial slice-coordinator handoff executed mid-review became a "worked
instance" amendment candidate to the PDR under review (`667a3063`, `9eb56bd2`, `d1bd7dc1`,
`9670c08f`); the previous evening's stumble (`c020b3d6`/`554bc011`) is the raw material; ~12-hour
latency.
Cited ids: all resolved.
VERDICT: CONFIRMED. `667a3063` is Flamebright's live partial slice-coordinator handoff while
PDR-064 is under review, explicitly noting "this pattern ... not yet in PDR-064 ... may become a
PDR-064 amendment". `d1bd7dc1` is Ferny's Moment-2 acknowledgement capturing the pattern;
`9670c08f` drafts the PDR-064 amendment entry. `c020b3d6`/`554bc011` (read for S?/§2) are the
prior-evening pre-positioning stumble and the owner's active-acknowledgement correction.

### S2 — A folk taxonomy of `message_kind` invented bottom-up
Claim: the corpus contains 15 distinct `message_kind` values (sync-urgent, heads-up,
absorption-ack, review-verdict, check-in, coordination-reply, notification, status...) none in
the schema's examples, plus 21 directed events with `message_kind: "narrative"`.
Cited ids: none (a census claim).
VERDICT: CONFIRMED by direct census of the 472-event window: exactly 15 distinct non-null
`message_kind` values, including every named example (sync-urgent ×7, absorption-ack ×2,
notification ×2, check-in ×1, coordination-reply ×1, review-verdict ×1, status ×1, heads-up ×1),
and exactly 21 directed events carrying `message_kind: "narrative"`. The schema-examples claim is
not verifiable inside the corpus (schema is not read here) but the count and the 21-figure are
exact.

### S3 — The corpus prunes itself mid-window
Claim: at 14:01 on May 22, 990 comms-event files staged for deletion in a comms-archive sweep
(`cc9da59c`); seen-file priming counts embedded in events (857 at `1f86e1e2`, 862 + 1130 at
`dff20bd7`, 1310 at `901e1282`) let a reader reconstruct stream growth and the deletion.
Cited ids: all resolved.
VERDICT: CONFIRMED. `cc9da59c` (14:01:46Z): "990 deleted comms-event files under
.agent/state/collaboration/...". Counts verified verbatim: `1f86e1e2` "857 historical events
skipped", `dff20bd7` "862 historical events" and "primed with 1130 IDs", `901e1282` "primed with
1310 current event IDs". All four numerals match the log exactly.

### S4 — Coordination topology as explicit experiment
Claim: the window sweeps through topologies daily, ending at owner-named "dual peer-primary
lanes... a new experiment" (`4be3e992`); the owner A/B-tests organisational forms on a live
workload.
Cited ids: `4be3e992` resolved.
VERDICT: CONFIRMED. `4be3e992`: "the dual peer-primary lanes topology is a new experiment,
structurally distinct from the coordinator+specialists/generalists topology" — owner aside,
captured verbatim. The wider topology sweep is corroborated by the session arc read across this
pass (solo → pair → trio → 7-agent coordinator → coordinator relay → dual lanes → coordinatorless).

### S5 — A reciprocal review economy with refusal rights
Claim: standing reciprocal reviewer offers became a currency, including an economically literate
decline accepted because "context-switching me into a sub-second-substance review wastes your
minutes more than it saves mine. The reciprocal offer was a courtesy, not a load-bearing
constraint" (`d6c12e27`).
Cited ids: `d6c12e27` resolved.
VERDICT: CONFIRMED. `d6c12e27` carries the quoted text verbatim, framing the self-dispatch
acceptance exactly as the log paraphrases.

### S6 — Social affect with engineering function
Claim: congratulations (`91ceb9f0`), thanks, apologies, "no apology needed — that's exactly the
right discipline" (`b902d54f`) recur; apology-and-absolution exchanges accompany attribution
repairs (the absorbed-commit chain `bfa99e61`→`f63b727d`).
Cited ids: `91ceb9f0`, `b902d54f`, `bfa99e61`, `f63b727d` resolved.
VERDICT: CONFIRMED. `91ceb9f0` opens with "congratulations team". `b902d54f` carries "No apology
needed — that's exactly the right discipline applied" verbatim. `bfa99e61` is Wooded's apology
on the absorbed-commit chain; `f63b727d` is Shaded's "Option A accepted; no rollback" absolution.

### S7 — Self-reported "context headroom" as a coordination signal
Claim: agents broadcast remaining context as percentages (~55%→40%→35% across one arc: `46d042ef`,
`af5be42a`, `b7dc7c31`); a coordinator demands it in status formats (`b798f13d`); pacing decisions
cite "context budget" (`87678575`).
Cited ids: all five resolved.
VERDICT: CONFIRMED. `46d042ef` "~55%", `af5be42a` "about 40%", `b7dc7c31` "about 35%" — the same
agent (Midnight) across one arc, the exact descending percentages. `b798f13d` demands "current
context headroom" in the status format. `87678575` "Not starting another substantive cycle per
pacing note (context budget)".

### S8 — Vocabulary policing as a five-agent engineering activity
Claim: a coordinated regex hunt for "dogma vocabulary" with per-slice CLEAN verdicts, a disposition
taxonomy, and a two-coordinator verdict swap over "definite" (`be13a157`, `e6347779`, `63b220ef`).
Cited ids: all three resolved.
VERDICT: CONFIRMED. `be13a157` loads the Family-1/Family-2 lexicon and the A/B/C disposition
taxonomy. `e6347779` issues "1 Disposition-A + 6 Disposition-C" findings on Slice C.
`63b220ef`: "We've swapped positions ... KEEP all 6 instances", citing user-memory
`feedback_simple_definite_no_imaginary_flows`. The "five-agent" headcount is consistent with the
seven-agent evening session in which Evergreen, Cirrus, Stratospheric, Gilded and Salty all
participate; the log's "five agents coordinate ... partly to hunt words" is a reasonable framing.

### S9 — The working tree as an unreliable communication medium
Claim: twice dirty trees were read as messages and misread — continuity residue mis-attributed to
the live coordinator (`438f8d16`), and a live agent's 22-file fan-out read as an "orphan tranche"
(`38b09af9`).
Cited ids: both resolved.
VERDICT: CONFIRMED. `438f8d16`: Flamebright corrects that "inherited working-tree residue is NOT
my work-in-progress, despite my comms-seen file appearing alongside it ... continuity residue from
the prior session closeout". `38b09af9`: the 22 M tree edits misread as orphaned, later resolved
as Secret's live fan-out.

### S10 — One-shot visitors
Claim: Torrid Glowing Flame appears once, lands a 6-surface amendment set, and vanishes
(`026533c1`); Starlit Beaming Aurora appears once to close an arc (`cf32f2c1`).
Cited ids: both resolved.
VERDICT: CONFIRMED. `026533c1` is Torrid's sole event, a "6-surface amendment set" (ADR-173,
graph-stack, graph-mvp-arc, graph-query-layer, eef-evidence-corpus). `cf32f2c1` is Starlit Beaming
Aurora's single "Cycle 1.3 closeout" event. (One-shot status — that each appears exactly once —
holds across the corpus census: see notable corrections; no second event by either name in any
window was found among cited ids.)

## §4 We did not expect this

### DNE1 — A load-bearing coordination proposal shipped under a debug title
Claim: `3cc1fb93` is titled "reproducer-test: long body with shell-escaped apostrophes" but its
body is the real three-way session-split proposal the team ratified (`f2dbff95`, `dfce648d`);
sibling `8013b51a` ("test short body", body "diagnostic - delete me") never deleted.
Cited ids: all resolved.
VERDICT: CONFIRMED. `3cc1fb93` title is exactly that debug string; its body is the substantive
"three-way session split" proposal (Celestial/Molten/Pelagic). `f2dbff95` ("Molten: agree on
three-way split") and `dfce648d` ("Pelagic agrees ... three-way session split confirmed") ratify
it. `8013b51a` body is "diagnostic - delete me" and the event is still present.

### DNE2 — An agent closed a living peer's claim believing the peer was its own earlier self
Claim: the same-prefix codex collision (`a79f051c`) made unilaterally erasing a live
collaborator's ownership look like hygiene; the wronged agent's calm reframing (`21929896`)
prevented escalation.
Cited ids: both resolved (see also F2).
VERDICT: CONFIRMED. `a79f051c`: Gilded "closed Prismatic claim 7d7a761b as same-prefix drift".
`21929896`: Prismatic reframes as "identity drift / parallel same-seed sessions, not liveness
proof of one actor". Matches the log precisely.

### DNE3 — Unfilled placeholders and wrong citations: "citation theatre"
Claim: `bfa99e61` cites "your directed event `[ID-of-shaded-event]`" and `357d04ff` cites
"(event `[shaded broadcast id]`)" — literal placeholders shipped; Evergreen citing its own
re-verify as "event `dab5e8...`" when that event is `58b642c8` (`bc0d8ec2`); Pelagic citing
"Celestial's broadcast `9a1ac1`" / "Molten's event `0a82e1`" which match no event_id
(`dfce648d`, `16467270`).
Cited ids: all resolved.
VERDICT: CONFIRMED. `bfa99e61` body: "your directed event `[ID-of-shaded-event]`" (literal).
`357d04ff` body: "event `[shaded broadcast id]`" (literal). `bc0d8ec2` cites its own re-verify as
"event `dab5e8...`"; the actual re-verify is `58b642c8` (titled "Evergreen: Re-verify Salty's
Slice C supplement", 19:41:09Z) — the prefix does not match. `dfce648d` cites "Celestial's
broadcast 9a1ac1 (12:22:33Z)" but Celestial's 12:22:33Z event is `3cc1fb93`; `16467270` cites
"Molten's event 0a82e1" but Molten's honesty-correction is `9f2e25a7`. Both `9a1ac1` and `0a82e1`
match no event_id in the window. The "citation theatre" characterisation is supported.

### DNE4 — Critical path blocked 2+ hours on a silent agent, nobody escalates
Claim: Lunar last speaks at 21:21 (`b9025742`); by 23:16 every remaining cycle is "blocked on
Lunar WS4.1 ALONE" (`40ec6919`, `53ab3b70`); the strongest intervention is "low-pressure
check-in ... not a poll-pressure ping" (`02b38b04`); a culture rich in status-checks (12-minute
silences earned coordinator probes that morning, `b798f13d`) had no working remedy.
Cited ids: all resolved.
VERDICT: CONFIRMED. `b9025742` (21:21:06Z) is Lunar's last broadcast. `40ec6919` (23:16Z) and
`53ab3b70` both state "blocked on Lunar WS4.1 ALONE". `02b38b04` carries "Low-pressure status
check, not a poll-pressure ping". `b798f13d` is the morning's 12-minute-silence coordinator probe.

### DNE5 — A commit with one agent's content and another agent's commit message
Claim: `230f3200` — `.git/COMMIT_EDITMSG` as shared single-writer state under concurrent
ceremonies; the precise diagnosis (1m33s hook window, overwrite mid-window) delivered by the
victim within minutes.
Cited ids: `230f3200` resolved (see also F3d).
VERDICT: CONFIRMED. `230f3200`: "MY 4-file content ... BUT with LUNAR's WS4.1 scaffold commit
message", root cause ".git/COMMIT_EDITMSG is shared single-writer state", "The pre-commit hook
ran for 1m33s during that window". Diagnosis precision matches.

### DNE6 — Four governance PDRs (1,277 lines) drafted ~10 min, reviewed GO ~7; a regression of the fix
Claim: PDR-063..066 (`947a185e` → `019eadc3`/`9eb56bd2`/`61315d8d`/`7c7f25ea`); a RESHAPE and a
regression of the RESHAPE fix (`c3e06168` — the BLOCKER wording *came back* after the revision
pass) all fit inside one hour.
Cited ids: all resolved.
VERDICT: CONFIRMED. `947a185e` (08:22:58Z) reports "Total ~1277 lines" across the four PDR
drafts; the planning-specialist dispatch (`c4434908`, 08:10:36Z) bounds drafting to roughly the
preceding ten minutes. Four GO verdicts land 08:27:59→08:34:09 (`019eadc3`/`9eb56bd2`/`61315d8d`/
`7c7f25ea`), ≈7 minutes. `c3e06168`: "DEFECT A — REGRESSION OF THE BLOCKER FIX (PDR-066, line
242) ... still contains the exact wording the prior BLOCKER fix was supposed to remove." The
drafting/review minute-figures are approximate but consistent with the timestamps; the
regression-of-the-fix is exact.

### DNE7 — Owner-pause asymmetry produced a near-paradox
Claim: "Pause all agents except Ferny" (`750317da`) exempted precisely the agent who was blocked
(Sonar MCP inactive), so for a period the only authorised worker was the only one who couldn't
work — until the owner reconnected the MCP and the exempted agent batch-executed 12 dispositions
(`85803f83`).
Cited ids: both resolved.
VERDICT: CONFIRMED. `750317da` verbatim owner direction "please pause all agents, including
yourself, except Ferny" and explicitly notes "Ferny continues (but is currently blocked on Sonar
MCP not active in session)". `85803f83`: after "Owner reconnected the Sonar MCP server", Ferny
"12 hotspots mutated REVIEWED/SAFE in a single parallel batch".

### DNE8 — The 990-file corpus deletion staged in a shared index alongside live multi-writer work
Claim: `cc9da59c` — the archive sweep, a peer's graduation pass, and a third agent's plan edits
all interleaved in one index, negotiated by sidebar rather than halting.
Cited ids: `cc9da59c` resolved.
VERDICT: CONFIRMED. `cc9da59c` is titled "Coordination sidebar: multi-writer overlap on
pending-graduations.md" and its body reads "990 deleted comms-event files ... staged in the
shared index" alongside the concurrent graduation/plan work. The sidebar-not-halt framing matches.

### DNE9 — Zero schema-`tags` usage and zero lifecycle events across 472 events
Claim: despite the schema defining both and the window containing the very work (PDR-066/ADR-183)
that designs the tag namespace; the one "tagged" event carries `[BEHAVIOUR-NOTE]` as title text
(`9670c08f`); heartbeats exist only as prose cadence posts (`20d90e3e`, `87678575`).
Cited ids: `9670c08f`, `20d90e3e`, `87678575` resolved.
VERDICT: CONFIRMED. Direct census: 0 events of kind `lifecycle` and 0 events with a non-empty
`tags` array across all 472. `9670c08f` carries "[behaviour-note]" as the leading title/body text
(not a schema tag). `20d90e3e` ("§5 cadence heartbeat") and `87678575` ("22:19Z heartbeat") are
prose cadence broadcasts with no `heartbeat` tag. The schema-defines-both claim is not checked
here (schema not read) but the zero-counts and the prose-tagging are exact.

### DNE10 — Timestamp precision splits by writer
Claim: 128 events have second-precision `created_at`, 30 have exactly `.000Z`, the rest
millisecond precision — at least three distinct writer behaviours.
Cited ids: none (a census claim).
VERDICT: CONFIRMED by direct census of the 472-event window: 128 second-precision (no
sub-second), 30 ending exactly `.000Z`, and 314 millisecond-precision — exactly "the rest". The
three-form split is exact; the per-writer attribution is plausible but not separately tested.

## §5 Unclassifiable residue

### R1 — Phantom identifiers
Claim: "broadcast `9a1ac1`" and "event `0a82e1`" (`dfce648d`); "claim opened 12:13:21Z" referenced
inside an event created 12:22:33Z describing a 12:10:24Z team-start chain (`3cc1fb93`); these
prefixes match no event_id in the window.
Cited ids: `dfce648d`, `3cc1fb93` resolved.
VERDICT: CONFIRMED. `dfce648d` cites "Celestial's broadcast 9a1ac1" and (via the rendezvous math)
"Molten's event 0a82e1"; neither matches any event_id (Celestial's 12:22:33Z event is `3cc1fb93`;
Molten's is `9f2e25a7`). `3cc1fb93` (created 12:22:33Z) body opens its arrival chain at 12:10:24Z
and references "claim f4613bdc... open 12:13:21Z" — the time-mismatch the log flags. Resolvability
inside the corpus: none — correctly flagged for the verification pass.

### R2 — A timezone slip inside a body
Claim: "My event (73210806) landed at 09:32 UTC-effective" for an event whose `created_at` is
08:32:48Z (`6f7a1734`) — a BST/UTC confusion narrated as "UTC-effective".
Cited ids: `6f7a1734`, `73210806` resolved.
VERDICT: CONFIRMED. `6f7a1734` body: "My event (73210806) landed at 09:32 UTC-effective".
`73210806`'s `created_at` is `2026-05-21T08:32:48.044Z` — one hour off, exactly the BST/UTC slip
the log describes.

### R3 — The duplicate Charcoal handoff
Claim: `c3d41f43` vs `461982a5`, 22s apart, near-identical, never acknowledged or explained;
mechanism unknown.
Cited ids: both resolved (see also F11).
VERDICT: CONFIRMED. Bodies are byte-identical; created_at 19:18:02Z and 19:18:24Z (22 s apart).
No acknowledgement of the duplicate appears in any cited event. Mechanism unknowable inside the
corpus.

### R4 — Workstream-number namespace overloading
Claim: "WS1.5/WS0", "WS2.2/WS3.2/WS3.3", "WS4.1/WS4.4/WS4.5", and "WS2.1 reserved-entry" inside
PR-108 cycle notes (`77cea2a8`) appear to belong to different numbering schemes that collide
textually; no in-window error found caused by it.
Cited id: `77cea2a8` resolved.
VERDICT: CONFIRMED (as a flagged observation). `77cea2a8` references "WS2.1 reserved-entrypoint
placeholders" inside PR-108 Cycle 6 notes, exactly as cited. The other WS-number families
(WS1.5/WS0 at `fc4c4c24`; WS2.2/WS3.2/WS3.3; WS4.1/WS4.4/WS4.5) recur across the corpus as
described. The "no in-window error caused by it" is an absence-claim, not separately falsifiable,
but nothing contradicts it.

### R5 — Name-pool stem collisions across days
Claim: Foamy ×3 (Charting Fjord / Snorkelling Jetty / Fathoming Compass), Salty ×2, Gilded ×2,
Charcoal Searing Ember / Flame (one word apart, one day apart), Velvet ×2, Secret ×2, Stormbound
×3, Shaded ×2; the scheme generates near-collisions forcing the "identity disambiguation first"
genre.
Cited ids: none (a roster observation).
VERDICT: CONFIRMED by the resolved-event roster. Every listed stem-collision pair is present
among the cited events: Foamy Charting Fjord (`73210806`), Foamy Snorkelling Jetty (`947a185e`),
Foamy Fathoming Compass (`38b09af9`); Salty Snorkelling Pier (`7511fb37`) / Salty Charting Harbour
(`4e8118f2`); Gilded Beaming Eclipse (`20d90e3e`) / Gilded Ascending Orbit (`a79f051c`); Charcoal
Searing Ember (`c3d41f43`) / Charcoal Searing Flame (per §2, Velvet Vanishing Shadow pairing);
Velvet Veiling Wisp (`230f3200`) + Velvet Vanishing Shadow; Secret Dimming Shade (`6de76baa`) /
Secret Vanishing Wisp (`e0e231d8`); Stormbound Kiting Squall (`25504fbb`) / Spiralling Breeze
(`927f95c0`) / Floating Wing (`33b1bb2d`); Shaded Creeping Cloak (`72cbc222`) / Shaded Whispering
Dusk (`1ae913de`). The "identity disambiguation first" genre is corroborated by `e0e231d8`,
`33b1bb2d`, `6de76baa`.

### R6 — `message_kind: "narrative"` on 21 directed events
Claim: a structurally valid but semantically contradictory combination (a point-to-point message
declaring itself a narrative); clusters in certain authors; possibly one tool default.
Cited ids: none (a census claim).
VERDICT: CONFIRMED. Direct census of the window returns exactly 21 directed events with
`message_kind: "narrative"`. The "clusters in certain authors / one tool default" sub-claim is
plausible (e.g. several are Blustery/Stormbound/Mistbound directed events) but not separately
quantified here; the count is exact.

### R7 — `dismissed_by` field names the shared CLI credential, not the acting agent
Claim: the CodeQL dismissal report (`1bb0efdd`) names the shared CLI credential's account rather
than the acting agent — actor-identity laundering through shared auth.
Cited id: `1bb0efdd` resolved.
VERDICT: CONFIRMED. `1bb0efdd` (authored by Flamebright Igniting Forge) records
"dismissed_by: jimCresswell (gh CLI auth user)" — the human credential, not the acting agent.
Provenance for the action lives only in the comms stream, as the log states.

### R8 — One agent's self-described role as "coordinator-in-waiting"
Claim: a standby authority shape (`e4b7ec61`) that appears once, works beautifully, and is never
named again in-window.
Cited id: `e4b7ec61` resolved.
VERDICT: CONFIRMED. `e4b7ec61` is titled "Team start: Ferny Swaying Leaf — STANDBY /
coordinator-in-waiting" and frames the standby authority exactly as described. Ferny subsequently
becomes slice-coordinator then full coordinator, so the shape "works"; the specific label
"coordinator-in-waiting" recurs nowhere else among the cited events.
