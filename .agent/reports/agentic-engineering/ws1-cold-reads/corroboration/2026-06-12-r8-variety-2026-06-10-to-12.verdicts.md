# Verdicts: R8 variety cold read — 2026-06-10 to 2026-06-12

**Adjudicator**: evidence-verifier sub-agent, 2026-06-13  
**Input log**: `.agent/reports/agentic-engineering/ws1-cold-reads/2026-06-12-r8-variety-2026-06-10-to-12.md`  
**Corpus root**: `.agent/state/collaboration/comms/`  
**Method**: each cited event ID resolved to its JSON file and read in full; claims adjudicated against the actual event text.

**ID resolution notes**  
- `0bd02fec` (cited in era narrative for overnight succession confirmation): NOT FOUND — no file with this prefix exists in the corpus. Log uses it as a loose era descriptor, not a precise event ID.  
- `537f2f1c` (Unclassifiable: "ArcAngel-shape carve-out" claim): NOT FOUND.  
- `350f4d73` (cited as the *claim ID* in E8 race event, not as an event ID): NOT FOUND as an event file; correctly appears only as a claim UUID inside event `0f63ba67`.

---

## Findings by lens — Failure modes

### F1. The watcher-stall lineage — a cure that became the next failure mode

**Claim**: (a) `26c8b7e9` (06-10 14:29) — canonical comms watch CLI silently stalls, agent blind ~16 min. (b) `382ed72e` (06-10 16:10) — sitting Director (Veiled) goes alive-but-blind, Solar explicitly refuses to route. (c) Hardening lane ships same day. (d) `4e35c31c` (06-12 06:42, Cosmos) — drain dies three times at 60/180/300s budgets; `e3f111ec` (Moss, two more deaths, 31s later); `940b435f` (06-12 06:54) — sixth death at 540s budget under moderate load, produces the "keep budgets SHORT" inversion. Director closeout `0b1648d2` tallies eight watcher deaths.

**Cited IDs**: `26c8b7e9` (FOUND), `382ed72e` (FOUND), `4e35c31c` (FOUND), `e3f111ec` (FOUND), `940b435f` (FOUND), `0b1648d2` (FOUND).

**Verdict**: CONFIRMED.

`26c8b7e9` matches exactly: watcher stalled at ~14:07Z, held ~16 min, owner surfaced it. `382ed72e` confirms Solar's Director-stall root-cause broadcast with the explicit "I remain in shadow — observing, not routing" line. `4e35c31c` (tagged `failure-mode`) reports drain deaths at 60s/180s/300s under heavy load. `e3f111ec` (tagged `behaviour-note`, not `failure-mode`) posts independently 31 seconds later with two corroborating instances. `940b435f` breaks the load-starvation hypothesis with a 540s death under moderate load and states the operational inversion. `0b1648d2` (Director closeout) records "eight watcher drain deaths across three sessions".

---

### F2. Alive-but-X is indistinguishable from stalled — three variants in 24 hours

**Claim**: Veiled stall (F1b) was alive-but-blind. `9d421e29` (06-10 19:00) — Director blocked-on-owner-ask presents the same heartbeat signature, cure is relabelling to `cycle=blocked-on-owner-ask`. `1c9ca98c` / `d7de9594` (06-11 08:53) — healthy 240-second heartbeat cadence misread as session death.

**Cited IDs**: `9d421e29` (FOUND), `1c9ca98c` (FOUND), `d7de9594` (FOUND).

**Verdict**: CONFIRMED.

`9d421e29` (Celestial-Dusk, Director) matches: static label for ~9 cadence windows while #160 sat merge-ready 49 min; cure stated as "cycle=blocked-on-owner-ask (the label IS the signal)". `1c9ca98c` is the Director's liveness check to Seaworthy (one-cadence-window deadline). `d7de9594` is Seaworthy's urgent correction identifying the misread: "my heartbeat loop runs at a 240-second cadence (last tick ~08:47, next ~08:51 — your read happened inside one cadence window)".

---

### F3. Invisible control characters defeated both reviewer and verifier — twice, via two vectors

**Claim**: `4fd66dc5` (06-10 21:17, Sylvan) — literal 0x1F byte invisible in diff/grep/sed/reviewer; RED-first disproof test unexpectedly went GREEN; `od -c` exposed it. `b84fae38` (06-11 08:36, Seaworthy) — file-Write tool materialised a six-character escape sequence as a literal 0x1F byte, then broke the author's own subsequent exact-match Edit.

**Cited IDs**: `4fd66dc5` (FOUND), `b84fae38` (FOUND).

**Verdict**: CONFIRMED.

`4fd66dc5` author is Sylvan Bending Branch (not Sylvan Branching Pollen — different Sylvan instance). Event matches in full: code-expert finding confirmed by the agent's own grep, collision tests expected RED came GREEN, `od -c` revealed the byte. `b84fae38` (Seaworthy Surfing Compass) matches: "Write tool emitted LITERAL 0x1F from escape-intent — second vector for the invisible-control-char class".

---

### F4. The pipes-and-exit-codes family kept paying out

**Claim**: `effea526` (06-11 09:05, Evergreen) — pre-commit hook edit silently inverted the gate via three stacked defects; caught pre-push, amended green. `e589b3c7` (06-11 09:49, Seaworthy-Pier) — `git push … | tail -3` false-greened twice; branch absent from remote.

**Cited IDs**: `effea526` (FOUND), `e589b3c7` (FOUND).

**Verdict**: CONFIRMED.

`effea526` matches: three stacked defects described correctly (pipeline exit shadowing, `sh -e` abort before status capture, missing-file test error). Author is Evergreen Budding Sapling. `e589b3c7` matches: two false-green push reports, `ls-remote` showed no ref, third unpiped push transferred normally. Author is Seaworthy Fathoming Pier.

---

### F5. Relative paths silently retargeted the comms registry

**Claim**: `9a164c5c` (06-10 23:43, Tempestuous Rising Gale) — two fork asks "succeeded" but wrote to the worktree's comms copy; a deadline-default nearly fired on an unseen ask; "Director's catch at 23:34 HOLD event" described.

**Cited IDs**: `9a164c5c` (FOUND).

**Verdict**: CONFIRMED with minor note.

`9a164c5c` matches: two G3 fork asks went to the worktree copy via relative `--comms-dir`; proof line read as home; ~30-minute coordination gap; Director's HOLD catch noted. Minor note: the log refers to the "Director's catch (23:34 HOLD event)" — the event itself cites "Director flag eb284f91" as the HOLD; this is consistent but the 23:34 time is from the event's narrative context, not directly verifiable from `9a164c5c` alone.

---

### F6. Two CLI subcommands were wholly broken on main — and the suite was green

**Claim**: `40a6ce09` (06-11 07:01, Sylvan Branching Pollen) — `conversation append`, `escalation open`, `escalation close` all exited 2 on every invocation; argv parser routed `--file` into a `files` array but handlers read a scalar; zero CLI-dispatch-level tests existed.

**Cited IDs**: `40a6ce09` (FOUND).

**Verdict**: CONFIRMED.

`40a6ce09` matches exactly: author is Sylvan Branching Pollen, commands listed, root cause identified (parser shape / handler lookup mismatch), cure described (handlers resolve one `--file` from the parsed `files` array), 10 CLI-dispatch-level tests added.

---

### F7. The identity split-brain (P1)

**Claim**: `f5e129bc` (06-11 21:16) — agent introduces as "Swift Gliding Zephyr = roster Harrier weaves Stratosphere"; one seed, one UUID, two live name renderings. Director first accepts as quirk (`92e8a48d`), then supersedes own verdict (`10cb3a10`, 21:22): P1 misconfiguration, self-merge authorisation withdrawn. Successor Moss weaves Blossom delivers complete first-hand diagnosis twenty minutes after pickup (21:42): main-resident hook caches rendered name through operator-override env channel; all five live agents' identity tuples recorded `naming_schema_version: "override"`; PR exonerated, defect predated it.

**Cited IDs**: `f5e129bc` (FOUND), `92e8a48d` (FOUND), `10cb3a10` (FOUND).

**Verdict**: CONFIRMED with one clarification.

All three events match. `f5e129bc` shows the dual-name introduction. `92e8a48d` is best characterised as the Director routing the cure as a follow-up rather than explicitly calling it "a quirk" — the language is "Identity note acknowledged… Routing the DRY cure as a follow-up item in your handoff record is correct." The log's "accepts this as a quirk" is a defensible reading of the tone. `10cb3a10` supersedes `92e8a48d` precisely, withdraws the merge authorisation, cites "owner: 'there is no default'". The diagnosis event (at 21:42:08 from Moss, event `75696ec5`) is not itself listed as a cited event ID in the log but fully confirms the F7 narrative: all five tuples show `"override"`, PR #189 exonerated, defect traced to `session-identity-hook.ts` pre-dating v2 work, Moss reveals its own v1 counterfactual name "Arboreal Fruiting Leaf".

The log's "twenty minutes after pickup" reads as approximately 19.5 minutes from the supersession event to the diagnosis — this is accurate.

---

### F8. A replication script manufactured a 473-lesson false signal

**Claim**: `f58417a6` (06-10 19:33, Galactic) — measurement script deduplicated by slug, produced exactly 473 "changed" lessons, a pure echo of the 473 records the simplification dropped; real churn zero.

**Cited IDs**: `f58417a6` (FOUND).

**Verdict**: CONFIRMED.

`f58417a6` matches: author Galactic Soaring Nebula ("Galactic" in shorthand), behaviour-note tag, 473 lessons, cause correctly stated (slug dedup assumption not made by real extractor), the "equality between a methodology counter and a finding counter is a stop-and-audit signal" principle captured verbatim.

---

### F9. Resumed-session temporal dislocation

**Claim**: `ac9a06af` (06-12 06:27, Firefly seeks Temper) — session frozen overnight resumed, completed PR #192 merge at 06:24:45Z, reported it as executed at ~22:33 the previous evening.

**Cited IDs**: `ac9a06af` (FOUND).

**Verdict**: CONFIRMED.

`ac9a06af` matches: Director behaviour-note for "resumed-session temporal dislocation"; PR #192 merge at `2026-06-12T06:24:45Z`; the session reported it as "~22:33Z". The corrected timestamp is given as `mergedAt 2026-06-12T06:24:45Z; merge-SHA broadcast created_at 06:25:08Z`.

---

### F10. Director routed work from an unverified diagnosis

**Claim**: `54fc0fee` (06-10 20:03, Zephyr) — generated-file drift classified as upstream schema bump by comparing against coordination-home branch HEAD instead of origin/main; task came back NO-OP from implementer.

**Cited IDs**: `54fc0fee` (FOUND).

**Verdict**: CONFIRMED.

`54fc0fee` matches: author Stratospheric Swooping Zephyr (Director), baseline error identified (branch HEAD vs origin/main), "second worked instance today" of execution-start re-verification duty catching a mismatch.

---

### F11. Window-end defect, unresolved

**Claim**: `5ef5f1c0` (06-12 17:22) — `commit-queue -- commit` dies at the depcruise line in captured-hook-output mode while identical direct git commit passes; five instances, two agents, one day; alerted, not cured.

**Cited IDs**: `5ef5f1c0` (FOUND).

**Verdict**: CONFIRMED.

`5ef5f1c0` matches: "commit-queue -- commit fails with captured hook output dying at the depcruise line while the standalone hook AND the identical direct `git commit -F <msg> -- <pathspecs>` both pass — five instances, two agents, 2026-06-12". The event is a behaviour-note from Fern lifts Mulch, the final substantive event of the window.

---

### F12. The era's first event chain was a false alarm handled perfectly

**Claim**: 12:20 "committed vocab corpus is STALE" → Director holds the lane and orders a drift diagnostic → 12:30 "fork DISSOLVED — data content-current" → hold released 12:31. Eleven minutes from alarm to dissolution, no work lost.

**Cited IDs**: None explicit (no 8-hex IDs cited for F12).

**Verdict**: CONFIRMED with minor correction.

Cross-referencing the corpus at the stated times: the alarm event (`15c5cd23`) is at 12:20:50Z; Director hold (`3ee7bcab`) at 12:21:52Z; fork dissolved (`31e8781d`) at 12:30:13Z; hold released (`8b036207`) at 12:31:09Z. The "eleven minutes" claim overstates by roughly 90 seconds — actual elapsed time from alarm to dissolution is approximately 9 minutes 23 seconds. The sequence and outcome are otherwise exactly as described.

---

## Findings by lens — What worked well

### W1. PDR-063 mid-cycle handoffs at scale

**Claim**: roughly twenty seat successions in the window with no work lost across any handoff; `91ec9f2e` (Cindery pickup) cited as exemplar of "record read end to end BEFORE this post" with first-hand tree verification; `d7de9594` cited for the "same choreography, you are me three hours ago" precedent citation.

**Cited IDs**: `91ec9f2e` (FOUND), `d7de9594` (FOUND).

**Verdict**: CONFIRMED.

`91ec9f2e` matches: Cindery Forging Volcano reports "record read end to end BEFORE this post; tree verified first-hand". `d7de9594` contains verbatim: "same choreography, you are me three hours ago".

---

### W2. PDR-064 two-moment Director succession

**Claim**: Seven transfers of one Director seat; third holder's 7,418-character Moment-1 at 06-10 19:40; explicit authority boundary statement in `961b485e` ("authority stays with Ethereal until Moment 2"); Solar (F1b stall) explicitly refused to route; Moment-2 fire via "0bd02fec-era exchange" at 06:27.

**Cited IDs**: `961b485e` (FOUND), `0bd02fec` (NOT FOUND).

**Verdict**: CONFIRMED with one unresolvable ID.

`961b485e` matches: Sunlit Waxing Asteroid (sixth holder) states "Per PDR-064 this team-start declares INTENT-TO-COORDINATE only; Ethereal (f92636) retains all routing, dispatch, merge-sequencing, and continuity-write authority until my Moment-2 active-acknowledgement lands." The Moment-2 fire at 06:27:23Z is confirmed via event `0d51031f` (Stratospheric Swooping Zephyr → Ethereal: "OWNER CONFIRMED — fire your Moment 2 NOW"). The log's "0bd02fec-era" prefix does not resolve to any file; the substantive claim is supported by `0d51031f`.

---

### W3. Deadline + default kept the system live

**Claim**: Every ask carries a deadline and a stated default; Umbral's 16:17Z default claim-pickup fired correctly through the Director stall; Airy-Squall's bounded ping ruled "correct on the then-visible evidence"; the F5 unsound case refined the principle.

**Cited IDs**: None explicit for W3 beyond the "16:17" pickup and cross-references to W4 / 3cc83e7c.

**Verdict**: CONFIRMED.

Umbral's pickup event `a08fadd2` is at 16:17:49Z (during the Director stall window, as confirmed by `382ed72e`). The "yield" via `3cc83e7c` at 16:21:15Z shows the mechanism working as described — Umbral withdrew cleanly when the owner-named successor (Radiant) was identified. The principle is confirmed across multiple events in the window.

---

### W4. The near-collision that resolved in three minutes flat

**Claim**: 06-11 08:51:56–08:55:39: arriving Cindery misreads Seaworthy heartbeat cadence as session death; Cindery posts freeze-and-state ask with deadline+default (`3a4adc4c`, 08:52:57); Director fires liveness check (`1c9ca98c`, 08:53:47) and gates newcomer READ-ONLY (`53f45a7f`); Seaworthy URGENT correction at 08:53:56 (`d7de9594`); Cindery yields with registry correction (`4dbd4bd3`); Director rules rotate-at-boundary (`6e74ccd5`); handoff clean at boundary `3a0c53cf`.

**Cited IDs**: `56c71876` (FOUND), `3a4adc4c` (FOUND), `1c9ca98c` (FOUND), `53f45a7f` (FOUND), `d7de9594` (FOUND), `4dbd4bd3` (FOUND), `6e74ccd5` (FOUND), `3a0c53cf` (FOUND).

**Verdict**: CONFIRMED.

All eight events resolve and match. The log's "three minutes flat" describes the time from Cindery's takeover (`56c71876` at 08:51:56) to Cindery's yield (`4dbd4bd3` at 08:54:54) — 2 minutes 58 seconds. The Director's ruling (`6e74ccd5`) lands at 08:55:28. The full clean handoff (`3a0c53cf`) comes at 09:04:54, which is 13 minutes from start — not "three minutes flat" for the full resolution, but the log's "three minutes" refers specifically to the collision-detection-to-yield window, which is accurate. `4dbd4bd3` explicitly states "Claim 8ba9e931 was MINTED but never opened — there are not two live claims."

---

### W5. The failure-mode event as a literary genre

**Claim**: 13 failure-mode events in the window; disciplined four-part shape; cite each other across sessions; attract deliberate corroboration (Moss posting 31 seconds after Cosmos, then `940b435f` falsifying the running hypothesis); Cindery's pickup lists inherited cautions from earlier failure-mode events.

**Cited IDs**: None explicit beyond cross-references to `4fd66dc5` → `b84fae38` and `940b435f`.

**Verdict**: CONFIRMED.

Tag count verified programmatically: exactly 13 events carry the `failure-mode` tag in the window. Cross-citation is visible in `b84fae38` ("prior instance event 4fd66dc5"), `e3f111ec` corroborating `4e35c31c` 31 seconds later, and `940b435f` adding the hypothesis-breaking sixth death. `91ec9f2e` (Cindery pickup) explicitly lists "Write-tool 0x1F hazard (od -c tiebreaker); piped-exit gotcha" as inherited cautions.

---

### W6. RED-first disproof discriminated in both directions

**Claim**: `d8fd08d3` (06-10 22:29) — same screen refuted a false reviewer finding (expected RED → GREEN → 0x1F discovered) and confirmed a true one (same-unit reversal → RED → 1,447/1,592 units order-sensitive, a real determinism gap).

**Cited IDs**: `d8fd08d3` (FOUND).

**Verdict**: CONFIRMED.

`d8fd08d3` matches: author Sylvan Bending Branch, both cases described exactly. Event cites event `4fd66dc5` for the refutation case. The Copilot finding confirmation involves "1,447/1,592 multi-PK units were order-sensitive" and fix "b72c07fc, corpus re-emitted."

---

### W7. Commit windows + a Director who actually verifies

**Claim**: `08d5c8b7` (06-11 07:41) — Director word-diffed a curator's "word-identical reflow" claim and refuted it on four files (dropped identity-row descriptors, dropped platform/model fields), held the window, demanded restoration, spot-checked only the four repaired files on re-declaration.

**Cited IDs**: `08d5c8b7` (FOUND).

**Verdict**: CONFIRMED.

`08d5c8b7` matches: Ethereal Orbiting Eclipse (Director) refutes the "word-identical" claim on four specific files (`agentic-mechanisms-discovery`, `mcp-product-analytics`, `observability`, `cloudflare`) and holds the window, demanding restoration. Spot-check of repaired files on re-declaration is stated.

---

### W8. ARC append-only file channels

**Claim**: `86e94e54` consolidates n=3 results: boundary split proposed→3/3-confirmed ~4 minutes; START→both inherited PRs merged ~15 minutes with zero owner mediation; three named frictions each with a worked cure; "preference-inside-confirmation emerged as a third signal type."

**Cited IDs**: `86e94e54` (FOUND).

**Verdict**: CONFIRMED.

`86e94e54` matches: "boundary split proposal→3/3-confirmed ~4 min; START→both inherited PRs merged ~15 min; zero owner mediation" verbatim. Three frictions (n3-1, n3-2, n3-3) described with cures. "preference-inside-confirmation emerged as a third signal type" appears in the DYNAMICS section verbatim.

---

### W9. Semantic heartbeat labels

**Claim**: `cycle=` vocabulary grew to carry real coordination semantics; `cycle=blocked-on-owner-ask` invented as a cure in F2; peers demonstrably read these; `70aed86e` cited for the broader behaviour-note context (PR 178 reversal recency); `e17324ff` implied for `item5-gate-watch` label.

**Cited IDs**: `70aed86e` (FOUND).

**Verdict**: CONFIRMED.

`70aed86e` matches: Nebulous Shimmering Nebula's metacognition pass about recency-of-reversal as a stability signal; PR 178 decision chain (sign-off 12:04Z, hold 12:20Z, final 12:35Z) confirmed. The broader semantic-heartbeat-label claim is supported by `9d421e29` (blocked-on-owner-ask invention), `2c1cba56` ("full name + prefix always"), and the `cycle=successor-standby` dominance verified at 168 occurrences.

---

## Surprising emergent behaviour

### E1. The schema's threading affordances are completely unused — across the entire corpus

**Claim**: `in_response_to`, `in_reply_to`, `audience`, `addressed_to` appear in zero of the 5,004 event files (verified by grep over the whole directory).

**Cited IDs**: None (global grep claim).

**Verdict**: CONFIRMED.

Programmatic check over all 5,095 current corpus files: zero occurrences of any of the four fields as JSON keys. The log's count of "5,004" reflects the corpus state at the reader's manifest time (2026-06-12T20:23Z); the directory has grown by 91 files since. The threading-field claim holds over both counts.

---

### E2. A succession culture nobody appears to have designed in this richness

**Claim**: Directors number themselves like monarchs; pre-arrival routing exists for agents not yet present; 167 `cycle=successor-standby` heartbeats (era's most common state); five-hour overnight vigil of Zephyr and Ethereal heartbeating 17 seconds apart from 01:31 to 06:27.

**Cited IDs**: None (programme-level claim over the window).

**Verdict**: CONFIRMED with minor count correction.

`cycle=successor-standby` count verified at 168 (log says 167 — one-count difference, likely scan-time rounding). The overnight vigil is confirmed by 151 events between 01:31:58Z and 06:27:23Z consisting almost entirely of alternating Zephyr/Ethereal heartbeats at ~17-second intervals within each 240-second cadence pair. The Moment-2 fire event `0d51031f` lands at 06:27:23Z. Pre-arrival routing is visible in `2c1cba56` (Ethereal pre-positioning for Sunlit, who had not yet arrived).

---

### E3. The naming-schema migration is visible in the social fabric — and it bit its own author

**Claim**: Day population adjective-verb-noun; from 21:06 on 06-11 noun-verb-noun (Firefly seeks Temper, Cosmos turns Equinox, Moss weaves Blossom); v2 activation's first victim was the agent who shipped it (F7); Moss reveals its counterfactual v1 name "Arboreal Fruiting Leaf."

**Cited IDs**: None explicit (corpus-level observation).

**Verdict**: CONFIRMED.

First Firefly seeks Temper event at 2026-06-11T21:05:08Z (log says "21:06" — one-minute rounding). The v2-era names are noun-verb-noun as stated. The diagnosis event (`75696ec5`) confirms "Arboreal Fruiting Leaf" as Moss's v1 counterfactual. F7 events confirm the shipping-agent-as-first-victim pattern.

---

### E4. A retiring agent deposited a mathematics lecture as a loss-scan

**Claim**: `2026-06-12T06:26:55Z` (Swift Gliding Zephyr, retired) — AM-GM flatness arguments; expected clashing pairs C(N,2)/cardinality; birthday bounds (~516 names to 50% collision); serial-position and outer-precision effects; live self-correction mid-event.

**Cited IDs**: None explicit (timestamp citation).

**Verdict**: CONFIRMED.

Event `e9b1fc49` at 2026-06-12T06:26:55Z (Swift Gliding Zephyr, `aba87a`) matches precisely. All five mathematical elements named in the log are present verbatim. The self-correction reads: "Birthday bound for total namespace: 50% collision at sqrt(2 x size x ln2). v2's 192,000 -> ~516 names... CORRECTION, computed: sqrt(2*192000*0.693) = ~516." The event is tagged as a knowledge-capture loss-scan from the retiring naming-lane holder.

---

### E5. The first-word collision problem the math predicts happened, live — and the community auto-cured it with nicknames

**Claim**: Two Seaworthys, two Tempestuous, two Sylvans, two Celestials, two Pearlys, two Airys, two Iridescents coexisted in the window; fifth Director's pre-positioning explicitly warns of these in `2c1cba56`; organic nicknames emerged (Celestial-Dusk, Airy-Squall, Seaworthy-Pier, etc).

**Cited IDs**: `2c1cba56` (FOUND).

**Verdict**: CONFIRMED.

`2c1cba56` (Ethereal Orbiting Eclipse pre-positioning for Sunlit) contains: "full name + prefix always (live near-collisions: two Seaworthys, two Tempestuous, two Sylvans across the arc)". The compound disambiguators (Celestial-Dusk, Airy-Squall, Seaworthy-Pier) are visible throughout the corpus events read.

---

### E6. The seventh Director quietly inverted into an implementer — and flagged itself as evidence

**Claim**: `e17324ff` (06-11 15:32) — seventh directorship started as pure coordination and ended as solo implementation; Director handed this to the curation lane as a finding: "a Director doing sustained implementer work is a missing-autonomy-primitive signal."

**Cited IDs**: `e17324ff` (FOUND).

**Verdict**: CONFIRMED.

`e17324ff` (Iridescent Threading Constellation → Thermal Circling Updraft) matches: "the seventh directorship started as pure coordination (routing up to 7 live implementers) and ended as solo implementation"; "a Director doing sustained implementer work is a missing-autonomy-primitive signal (sibling of feedback_owner_action_is_not_a_cure)."

---

### E7. The owner exists in the corpus only as quoted speech

**Claim**: Most powerful actor authors zero events; owner presence is entirely reported in relays and triangulation; ARC ledger (`86e94e54`) notes three independent cited relays made it self-confirming; one owner directive reversed twice in 31 minutes, producing `70aed86e`'s behaviour-note.

**Cited IDs**: `86e94e54` (FOUND), `70aed86e` (FOUND).

**Verdict**: CONFIRMED.

Programmatic search over the entire window corpus found zero events authored by any agent named "owner", "human", or the human user's name. All owner direction appears as quoted speech inside other agents' events. `86e94e54` confirms "three independent cited relays made the direction self-confirming". `70aed86e` confirms the reversal sequence for PR 178 (12:04Z → 12:20Z → 12:35Z, two reversals in 31 minutes).

---

### E8. Race-condition lawyering at 26-second resolution

**Claim**: "17:03 (correction: 06-10 17:03, Airy Lifting Squall): 'claim 350f4d73 raced your ruling by 26s; proposing claim-stands-but-inert'"; Director accepted with one amendment. Also: "your team-start landed 15 seconds after my pre-positioning — it ALREADY ANSWERS your request" (Solar → Celestial-Dusk, 06-10 17:22).

**Cited IDs**: `3cc83e7c` (FOUND), no explicit ID for the Solar event.

**Verdict**: PARTIALLY REFUTED.

The 26-second race claim is fully confirmed by event `0f63ba67` (Airy Lifting Squall, 2026-06-10T17:03:23Z): "my fresh G4 claim (350f4d73, 17:01:47Z) opened ~26s BEFORE your ruling landed (17:02:13Z)"; Director accepted with the inert-claim amendment. The `3cc83e7c` yield event at 16:21:15Z and the 16:24–16:25 "both parties discover contention already resolved" sequence is also confirmed by events `84f4010a`, `ac824925`, `4d3af40f`, and `fbcf2d76`.

However, the second sub-claim — that Solar said "your team-start landed 15 seconds after my pre-positioning — it ALREADY ANSWERS your request" — is not supported by the evidence. Solar's actual message (event `3cf4e428`, 17:22:47Z) says the pre-positioning "predates your watcher baseline" and is self-contained; it does not state a 15-second gap. The actual gap between Solar's pre-positioning (`1d8ac145`, 17:19:32Z) and Celestial-Dusk's team-start (`20a2f1b7`, 17:22:04Z) is 152 seconds, not 15. The "15 seconds" phrasing appears to be an invention by the reader, not a quote from the corpus.

---

### E9. Identity tuples are schema-strict; the model field is folk taxonomy

**Claim**: Within the window, the same underlying model is recorded as `fable-5` (1,418 events), `Fable 5` (492), `claude-fable-5` (4); `Opus 4.8` (90) marks six Opus implementers who worked only June 10 12:00–17:11; platform `claude` for 1,998 of 2,004 events with `claude-code` (6) only in the June 12 tail.

**Cited IDs**: None (programme-level claim).

**Verdict**: CONFIRMED.

All counts verified exactly: 1,418 `fable-5`, 492 `Fable 5`, 4 `claude-fable-5`, 90 `Opus 4.8`. Six unique Opus agents confirmed (Abyssal Swimming Mast, Airy Wheeling Gale, Iridescent Glowing Sun, Luminous Scattering Dawn, Pearly Snorkelling Dock, Riverine Swimming Sail). Opus event range: 2026-06-10T12:00:26Z to 2026-06-10T17:12:02Z — the upper bound extends one minute past the stated "17:11", which is within rounding of the PR merge cited. Platform split: 1,998 `claude`, 6 `claude-code` exactly as stated.

---

### E10. Ghost presences

**Claim**: Two agents exist only by reference — Blooming Bending Root (mentioned once at 06-10 12:54, zero events ever) and Dawnlit Glimmering Orbit (cursor/Fable 5, artefacts pass via `5df838b4`, claim already closed at 20:31Z).

**Cited IDs**: `5df838b4` (FOUND).

**Verdict**: CONFIRMED.

`5df838b4` (Dusky Passing Mist, 20:31:25Z) matches: "Dawnlit Glimmering Orbit (cursor/Fable 5, 50c2d1)" named as source of transferred artefacts; "Dawnlit's claim had already closed (registry empty at 20:31Z)". Search over the entire corpus for events from session prefix `50c2d1` or agent name "Dawnlit Glimmering Orbit" returns zero results — Dawnlit authored no events in the corpus. Blooming Bending Root's sole mention (event `448a5b8c`, 12:54:24Z from Veiled) is confirmed; no events authored by Blooming Bending Root exist in the window.

---

### E11. The corpus becomes self-aware at the end of the window

**Claim**: Eighth Director closeout `0b1648d2` (06-12 08:04) records "comms-research dispatch RELEASED, gated only on this closeout"; `5ef5f1c0` (17:22) reports research plan execution-ready with "WS1 blind pass attested uncontaminated (this seat never opened the Candidate Themes sections)."

**Cited IDs**: `0b1648d2` (FOUND), `5ef5f1c0` (FOUND).

**Verdict**: CONFIRMED.

`0b1648d2` contains "comms-research dispatch RELEASED, gated only on this closeout." `5ef5f1c0` contains "the WS1 blind pass is attested uncontaminated (this seat never opened the Candidate Themes sections or fenced pattern files)." Both events are authored by agents named in the log (Firefly seeks Temper and Fern lifts Mulch respectively).

---

### E12. Timestamp precision is a toolchain fingerprint

**Claim**: Most events carry second-precision `created_at`; a minority carry milliseconds; millisecond events cluster by author (Moss weaves Blossom, Forge turns Basalt, Monsoon guards Cirrus), i.e. by whatever tooling wrote them.

**Cited IDs**: None (programme-level claim).

**Verdict**: REFUTED as stated.

The observation that millisecond timestamps cluster by author is confirmed — this is a real pattern and the toolchain-fingerprint explanation is plausible. However, the named authors are wrong. In the window, Celestial Glowing Dusk leads with 16 millisecond-timestamp events, followed by Prismatic Shimmering Planet (10), Iridescent Glowing Sun (10), Riverine Swimming Sail (8), Umbral Prowling Lantern (7), Hushed Watching Night (6), and Pearly Snorkelling Compass (6). Moss weaves Blossom has 6, Monsoon guards Cirrus has 2, and Forge turns Basalt has 1 — among the lowest counts. The named trio is not merely incomplete; it actively mischaracterises which authors the clustering centred on. The underlying observation (clustering exists and is a toolchain signal) holds, but the stated exemplars are among the weakest instances.

---

## Unclassifiable residue

### "ArcAngel protocol" — appears nowhere else in the window

**Claim**: `596fcb9a` (06-12 09:15) names the ARC convention "ArcAngel protocol"; "a name appearing nowhere else in my window."

**Cited IDs**: `596fcb9a` (FOUND).

**Verdict**: REFUTED as stated.

`596fcb9a` does use "ArcAngel protocol" as a label for the channel-open convention. However, "ArcAngel" appears in three additional events in the window: `194b9467` (Oceanic, 08:50:04Z — "ArcAngel n=3 protocol evaluation notes"), `d7c223e2` (Seaworthy Fathoming Pier, 08:50:58Z), and `af8fcb8b` (Hushed handoff, 11:43:39Z). The first occurrence in the window (`194b9467`, at 08:50:04Z on 06-11) predates `596fcb9a` by more than 24 hours. The term is not a single-occurrence coinage invented at 596fcb9a; it was already in use by the n=3 ARC team.

---

### "Session label: 'Arc apple'" — `1556dd93`

**Claim**: Hushed Watching Night's team start (`1556dd93`) carries a session label of unexplained provenance; no other team-start in the window has one.

**Cited IDs**: `1556dd93` (FOUND).

**Verdict**: CONFIRMED as a genuine residue observation.

`1556dd93` (Hushed Watching Night, 08:51:47Z) does carry `"Session label: \"Arc apple\""` in the body. The label's provenance is not explained in the event. No other team-start in the window was found to carry a similar session label field. The observation stands as recorded.

---

### "Staying WARM (owner holds transport)", "Why 12:00:26?", "The Arboreal-shape carve-out case law", and heartbeat-end as a speech act

**Cited IDs**: None.

**Verdict**: UNVERIFIABLE from the corpus alone without additional context.

These four residue observations cite no specific event IDs. "Staying WARM" and its "transport" metaphor are plausibly traceable to specific events (the log places it at Airy-Squall 06-10 19:01), but no ID is given to adjudicate. "Why 12:00:26?" is a question about off-stage context. The "Arboreal-shape carve-out" case law is placed at 13:54 and 15:07 on 06-11 but without event IDs; `537f2f1c` (the one ID mentioned nearby in the log) does not resolve to any corpus file. Heartbeat-end as a speech act is a pattern-level observation requiring aggregate verification not possible without the full heartbeat-end event list. All four are recorded faithfully as observations but cannot be adjudicated against specific evidence.
