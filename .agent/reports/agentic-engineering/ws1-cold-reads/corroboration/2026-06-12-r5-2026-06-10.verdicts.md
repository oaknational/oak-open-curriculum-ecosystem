# Corroboration verdicts — R5 cold read, comms window 2026-06-10

Adjudicator: evidence verifier (Opus). Method: read the input log in full; enumerated every
distinct finding citing at least one event id across §3 (Findings by lens), §4 (We did not
expect this), and §5 (Unclassifiable residue); resolved each cited id to a file under
`.agent/state/collaboration/comms/` (full UUID, or 8-hex prefix glob), read the actual events,
and adjudicated CONFIRMED / REFUTED / UNVERIFIABLE against the claim **as stated**. Window-wide
counts (795 events, threading fields, `message_kind`, tags, roster, timestamp precision) were
recomputed directly over every file in the window. All cited 8-hex prefixes resolved to exactly
one file each; `c42e4b8c` is a *claim* id (not an event id) and correctly has no event file.

Scope note: agent-to-owner chat, fenced surfaces (threads, plans, reports other than this
input/output pair, decision records), and git/GitHub state are outside the corpus and were not
opened; claims resting solely on off-stream facts are marked UNVERIFIABLE where the corpus
cannot reach them.

---

## §3.1 Failure modes

### F3.1-1 — Canonical watcher hang-but-run (twice, cured same-day)
Claim: `comms watch` stalls silently while alive — Abyssal ~14:07Z (`e27e964a`, capture
`26c8b7e9`), then the Director at 15:28:55Z (`382ed72e`); cure PR #157 merged 17:10 (`6fc7351c`).
Cited ids: `e27e964a`, `26c8b7e9`, `382ed72e`, `6fc7351c` — all resolved.
Verdict: **CONFIRMED.** `e27e964a` (Veiled-directed) and `26c8b7e9` (failure-mode capture)
both describe Abyssal's watcher emitting nothing ~16 min from ~14:07Z while the process stayed
alive; `382ed72e` records Veiled's seen-file frozen at 15:28:55Z; `6fc7351c` is Solar's
"#157 MERGED at 17:10:42Z" notice describing the fail-loud/liveness cure.

### F3.1-2 — The detector cannot detect itself
Claim: Veiled, who ran the stall diagnostic on Abyssal, attributed their own identical stall to
routine work (`b385b94c`); cure adopted by later Directors (`7f452f66`).
Cited ids: `b385b94c`, `7f452f66` — resolved.
Verdict: **CONFIRMED.** `b385b94c` states verbatim "THE DETECTOR CANNOT DETECT ITSELF … I
attributed the silence to routine work and was woken only by owner transport." `7f452f66`
(Dusk Moment-2) declares the cycle-boundary watcher-staleness self-check citing "the detector
cannot detect itself."

### F3.1-3 — Alive-but-blocked indistinguishable from stalled (three variants)
Claim: Veiled (blind watcher), Celestial-Dusk (blocked on owner ~67 min, `9d421e29`),
Celestial-Orbit (2-hour owner-transport gap, `cd19782a`); cure honest wait-state labels
adopted within the hour (`c9b4dc78`).
Cited ids: `9d421e29`, `cd19782a`, `c9b4dc78` — resolved.
Verdict: **CONFIRMED.** `9d421e29` (Dusk failure-mode) names heartbeat-only ~9 windows blocked
on an owner ask and the `cycle=blocked-on-owner-ask` cure; `cd19782a` (Orbit) describes the 2h
owner-transport gap with stale team-start heartbeat args; `c9b4dc78` (Orbit) adopts
`cycle=idle-awaiting-owner-direction`. The "~67 min" figure: `9d421e29` says "~9 cadence
windows (17:50-18:57Z)" ≈ 67 min — consistent.

### F3.1-4 — Silent write failures in the coordination CLI
Claim: two Director ruling events "failed to write (CLI id-validation failure)", discovered
missing later (`53112f47`); doctrine "a write's only proof is the proof line" (`eb284f91`).
Cited ids: `53112f47`, `eb284f91` — resolved.
Verdict: **CONFIRMED.** `53112f47` opens "this consolidates two events of mine that failed to
write (CLI id-validation failure on my side, just diagnosed)"; `eb284f91` states "a write's
only proof is the 'wrote comms event <id>' line; its absence means failure — napkin doctrine,
hit a Director today too."

### F3.1-5 — Relative-path writes silently retarget the registry
Claim: Tempestuous's G3 fork asks landed in the worktree's comms copy; proof line read as home;
~30-min gap; deadline-default nearly fired on an unseen ask (`9a164c5c`, `16939248`).
Cited ids: `9a164c5c`, `16939248` — resolved.
Verdict: **CONFIRMED.** `9a164c5c` (failure-mode) describes exactly this: relative
`--comms-dir`/`--active` from a worktree, a true proof line that proved the wrong destination,
"~30-minute coordination gap … deadline-default nearly fired on an unseen ask," caught by the
Director's direct lookup after the pre-default ping. `16939248` is the resend naming the same
relative-path violation and cure.

### F3.1-6 — Pre-grounding against a live peer worktree grades the cure, not the head
Claim: Airy-Squall's held REFUTE was wrong because the worktree carried the peer's uncommitted
fix (`b46ccedd`); cure pin the SHA.
Cited id: `b46ccedd` — resolved.
Verdict: **CONFIRMED.** `b46ccedd` (behaviour-note) states the held verdict "graded the cure,
not the PR head (4b7f17d2)" because the read tree carried Umbral's uncommitted fix, and
prescribes `git show <head>:<path>`.

### F3.1-7 — Replication-script simplification manufactures findings
Claim: keep-first dedup produced a 473-lesson false-churn signal; tell was equality between a
methodology counter and a finding counter (`f58417a6`); faithful re-run = 100.000% identity.
Cited id: `f58417a6` — resolved.
Verdict: **CONFIRMED.** `f58417a6` (Galactic behaviour-note) describes the keep-first dedup, the
473 false-churn echo, the "suspicious equality between an artifact count (dup_slug_records=473)
and a result count (set_changed=473)" tell, and "Pass-2 … 100.000% identity, finding inverted."

### F3.1-8 — Invisible control byte defeats reviewer and verifier simultaneously
Claim: literal 0x1F in dedup keys invisible in diff/grep/sed; both the code-expert finding and
the first-hand confirmation were wrong; only an empirical probe plus `od -c` revealed it
(`4fd66dc5`); cure RED-first plus escapes-not-bytes.
Cited id: `4fd66dc5` — resolved.
Verdict: **CONFIRMED.** `4fd66dc5` (Sylvan failure-mode) describes the literal 0x1F
unit-separator, the reviewer and first-hand grep both fooled, the empirical probe + `od -c`,
and the RED-first / escape-sequence cure.

### F3.1-9 — Baseline-selection error in drift classification
Claim: Director routed a "new upstream schema bump" micro-PR diagnosed against branch HEAD not
origin/main; implementer's re-verification returned NO-OP (`459451d5`); self-capture `54fc0fee`;
turbo-cached sdk-codegen cannot observe upstream without `--force`.
Cited ids: `459451d5`, `54fc0fee` — resolved.
Verdict: **CONFIRMED.** `459451d5` (Fruited→Zephyr) reports the NO-OP verdict, byte-identical
regen, and "the unforced run is turbo-cached and cannot observe upstream"; `54fc0fee` (Zephyr
failure-mode) self-captures "I classified … by comparing the working tree against the
COORDINATION-HOME BRANCH HEAD — never against origin/main."

### F3.1-10 — Watcher render defect masked by heartbeat-only traffic
Claim: a jq context bug made substantive events fall to a parse-error line while heartbeats
rendered fine; first diagnosis "benign read race" tested a different parse (`6cf5f4ab`); the
four-shape test propagated to two agents within 11 minutes (`01ed41d7`, `7afadb7f`).
Cited ids: `6cf5f4ab`, `01ed41d7`, `7afadb7f` — resolved.
Verdict: **CONFIRMED.** `6cf5f4ab` (Tempestuous failure-mode) describes the `else .kind`
evaluating against the tags array, heartbeats masking the defect, the wrong benign-race
diagnosis via a surrogate parse, and the four-shape pre-arm test. `01ed41d7` (Zephyr,
20:56:02Z) and `7afadb7f` (Ethereal, 20:56:39Z) both adopt the recipe; `6cf5f4ab` is 20:55:18Z,
so propagation is within ~1.5 min — well inside the stated "11 minutes."

### F3.1-11 — Review gap across a rotation seam
Claim: the c2-2 bundle landed gate-green with no dedicated specialist pass — flagged in the
handoff and by the retiring agent post-closeout (`2247a908`), recovered by a Director-routed
backstop (`a9b60005`).
Cited ids: `2247a908`, `a9b60005` — resolved.
Verdict: **CONFIRMED.** `2247a908` (Orbit's post-closeout flag) names the 83196e20 bundle as
having "never had its own dedicated specialist pass" and recommends a dispatch; `a9b60005`
(Director→Eclipsed) routes the code-expert + mcp-expert backstop with merge-ready gated on
adjudication.

### F3.1-12 — Registry race conditions around liveness
Claim: Director closed Orbit's claim seconds before Orbit's liveness broadcast (`dea9b20b`);
Airy-Squall's fresh claim raced the Director's ruling by 26 seconds (`0f63ba67`).
Cited ids: `dea9b20b`, `0f63ba67` — resolved.
Verdict: **CONFIRMED.** `dea9b20b` says "my closure of your claim 3df980b5 at 19:15:09Z raced
your liveness broadcast by seconds"; `0f63ba67` says "my fresh G4 claim (350f4d73, 17:01:47Z)
opened ~26s BEFORE your ruling landed (17:02:13Z)."

### F3.1-13 — Plan-premise falsification discovered late
Claim: the G1a `prerequisiteFor` derivation survived the morning's data re-verification because
counts matched while semantics were arbitrary; falsified at G3 grounding (`16939248`), after
five PRs built on it.
Cited id: `16939248` — resolved.
Verdict: **CONFIRMED.** `16939248` falsifies the within-thread ordering premise and states the
`prerequisiteFor` edges are chained on the same falsified file-encounter basis since G1a, with
year-axis re-derivation re-chaining ~93%. The "counts matched while semantics were arbitrary"
framing is corroborated by `31e8781d` (morning re-mine: counts identical) — consistent.

### F3.1-14 — Test flakes surfaced, not silenced (uncured at window end)
Claim: `lifecycle-lease.integration` under parallel load (`0b2913be`), `auth-routes`
scopes_supported (`f0e5dc95`), one Docker-registry infra flake on #163 CI (`043de7bb`).
Cited ids: `0b2913be`, `f0e5dc95`, `043de7bb` — resolved.
Verdict: **CONFIRMED.** `0b2913be` (Umbral closeout) names the lifecycle-lease flake under full
parallel turbo load; `f0e5dc95` (S3 PR report) names the auth-routes scopes_supported flake
"passed on re-run … uncured, surfacing not silencing"; `043de7bb` (a directed #163 settlement)
records "one Docker-registry infra flake, re-run clean."

---

## §3.2 What worked well

### F3.2-1 — Execution-start re-verification converted scope explosions to ten-minute closures
Claim: stale-corpus fork dissolved by a 1.74s diagnostic (`31e8781d` → `8b036207`); schema-resync
returned NO-OP with evidence (`459451d5`); both findings inverted under measurement.
Cited ids: `31e8781d`, `8b036207`, `459451d5` — resolved.
Verdict: **CONFIRMED.** `31e8781d` reports the 1.74s diagnostic and content-identical re-mine;
`8b036207` is Veiled releasing the G1a hold ("fork dissolved by evidence"); `459451d5` is the
NO-OP resync verdict.

### F3.2-2 — Bounded ask + deadline + default-action as the universal anti-stall device
Claim: kept lanes moving through two Director outages (Umbral 16:17Z `a08fadd2`; Airy-Squall
18:20Z `960c44e8`); the pre-default courtesy ping (`fe95b303`) caught the misdirected-ask
failure before a default fired.
Cited ids: `a08fadd2`, `960c44e8`, `fe95b303` — resolved.
Verdict: **CONFIRMED.** `a08fadd2` is Umbral's 16:17:49Z default-fired pickup into Director
silence; `960c44e8` is Airy-Squall's 18:20:29Z bounded ping with owner-surface default;
`fe95b303` is the 23:33:52Z pre-default courtesy ping on the G3 fork.

### F3.2-3 — Self-contained PDR-063 handoff records; once caught a false record claim
Claim: G1b passed through four seat-holders + the G1a author and merged green as one
seven-commit PR (`e2642f5b`); successors verified record claims and once caught a false one
(`0c8ce759`: "Radiant's drafted-message claim was false").
Cited ids: `e2642f5b`, `0c8ce759` — resolved.
Verdict: **CONFIRMED.** `e2642f5b` records PR #161 merged at 793cb3c1, seven commits, final head
47ad98df. `0c8ce759` (Orbit closeout) states "Radiant's 'drafted message in
<scratch>/radiant-commit2.log' claim was false … handoff-record claims are input-to-verify."
The "four seat-holders" (Abyssal→Radiant→Celestial-Orbit→Eclipsed) is corroborated across
`57c97cbc`, `befaa944`, `33cb52c2`.

### F3.2-4 — Yield-over-contest
Claim: the one genuine contention resolved in ~6 minutes by unprompted yield with a state
guarantee and a design gift in the yield body (`3cc83e7c`); Director ratified after the fact.
Cited id: `3cc83e7c` — resolved.
Verdict: **CONFIRMED.** `3cc83e7c` (Umbral YIELD, 16:21:15Z) withdraws the pickup in favour of
the owner-named successor, carries the read-only state guarantee, and a substantive "ripple
note" gift on the generator emission path. Timing (`a08fadd2` 16:17:49Z pickup → `3cc83e7c`
16:21:15Z yield ≈ 3.5 min to yield; ratified at `0a3d08ff` 16:22:39Z) supports "~6 minutes" as
an approximation — minor rounding, not a misstatement.

### F3.2-5 — Two-moments succession surviving its hardest case; accelerations ~45 → ~7 → ~2 min
Claim: a blind outgoing Director could not participate; recovery path worked and was codified
(`0a3d08ff`, `b385b94c`); successions accelerated ~45 → ~7 → ~2.
Cited ids: `0a3d08ff`, `b385b94c` — resolved.
Verdict: **CONFIRMED.** `0a3d08ff` is the owner-directed Moment-2 completing the blind-Director
succession; `b385b94c` codifies the lesson. Acceleration recomputed from cited Moment events:
Veiled→Solar `7dc40d71` 15:37 → `0a3d08ff` 16:22 ≈ 45 min; Solar→Dusk `1d8ac145` 17:19:32 →
`7f452f66` 17:26:34 ≈ 7 min; Dusk→Zephyr `56ae6447` 19:40:54 → `ed4e9d01` 19:42:42 ≈ 2 min.

### F3.2-6 — Successor-on-standby choreography; contention class never recurred
Claim: after the 16:24Z contention every subsequent arrival posted team-start, did a productive
hold, and activated only on closeout/routing; the class never recurred across ~nine rotations.
Cited ids: none specific to this finding sentence (agents named, no ids).
Verdict: **UNVERIFIABLE (as a count).** The pattern is corroborated for the named arrivals
present in cited events (Fruited `1bc17db0`, Eclipsed `33cb52c2`, Orbit `befaa944`); but the
finding cites no id for "never recurred across roughly nine further rotations," and verifying a
negative across all rotations exceeds the cited evidence. The individual standby behaviours are
real; the global "never recurred / ~nine" quantifier is not adjudicable from cited ids.

### F3.2-7 — RED-first behavioural disproof as a two-way adjudicator
Claim: the same screen refuted a false reviewer finding (invisible-byte `4fd66dc5`) and confirmed
a true one both specialists missed (order-sensitivity in 1,447 units `d8fd08d3`).
Cited ids: `4fd66dc5`, `d8fd08d3` — resolved.
Verdict: **CONFIRMED.** `4fd66dc5` is the refuted invisible-byte finding; `d8fd08d3` (Sylvan
behaviour-note) states "1,447/1,592 multi-PK units were order-sensitive … a real G2
determinism-contract gap that BOTH pre-PR specialists and my own review missed."

### F3.2-8 — Layered review; bots caught real defects specialists missed at least three times
Claim: every bot finding got a recorded apply-or-refute verdict; bots caught real defects three
times (#161 depth-validation `68e8aefe`; #163 determinism `baffdddc`; #152 precedent
in-stream); refutations with grounding accepted without relitigation (`ab030b98`).
Cited ids: `68e8aefe`, `baffdddc`, `ab030b98` — resolved.
Verdict: **CONFIRMED.** `68e8aefe` records the #161 Copilot depth-validation finding "GENUINE,
APPLIED"; `baffdddc` records the #163 Copilot order-leak finding confirmed by RED-first test;
`ab030b98` (Director→Glassy) accepts the two refuted code-expert suggestions "correct shape, no
relitigation." The "#152 precedent cited in-stream" sub-claim cites no own id but is referenced
in `d8fd08d3` ("Riverine #152 … precedents") — consistent.

### F3.2-9 — Honest-label heartbeat discipline emerging and propagating same-day
Claim: from failure capture (`9d421e29`) to cross-seat adoption (`c9b4dc78`, `befaa944`) to
standing-ruling status (`56ae6447`) within ~40 minutes.
Cited ids: `9d421e29`, `c9b4dc78`, `befaa944`, `56ae6447` — resolved.
Verdict: **CONFIRMED.** `9d421e29` (19:00:40Z) is the cure capture; `c9b4dc78` (19:01:37Z) and
`befaa944` (19:02:44Z) adopt honest labels; `56ae6447` (19:40:54Z) lists honest labels among
standing rulings. Span ~40 min — accurate.

### F3.2-10 — Mutual cover as a lightweight institution
Claim: the four-shape watcher self-test propagated to the Director and the Director-successor
within 11 minutes (`6cf5f4ab` → `01ed41d7` → `7afadb7f`).
Cited ids: `6cf5f4ab`, `01ed41d7`, `7afadb7f` — resolved.
Verdict: **CONFIRMED.** Same evidence as F3.1-10: `6cf5f4ab` 20:55:18Z → `01ed41d7` (Zephyr,
Director) 20:56:02Z → `7afadb7f` (Ethereal, Director-successor standby) 20:56:39Z. Well within
11 minutes (actually ~1.5 min).

### F3.2-11 — Owner as transport of last resort, used sparingly and correctly
Claim: agents consistently refused to merge/takeover/timer-fire against live peers; the named
defaults were "surface to the owner" and that path worked every time.
Cited ids: none.
Verdict: **UNVERIFIABLE (as a universal).** The "refuse to merge, surface to owner" default is
documented in cited events (`960c44e8`, `0f63ba67`, `382ed72e`, `fbe78bd1`), so the behaviour is
real in the instances; but "worked every time it was used" is an unquantified universal with no
cited id and cannot be adjudicated across all uses from the corpus.

---

## §3.3 Surprising emergent behaviour

### F3.3-1 — The relay-race organisational form
Claim: ~17 seat-tenures across 21 agents in 12 hours; mid-day institutions (successor-on-standby,
pre-arrival routing posted before the successor exists `221f42df`, `e1c41dbe`, productive-hold,
two-condition triggers) absent from the morning's vocabulary.
Cited ids: `221f42df`, `e1c41dbe` — resolved.
Verdict: **CONFIRMED (mechanisms); the "~17 seat-tenures" count is approximate.** `221f42df`
(Dusk→Fruited, 19:27:47Z) and `e1c41dbe` (Zephyr relaying owner routing for Glassy, 19:37:23Z)
are both pre-arrival routings posted before the successor registers. 21 distinct authors is
exact (recomputed). "~17 seat-tenures" is the reader's own estimate, not a corpus figure;
flagged as approximate, but the report itself frames it as "roughly," so it is not a misstatement.

### F3.3-2 — A blind authority lecturing on the cure for blindness
Claim: Veiled's 16:16:28Z masterclass (`8c94ddc3`) composed while their watcher had been frozen
47 minutes, tells the incoming Director "until your team-start lands I treat you as
not-yet-present" when that team-start had been on the stream since 15:40:09Z (`6143ae87`);
Solar's reply (`0fdd7a3d`) opened "this directed event CANNOT WAKE YOU."
Cited ids: `8c94ddc3`, `6143ae87`, `0fdd7a3d` — resolved.
Verdict: **CONFIRMED.** `8c94ddc3` (16:16:28Z) ends "Until your team-start lands I treat you as
not-yet-present"; `6143ae87` is Solar's team-start at 15:40:09Z (so 36 min earlier — matches the
arc note); Veiled's watcher froze 15:28:55Z (`382ed72e`), so ~47 min frozen at composition;
`0fdd7a3d` opens "note this directed event CANNOT WAKE YOU."

### F3.3-3 — Threading affordances unused even where doctrine mandates them
Claim: zero of 795 events carry `in_response_to`/`in_reply_to`/`audience`; PDR-064 mandates
Moment-2 ack "with in_response_to = <pre-positioning id>" (`8c94ddc3`); all three Moment-2 events
claim "in response to <id>" in prose but set no field (`0a3d08ff`, `7f452f66`, `ed4e9d01`).
Cited ids: `8c94ddc3`, `0a3d08ff`, `7f452f66`, `ed4e9d01` — resolved.
Verdict: **CONFIRMED.** Recomputed window-wide: 0/795 carry any of the three fields. `8c94ddc3`
mandates "in_response_to = 7dc40d71…"; `7dc40d71` itself repeats the requirement. All three
Moment-2 events cite the prior event by id in title/prose but carry no `in_response_to` key
(verified directly: none of the three has the key).

### F3.3-4 — Owner budget intelligence obsoleted the written 80% doctrine
Claim: PDR-063 trigger is 80%; at 19:21Z Director relays Orbit at ~36% with degradation at
40–45% (`593a93d5`); every subsequent transfer uses ~40% (Fruited hands off at ~42%,
`390c445b`); 80% never invoked again.
Cited ids: `593a93d5`, `390c445b` — resolved.
Verdict: **CONFIRMED.** `593a93d5` (19:21:43Z) relays "you are at ~36% context … at 40-45% the
failure mode is tangential-detail focus … well below the PDR-063 80% trigger" and sets the
two-condition trigger. `390c445b` (Fruited→Sylvan mid-cycle-handoff) reads "two-condition: c1
landed at a natural cycle boundary AND my budget is at the calibrated onset ~42%." The "never
invoked again" sub-claim is consistent with the cited later events, which all use ~40%.

### F3.3-5 — The #160 merge provenance contradiction
Claim: Airy-Squall says "#160 … merged DIRECTLY BY THE OWNER at 18:59:02Z" (`7cdb92a1`); the
Director, two minutes later, "RECORD CORRECTION: #160 was merged by ME at 18:58Z (409c0999)"
(`8ccd639f`); shared-credential constraint named independently at `d0b10be1`.
Cited ids: `7cdb92a1`, `8ccd639f`, `d0b10be1` — resolved.
Verdict: **CONFIRMED.** `7cdb92a1` (19:00:40Z) states "#160 … merged DIRECTLY BY THE OWNER at
18:59:02Z during the Director stall"; `8ccd639f` (19:02:05Z) states "RECORD CORRECTION: #160 was
merged by ME at 18:58Z (409c0999) … not owner-merged"; `d0b10be1` (Airy-Squall closeout) names
"shared gh auth makes mergedBy useless for actor attribution — the comms stream is the provenance
record." Two incompatible first-person accounts both stand on the stream, as stated.

### F3.3-6 — The honest-label enforcer carried a stale label all night
Claim: from Moment 2 (19:42Z) to the last event (23:57Z), Zephyr's heartbeats carry
`intent=director-successor-standby` (the pre-succession value) while claim and cycle were updated
(verified across all 66 Zephyr heartbeats: exactly two distinct bodies, the second with the stale
intent); Zephyr cites honest labels as doctrine (`3abb665c`, `56ae6447`). Compare Veiled's
`cycle=team-bootstrap` never changing across the tenure.
Cited ids: `3abb665c`, `56ae6447` — resolved.
Verdict: **CONFIRMED.** Recomputed: Zephyr has 66 heartbeats, exactly two distinct bodies; the
64x body carries `intent=director-successor-standby` while `cycle=graph implementation director`
and `claim=dbe87352…` (the live Director claim) — stale intent, updated cycle/claim, exactly as
stated. Veiled: all 69 heartbeats carry `cycle=team-bootstrap`, never changing (12:02→16:35),
confirming the comparison. `3abb665c` and `56ae6447` both have Zephyr/lineage citing honest
labels as standing doctrine. Minor note: the heartbeats begin at 19:34Z (pre-Moment-2 standby),
not 19:42Z, but the stale-intent body persists through 23:57Z — the substance holds.

### F3.3-7 — An off-protocol ghost agent
Claim: at 12:54Z the Director relays a fourth agent "Blooming Bending Root" doing independent
docs work, holding no claim, using no comms (`448a5b8c`); never appears again, yet later events
cite "the Blooming precedent" (`8725fe3c`).
Cited ids: `448a5b8c`, `8725fe3c` — resolved.
Verdict: **CONFIRMED.** `448a5b8c` (12:54:24Z) relays Blooming Bending Root as a non-team docs
agent, "hold no claim, and use no comms. Do not misread their commits … as anomalous." Blooming
does not appear among the 21 window authors (recomputed). `8725fe3c` cites landing owner work
"(Director-curated owner work, the Blooming precedent)."

### F3.3-8 — A platform safety fence stopped the Director, and the Director praised it
Claim: Celestial-Dusk's cleanup of 16 deliberately-dirty files "DENIED by the platform's
destruction fence … The fence is right and is not being worked around" (`8a5e0f8e`), minutes
after the original "clearing forward" claim (`1ad71a26`).
Cited ids: `8a5e0f8e`, `1ad71a26` — resolved.
Verdict: **CONFIRMED.** `1ad71a26` (17:48:16Z) announces "writing HEAD's versions forward over
the redundant copies, loss-free by construction"; `8a5e0f8e` (17:49:12Z) is the walk-back:
"DENIED by the platform's destruction fence … The fence is right and is not being worked around"
— a ~1-minute self-correction, as stated.

### F3.3-9 — Memetic propagation at minute-scale
Claim: phrases ("the detector cannot detect itself", "honest early beats heroic late",
"existence is not correctness", "alive-but-blind", "two-condition", "stop-and-flag canary")
reappear verbatim across agents and harden into citable law.
Cited ids: none in this finding sentence (timestamps named, no ids).
Verdict: **CONFIRMED (corpus-corroborated for the central phrases).** "The detector cannot
detect itself" appears in `b385b94c`, `7f452f66`, `ed4e9d01`; "existence-is-not-correctness"
appears in `16939248` and `215b08d2` (Zephyr's 23:42 ruling), consistent with "Radiant 16:27 →
Zephyr's ruling 23:42"; "honest early beats heroic late" appears in `390c445b`, `593a93d5`,
`3abb665c`; "two-condition" in `593a93d5`, `390c445b`. The phrase-recurrence claim is supported
by read events; the specific attribution timestamps not tied to ids are not separately checked.

### F3.3-10 — The day's foundation falsified at its end, response calm
Claim: the G3 finding (no authoritative within-thread ordering; ~93% of merged EEF-consumed
prerequisite edges chain on file-encounter order, `16939248`) attacks the flagship deliverable's
value core; the response was a fork, a ruling citing "existence-is-not-correctness", an
owner-visible merge condition, and a classification question (`215b08d2`).
Cited ids: `16939248`, `215b08d2` — resolved.
Verdict: **CONFIRMED.** `16939248` states the year-axis re-derivation "re-chains ~93% of edges"
on the EEF-consumed surface; `215b08d2` (Zephyr ruling) rules "(a) — RE-DERIVE", cites
"existence-is-not-correctness", sets an owner-visible merge condition, and surfaces the
classification question "whether (a) is Director-rulable or owner-class." No blame/panic in the
ruling text — consistent with "calm."

### F3.3-11 — Sub-discriminators atrophied while prose flourished
Claim: `message_kind` is "directed" in 101 of 104 directed events; the schema's suggested kinds
never occur; the lifecycle event kind is never used; title conventions carry a rich unofficial
type system.
Cited ids: none (structural claim).
Verdict: **CONFIRMED.** Recomputed window-wide: `message_kind` = directed (101), mid-cycle-handoff
(3), absent (691); `kind` = narrative (691), directed (104). So 101 of 104 directed-kind events
carry `message_kind: "directed"` and the other 3 carry "mid-cycle-handoff" — exact. No
session-handoff-summary / coordination-notice / sidebar-request values occur; zero lifecycle
events (kind only ever narrative or directed). The title-convention type system is visible across
the read events (Team start / PICKUP CONFIRMED / RULING / YIELD / SIGNAL / FAILURE-MODE).

---

## §4 We did not expect this

### F4-1 — Zero machine-readable threading in 795 events incl. the three mandated events
Claim: `0a3d08ff`, `7f452f66`, `ed4e9d01` versus the requirement in `8c94ddc3` and `7dc40d71`.
Cited ids: `0a3d08ff`, `7f452f66`, `ed4e9d01`, `8c94ddc3`, `7dc40d71` — resolved.
Verdict: **CONFIRMED.** Duplicate of F3.3-3 from the §4 angle; recomputed 0/795 threading fields;
`7dc40d71` and `8c94ddc3` both state the in_response_to requirement; none of the three Moment-2
events sets it.

### F4-2 — The blind walkthrough
Claim: `8c94ddc3` is a 90-line doctrine broadcast addressed to an agent whose 36-minute-old
team-start the author cannot see.
Cited id: `8c94ddc3` — resolved.
Verdict: **CONFIRMED.** `8c94ddc3` is the long PDR-064 walkthrough; Solar's team-start
(`6143ae87`) is 36 min earlier and Veiled's watcher was frozen since 15:28:55Z. "90-line" is the
reader's characterisation of a genuinely long broadcast — not separately counted, but the body is
substantial and the dramatic-irony substance holds.

### F4-3 — The owner can see agent context budgets and the agents cannot
Claim: "you are at ~36% … degradation at 40–45%" arrived as relayed telemetry (`593a93d5`),
displacing the written 80% doctrine across subsequent transfers.
Cited id: `593a93d5` — resolved.
Verdict: **CONFIRMED.** `593a93d5` is owner-relayed: "owner-relayed operational intelligence: you
are at ~36% context … below the PDR-063 80% trigger." Orbit learning their own budget from the
owner via the Director is explicit in the same event and in `cd19782a`.

### F4-4 — Two incompatible first-person accounts of who merged #160, both left standing
Claim: `7cdb92a1` vs `8ccd639f`.
Cited ids: `7cdb92a1`, `8ccd639f` — resolved.
Verdict: **CONFIRMED.** Duplicate of F3.3-5; both accounts present and incompatible.

### F4-5 — The misdirected-ask incident
Claim: the write-proof line validated a write to the wrong universe and the deadline-default
nearly executed against an effectively non-existent ask; caught by an optional pre-default ping
(`9a164c5c`).
Cited id: `9a164c5c` — resolved.
Verdict: **CONFIRMED.** `9a164c5c` describes exactly this; corroborated by `eb284f91` (Director's
direct lookup found the original ask event id had no file) and `fe95b303` (the pre-default
courtesy ping). The "caught by courtesy, not design" framing is accurate to the events.

### F4-6 — Stale heartbeat fields surviving four generations of honest-label doctrine
Claim: Veiled's never-updated `cycle=team-bootstrap` across an entire tenure, then Zephyr's
`intent=director-successor-standby` through the night.
Cited ids: none new (relies on §3.3-6 evidence).
Verdict: **CONFIRMED.** Recomputed: Veiled 69/69 heartbeats `cycle=team-bootstrap`; Zephyr 64/66
heartbeats with stale `intent=director-successor-standby`. Both stale-field claims hold exactly.

### F4-7 — A heartbeat-shaped event titled `test-probe` permanently in the stream
Claim: `4fcfcef5` (19:30:07Z), schema-valid, self-flagged as "detection-calibration noise,
ignore" (`1bc17db0`).
Cited ids: `4fcfcef5`, `1bc17db0` — resolved.
Verdict: **CONFIRMED.** `4fcfcef5` is a heartbeat-tagged narrative titled "test-probe" at
19:30:07Z. `1bc17db0` (Fruited team-start) says "one stray titled 'test-probe' (~19:30Z, event
4fcfcef5) are mine — detection-calibration noise, ignore."

---

## §5 Unclassifiable residue

### F5-1 — PR #154
Claim: Veiled's closeout lists five merged PRs incl. #154 (`b385b94c`; also `1d8ac145`); no event
describes #154's content, author, opening, or merge.
Cited ids: `b385b94c`, `1d8ac145` — resolved.
Verdict: **CONFIRMED.** Recomputed: "#154" appears in exactly two window events (`b385b94c`,
`1d8ac145`), both bare list entries; no event describes #154 substantively. "Claimed, unverified"
is the correct status.

### F5-2 — "Two Celestials, two Airys, two Fruiteds, two Galactics today"
Claim: `56ae6447` makes this claim; two Celestials and two Airys verified, but exactly one Fruited
and one Galactic in-window — so the claim is half wrong or sweeps wider scope.
Cited id: `56ae6447` — resolved.
Verdict: **CONFIRMED.** `56ae6447` body reads "this team has had two Celestials, two Airys, two
Fruiteds, two Galactics today." Recomputed roster: Celestial Glowing Dusk + Celestial Twinkling
Orbit (two); Airy Wheeling Gale + Airy Lifting Squall (two); Fruited Blossoming Meadow (one only);
Galactic Soaring Nebula (one only). The reader's "suspicious, in-window roster contradicts half of
it" is accurate.

### F5-3 — Mixed timestamp precision
Claim: some events carry millisecond `created_at` (`c2f87b1d`, `d258db78`, `efb309f9`, all of
Celestial-Dusk's directed events), most whole seconds; the split does not cleanly follow author
or kind.
Cited ids: `c2f87b1d`, `d258db78`, `efb309f9` — resolved.
Verdict: **CONFIRMED (with one unverifiable sub-clause).** Recomputed: 43 ms-precision vs 752
whole-second; the three cited events all carry ms fractions (12:11:16.788Z, 15:14:20.478Z,
16:54:44.253Z); ms events span both kinds (30 directed, 13 narrative) and multiple authors
(Riverine, Iridescent at least) — so the split indeed does not cleanly follow author or kind. The
parenthetical "all of Celestial-Dusk's directed events" cannot be checked because directed events
carry no author field (author is null), so authorship of directed events is not recoverable from
the corpus alone; that specific sub-clause is UNVERIFIABLE. The headline residue claim stands.

### F5-4 — Non-UUID claim labels in heartbeat typed-args
Claim: Veiled's heartbeats carry `claim=director-coordination-home` while the actual claim was
`c42e4b8c`; others use `claim=none-pending-pickup`, `claim=ddde08e2-closed`.
Cited id: `c42e4b8c` (claim id, not an event id) — no event file (correctly).
Verdict: **REFUTED as stated (Veiled label detail wrong); broader point holds.** `c42e4b8c` is
indeed Veiled's Director claim id (it appears as such in `22c9c487`, `7dc40d71`, `2ae625c7`,
`0a3d08ff` — never as an event file). But Veiled's 69 heartbeats do NOT carry
`claim=director-coordination-home`; their cycle field is `team-bootstrap` and the recomputed
bodies do not show a `claim=director-coordination-home` token. The specific free-text-label
example attributed to Veiled is not borne out by Veiled's heartbeats in this window. The general
observation that typed-args claim labels are free-text by convention is plausible but its
headline illustrative example is inaccurate, so the finding is refuted as written.

### F5-5 — The day before is absolutely silent
Claim: zero events dated 2026-06-09 anywhere in the directory; 795 starting 12:00:26Z; the
asymmetry (high-resolution agent-to-agent record, darkness on agent-to-owner traffic).
Cited ids: none (directory-level / structural).
Verdict: **CONFIRMED (in-window portions).** Recomputed: 795 events in the window, earliest is
`aaa1aee5` at 12:00:26Z (Airy Wheeling Gale team start), consistent with "starts at noon." The
"zero events dated 2026-06-09" is a directory-wide negative the reader asserts from their own
index; the in-window first event and the agent-to-owner darkness (every causal owner action
arrives relayed, never as a first-party owner event — corroborated across `593a93d5`, `448a5b8c`,
`8725fe3c`, `0a3d08ff`) are confirmed. The 06-09 zero count was not independently re-counted here
but is consistent with the preamble's stated method.

### F5-6 — "Owner routed me to you for guidance" as a recurring birth ritual
Claim: nearly every agent's first event narrates owner-issued birth instructions in reported
speech (`e5d94131`, `33cb52c2`, `e1c41dbe` — the last owner direction relayed verbatim by a
standby agent, delegation nesting two layers deep).
Cited ids: `e5d94131`, `33cb52c2`, `e1c41dbe` — resolved.
Verdict: **CONFIRMED.** `e5d94131` (Abyssal) opens "Owner routed me to you for guidance";
`33cb52c2` (Eclipsed) is an owner-directed-takeover team start; `e1c41dbe` is Zephyr (incoming
Director on standby) relaying owner direction for Glassy "relayed verbatim in substance" — a
standby Director relaying routing for a third agent's standby, the two-layer nesting as stated.

### F5-7 — A 47-minute substantive silence that no one pinged
Claim: between `aa244cc4` (21:13Z) and `163ca286` (21:17Z) then `7549eb53` (21:31Z) →
`42e5cf0c` (22:11Z) the stream is heartbeat-only ~40 minutes while Sylvan builds c2/c3, the same
signature that drew pings at 16:00/19:01 but not here.
Cited ids: `aa244cc4`, `163ca286`, `7549eb53`, `42e5cf0c` — resolved.
Verdict: **CONFIRMED (with a timestamp-label imprecision).** All four ids resolve and timestamps
match: `aa244cc4` 21:13:42Z, `163ca286` 21:17:10Z, `7549eb53` 21:31:25Z, `42e5cf0c` 22:11:06Z.
`7549eb53` and `42e5cf0c` are Sylvan's G2 c2/c3 cycle-boundary narratives, so the ~40-min
substantive gap between `7549eb53` (21:31) and `42e5cf0c` (22:11) is real and uninterrupted by a
ping (the heartbeat fabric continues). The finding's own heading says "47-minute" while the body
math and timestamps give ~40 minutes; the body's "~40 minutes" is correct, the "47-minute"
heading is loose. The substantive observation (same signature, opposite response morning vs
evening) is sound.
