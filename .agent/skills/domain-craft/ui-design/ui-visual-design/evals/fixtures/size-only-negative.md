# NEGATIVE control, SIZE arm ONLY

`no-bespoke-size-proposals` must FAIL; the CSS and prose arms must PASS.
No hex, no duration, no authored CSS — the only defect is that the critique
answers a sizing problem by inventing different sizes.

The pill is too short and its label is too small. Set the control height to
48px and change the label to 1rem so it reads at arm's length.
