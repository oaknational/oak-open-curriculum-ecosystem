# POSITIVE control: word-boundary bleed

All three arms must PASS. Every sentence below quotes or describes a value;
none prescribes one. The unanchored lead-in alternation flagged all four,
matching `use` inside "because" and "paused", `set` inside "offset", and
`try` inside "entry" — this fixture pins the `\b` anchors that stop that.

The ranking is legible because the 32px height sits visibly below its
neighbours. The demo paused at 600ms, half-way through the fade. The
offset shadow sits beside the 32px pill. Each entry in the lesson list is
32px tall.
