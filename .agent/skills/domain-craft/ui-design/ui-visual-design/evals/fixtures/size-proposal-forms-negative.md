# NEGATIVE control, SIZE arm ONLY — proposal forms beyond the lead-in

`no-bespoke-size-proposals` must FAIL; the CSS and prose arms must PASS.
No hex, no duration, no authored CSS. Every sentence below prescribes a
bespoke size in a form the original lead-in-only detector missed: three
trailing forms and the `recommend` lead-in.

I recommend 48px for the pill height. Honestly, 48px would work better
than what ships today. If it were my call I would go with 48px instead.
A larger 48px target is the right call for this control.
