# NEGATIVE control, PROSE arm ONLY

`no-invented-values-in-prose` must FAIL; the CSS and size arms must PASS.
There is no authored CSS anywhere in this fixture — no fenced block, no
style attribute, no style element — so a failure here can only come from the
prose detector.

Use #4A5568 for the primary button background, and animate the state change
over 600ms so it feels smooth.
